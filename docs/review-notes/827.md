- 2026-09-15 · #827 · fail-open default · Found by the substitute review: `syncCodexHookTrust` caught every `config.toml` write failure, logged it and RETURNED, so `writeCodexHooksAndTrust` reported a postcondition — hooks written and trusted — it had not achieved, and all four provider spawn paths plus the peer runtime continued straight into an app-server. The resulting state is the exact one this PR exists to eliminate: hooks.json on disk, no matching trust entry, codex loading the handlers, reporting them untrusted, never dispatching them, and starting anyway. The peer path's own fail-closed catch was already there and was defeated by the inner swallow, which is the tell — a caller that guards a call the callee has already decided not to fail. A write whose only purpose is to install a guard cannot treat its own failure as non-fatal, because "the container still boots" is the failure, not the mitigation · `syncCodexHookTrust` throws; `writeCodexHooksAndTrust` propagates, so the spawn paths abort and the peer catch selects `FAILED_CODEX_HOME`; the post-registration re-sync returns the same sentinel (`codex plugin add` has already rewritten config.toml by then, so a failure leaves no entries rather than the earlier ones), and only the startup sync in `setupCodexPrimaryRuntime` records the failure into the registration result's error list instead of throwing, because the spawn enforces the invariant where it actually matters (`container/agent-runner/src/codex-companion-setup.ts`); the read-only-config.toml case is driven through `writeCodexHooksAndTrust` in `container/agent-runner/src/codex-companion-setup.test.ts`, mutation-checked against restoring the swallow
- 2026-09-15 · #827 · unescaped interpolation · Recurrence of #822's registered class in a new file, found by the substitute review and reproduced against the container's own pinned codex 0.154.0: `tomlQuotedKey` escaped backslash, quote, `\n`, `\r` and `\t` and stopped there, while a hook state key embeds the plugin's own relative hook filename and codex accepts a manifest declaring any legal Linux name — a form feed included. TOML forbids raw C0/DEL in a basic string, and codex answers one such byte with "Invalid configuration; using defaults" and then STARTS: `hooks/list` returns nothing, so the whole PreToolUse/PostToolUse guard chain vanishes along with the plugin hook. The write succeeds, the launch succeeds, only the protection is gone. The repo already had a complete encoder carrying this exact lesson (upstream 05860324c) one file away; a second, partial one was written beside it rather than the first being reused, which is what the class is · both callers now go through one `escapeTomlBasicStringBody` in `container/agent-runner/src/providers/codex-app-server.ts` — split out of `tomlBasicString` so keys get its full C0/DEL escaping without its newline REFUSAL, which is right for a value the operator authored and wrong for a filename this process was merely handed — with form-feed, NUL, DEL and ESC keys round-tripped in `container/agent-runner/src/providers/codex-hook-trust.test.ts`, mutation-checked against restoring the partial escape set
- 2026-09-15 · #827 · deferred hardening · One substitute-review finding recorded rather than fixed: the trust collector covers only part of codex 0.154.0's hook grammar — `PermissionRequest` is absent from the event map, a declared matcher is hashed for `UserPromptSubmit`/`Stop`/`Interrupt` where codex normalizes it away, `mcp_tool` handlers are skipped, inline hook objects keyed `plugin.json#hooks[index]` are not read, and an invalid path declaration is accepted instead of falling back to the conventional file. Every currently mounted registered plugin uses the covered shapes and the real binary reported both generated guards trusted; re-raise before any registered plugin adopts one of the omitted forms · #830
- 2026-09-15 · #827 · enforcement scope · Found by Codex review r1 and then measured against the binary: `declaredPluginHookFiles` read the .codex-plugin manifest and, when that manifest declared no `hooks`, fell THROUGH to the .claude-plugin manifest and trusted whatever that declared — while codex 0.154.0 selects the Codex manifest alone. Three fixtures registered through the real `codex plugin marketplace add`/`plugin add` path and read back through `hooks/list`, with a control in the same run, settle it: a Codex-declared file loads; the same file declared ONLY in the Claude manifest loads nothing at all; and a Codex manifest without `hooks` plus a Claude manifest declaring `hooks/declared.json` loads `hooks/hooks.json`, never the declared one. The real `wwbd@davekim917-bootstrap` is the middle shape — codex reports zero hooks for it — so the PR's own blast-radius table listed a trust row keyed on `hooks/wwbd-hooks.json`, a file codex never reads, and recorded as covered a hook that does not run. Nothing over-trusts: the row names a file that is never loaded. The lesson is that the precedence was taken from a code comment and pinned by a unit test written from the same belief, which is the oracle agreeing with the code rather than with the tool — the same shape as #817, one layer up · the Claude-manifest branch is gone (`declaredPluginHookFiles` reads the .codex-plugin manifest only, then the conventional fallback — every registerable plugin has that manifest by construction, since `readCodexPluginEntryName` is what admits it), with the two measured shapes pinned as `ignores a hooks declaration that only the Claude manifest makes` and `takes the conventional file when the Codex manifest declares none, even if the Claude manifest declares one` in `container/agent-runner/src/providers/codex-hook-trust.test.ts`, mutation-checked against restoring the fall-through
- 2026-09-15 · #827 · fail-open default · Recurrence of this fragment's own first line on the READ side, found by Codex review r2 after the write side was fixed — and worse than it, because nothing fails: `syncCodexHookTrust` read config.toml under a bare `catch { existing = '' }`, so a file that is unreadable but writable (mode 0200, a mount that kept write and lost read) was treated as absent and the rewrite then SUCCEEDED from an empty base, deleting `[features] hooks = true` along with the marketplace and plugin tables. Codex with that flag gone loads no hooks at all, the write reported success, and the spawn continued — the guard chain inert and silent again, reached from the opposite end of the same function. The audit the finding prompted turned up a second, unflagged site of the same class with a wider blast radius: the primary config that feeds every OAuth fallback home was read as `existsSync(p) ? readFileSync(p) : ''`, and `existsSync` answers false on EACCES as readily as on absence (#818 measured this at uid 1001), so one unreadable primary projected an EMPTY config over every fallback home in a single pass — the homes a rotation is about to switch to. Both reads exist only to rewrite the file from what they read, which is what makes the two-answer read wrong: it does not lose a read, it loses the file · one `readCodexConfigToml` in `container/agent-runner/src/codex-companion-setup.ts` answering three ways — contents, `''` for ENOENT and ENOENT alone, throw for everything else — used by all three config.toml reads, with the fallback projection skipped rather than emptied when the primary cannot be read; pinned in `container/agent-runner/src/codex-companion-setup.test.ts` by the helper's own three cases, an end-to-end refusal that asserts the file still holds its feature flag afterwards, and `is the only way this module reads a config.toml`, which fails if the `existsSync ? read : ''` shape returns at ANY site — mutation-checked against restoring the swallow (3 tests) and against restoring the projection-site shape (1)
- 2026-09-15 · #827 · deferred hardening · Codex review r3 returned two P1s and `codex-review.sh churn` flagged the round CHURN, which is the correct read: all four findings across the three rounds are one invariant reported at one more site per round — the generated container config.toml must not survive any writer's failure without `[features] hooks = true`, since codex without that flag loads no hooks, starts anyway, and leaves the guard chain inert and silent. Rounds 1 and 2 closed the two sites inside code this PR introduced (the trust write, then the trust read). The two left are both on `writeCodexMcpConfigToml` in `container/agent-runner/src/providers/codex-app-server.ts`, which this PR does not touch and which carries them byte-identically on origin/main: the same two-answer read under `catch { base = '' }`, and a plain `writeFileSync` that truncates in place. The MCP writer runs immediately before the trust writer on every spawn, so it truncates first and a later query then preserves the flagless base and spawns unguarded — meaning a temp-file-and-rename on the trust writer ALONE would buy nothing while the same file is destroyed two lines earlier. That is the per-call-site guard the churn gate exists to refuse, and it is why effectiveness, not only scope, kept both out of this PR. Recorded rather than fixed: this PR does not make the condition worse — before it the guard chain never fired in any Codex container · #831, naming the one durable-write primitive both writers should go through (three-answer read, render whole, sibling temp file, fsync, rename, plus a post-write assertion that the committed file still carries the flag) and carrying the re-raise trigger — a Codex container spawning with its hooks/list empty while hooks.json is on disk, a live config.toml found without `[features]`, or a third finding on a config.toml writer
- 2026-09-15 · #827 · deferred hardening · Recorded on operator routing, not raised by a reviewer: this PR computes codex's hook identity hash offline and trusts that the entries it writes will match, but every way that reproduction can drift produces the SAME silence the PR exists to remove — a mismatched hash makes codex report the handler untrusted and never dispatch it, with no failure, no log and a normal-looking session. The offline oracles are real hashes codex wrote, which is the strongest available check and still only proves agreement at one codex version with the shapes the collector covers; a container pin that moves independently of the host binary is #817's class verbatim, and the uncovered hook shapes are already listed in #830. Nothing asserts at runtime that the handlers came back trusted · #832, to query the hooks/list RPC on the same connection after `initializeCodexAppServer` and refuse the spawn when a handler NanoClaw GENERATED reads back anything but trusted/managed (a plugin hook reading untrusted is reported, not fatal, so one odd third-party plugin cannot take a container down); re-raise trigger — the container's codex pin moving, a registered plugin adopting one of #830's uncovered shapes, or a container observed running a turn with its generated chain untrusted
- 2026-09-15 · #827 · deferred hardening · Also recorded rather than fixed, and classified non-blocking because it fails closed: trusting the registered plugins' hooks means a Codex container now runs TWO PreToolUse handlers per tool call — the generated `bun /app/src/codex-hooks/cli.ts PreToolUse` and `bootstrap-workflow-agents`' own `codex-guard.ts`. Before this PR neither fired, so the overlap was invisible; the PR body's live proof shows `hook/started: 2` on a benign turn. It is not merely duplicated work: inside a container both session DBs exist, so `IS_NANOCLAW` is true and the plugin adapter does not fall back to codex's native ask — it calls `runNanoclawGate`/`runEmailGate` and stages a session-DB approval, as the generated chain does for the same command, so one gated command can raise two cards each needing its own answer. What is NOT measured is whether codex dispatches the second handler after the first denies, and in what order; both being capable of staging is read from the two sources, the interleaving is not, and the right fix depends on it · #833, with de-duplication at the approval layer on the `tool_use_id` both handlers see as the option that also survives a third handler appearing; re-raise trigger — a human reporting two cards for one command, or approval-card volume on Codex groups rising after this deploys
- 2026-09-15 · #827 · risk coverage · `codex-review.sh scope` answered `skip` for this PR — no changed path matches a `risk:high` glob — even though it changes whether the destructive-action guard chain executes at all in every Codex container. The rounds only happened because `review:requested` was added by hand, which means the same change made by someone who did not think to add it would have merged unreviewed. The paths that should carry `risk:high` are the container provider and hook surfaces that decide whether a guard runs: `container/agent-runner/src/providers/codex-hook-trust.ts`, `container/agent-runner/src/providers/codex-app-server.ts`, `container/agent-runner/src/codex-companion-setup.ts`, and `container/agent-runner/src/codex-hooks/**` — the Codex-side counterpart of the `risk:guard` dimension the host already has · deferred to the operator rather than changed here, because a `.github/labeler.yml` edit is itself `risk:high` and widening the review surface is a policy call, not a review-loop one; re-raise trigger — the next `skip` verdict on a PR touching any of those paths
- 2026-09-15 · #827 · deferred hardening · Round-4 substitute review (`gpt-5.6-sol high`) returned one blocker, and it was the PR BODY rather than the code: the body claimed `normalizeCommandHandler` "reproduces exactly" codex's normalization, and it does not at the numeric boundary — `timeout` is a `u64` and `additionalContextLimit` a `usize` on the Rust side, codex retains the exact integer before hashing, and `JSON.parse` rounds anything past 2^53, so a plugin legally declaring `timeout: 9007199254740993` hashes to `sha256:926150…` on codex and `sha256:d1fc56…` here, measured against 0.154.0. The handler then reads back untrusted and is silently never dispatched — this module's whole failure mode. It is the review-policy PR-body-contradiction exception rather than destructive severity: no mounted plugin is within orders of magnitude of that range, so the exposure is a claim of exactness the code does not hold. Correcting the body was the right branch precisely because it costs no push and leaves the reviewed head standing, and the same review confirmed the four live hashes trusted against a fresh `hooks/list` probe. Also recorded there: a declared hooks path containing `..` is a DEAD key rather than an execution widening, since codex rejects parent components before loading and takes the conventional fallback — the same "trusted a file codex does not load" shape round 1 fixed for the Claude-manifest fall-through · body narrowed to safe-integer/current-fleet shapes with the measured digests, both numeric fields and the traversal case appended to #830 with the fix located at the plugin-hook reader/hash boundary (parse losslessly, or refuse outside the safe-integer range — noting that a hard throw over one absurd plugin value is the `over-broad guard` class), and #832 is the general runtime check that catches this class, and a future codex normalization change, rather than each instance; re-raise trigger — any plugin declaring a hook timeout or context limit outside the safe-integer range, or #830 being worked
- 2026-09-15 · #827 · deferred hardening · Round-5 substitute review (`gpt-5.6-sol high`) returned `changes` on the body again, and was right again: round 4's correction narrowed the claim to "exact for every shape the collector covers whose numeric fields are safe integers", and that is still false — a declared matcher on `UserPromptSubmit`/`Stop`/`Interrupt` is hashed here (`codexHookTrustHash` adds it whenever it is non-null, with no event filter) where codex normalizes it away, which this fragment's own first line already recorded. Two body corrections in a row on one claim is the tell: narrowing an exactness assertion re-asserts it at a new boundary, and the next reviewer finds the next boundary. So the claim is gone rather than narrowed a third time — the body now states what is VERIFIED (three codex-written oracle hashes byte-exact, plus a fresh hooks/list probe reporting all four live handlers trusted, which is every hook the fleet loads) and tables the known divergences by name with #830 as their home. All four fail identically — handler reads back untrusted, silently never dispatched — which is what makes #832 the general answer instead of closing them one at a time · the exactness assertion is removed from the PR body in favour of a verified-vs-divergent split; the divergences are enumerated on #830, and #832 carries the runtime check that catches the whole class including a future codex normalization change; re-raise trigger — a registered plugin adopting any tabled shape, or #830 being worked
- 2026-09-15 · #827 · pattern audit · Round-6 substitute review (`gpt-5.6-sol high`) confirmed the hash-exactness section converged and found the body stale in a DIFFERENT place: the "Not done / notes" list still said the `.claude-plugin` hooks fallback "is implemented but unreachable in practice", which round 1 had deleted from the code, and it still said `impeccable` is "not registerable for Codex" when it carries both a `.codex-plugin/plugin.json` and an `.agents/plugins/marketplace.json` and does register — it contributes no entry because its TOP-LEVEL Codex manifest declares no hooks and its hook-bearing `hooks/hooks.json` files live in nested `plugin/`/`cursor-plugin/` sub-packages the top-level manifest does not select. A third stale claim went unreported: the cited `codex-app-server.ts:485-490` had shifted to `:510-515` because this PR added to that file. The lesson is #729's registered class applied to PROSE: a code change that deletes a branch has to be grepped across every sentence that describes the branch, and a body is a set of claims with more than one site, so fixing the two a reviewer names and not sweeping the rest is how the next round finds the third. Three consecutive body-only `changes` verdicts on one PR, each on a different claim, is the measurement · all three corrected in one pass after auditing the whole body rather than the named lines — the fallback note deleted, the `impeccable` paragraph rewritten to say it registers and why it still contributes nothing, and the line reference re-anchored; the code has been byte-identical since `9e91149f7` through three substitute rounds, which is what isolates these as prose defects rather than code ones
