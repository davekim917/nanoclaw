# Pre-Build Drift Report — mnemon-rearchitecture

> Generated by `/team-drift` at Stage C of `/team-auto`.
> SOT: `docs/specs/mnemon-rearchitecture/design.md` (1300 lines, cycle-3-finalized)
> Target: `docs/specs/mnemon-rearchitecture/plan.md` (1666 lines)
> Two extractors: Agent A (Claude Sonnet) + Agent B (Codex GPT-5.5, read-only sandbox)

## Summary

| Verdict | Count |
|---------|-------|
| CONFIRMED | ~111 (B1 reclassified after fix) |
| PARTIAL   | 2 |
| DIVERGED  | 3 (all acknowledged) |
| MISSING   | 0 |

`MISSING == 0 && effective_DIVERGED == 0` → **gate passes** (B2/B3/B4 acknowledged in `drift-acks.json`; B1 was found and fixed inline pre-acknowledgment).

## Findings fixed inline before report finalization

- **Plan footer "SKELETON — group sections being filled"** (Codex Claim #85): plan trailing line was stale boilerplate from the skeleton-then-edit pattern despite all 7 groups being fully populated. Edited plan footer to `**Status:** FINALIZED — all 7 group sections populated, file-conflict and constraint-traceability checks passed.` Re-classified to CONFIRMED — no longer drift.

## DIVERGED entries

### [B2] `should-recall.ts` consolidated into `recall-injection.ts` instead of separate file

**Class:** DIVERGED
**Source:** Agent A Claim #90. SOT §Cleanup Inventory line 1136 lists `src/modules/memory/should-recall.ts` as a new file; SOT §Test Plan line 1232 references `should-recall.test.ts`. Plan exposes `shouldRecall(text)` from `recall-injection.ts` and tests it under `recall-injection.test.ts`.
**Resolution:** Acknowledged in `drift-acks.json` (id=B2). See ack reason below.

### [B3] Memory module barrel `src/modules/memory/index.ts` not created

**Class:** DIVERGED
**Source:** Agent A Claim #91. SOT §Cleanup Inventory lines 1137-1138 list `src/modules/memory/index.ts` (barrel) and `index.test.ts` as new files. Plan File Ownership Map omits both.
**Resolution:** Acknowledged in `drift-acks.json` (id=B3). See ack reason below.

### [B4] systemd unit `Requires=` differs between SOT unit block and plan G4

**Class:** DIVERGED
**Source:** Agent A Claim #68. SOT §Detailed Design 3 line 848 shows `Requires=ollama.service`. Plan G4 line 1568 + ASSERT line 1588 specify `Requires=nanoclaw-v2.service ollama.service`.
**Resolution:** Acknowledged in `drift-acks.json` (id=B4). The plan reflects the cycle-2 S1 "fate-share with parents" decision recorded in design's review-resolutions table; only the design's literal unit block didn't get updated. Plan is the corrected version.

## PARTIAL entries (review-only, non-blocking)

### [P1] `extractRecallQueryText` exported but specific char-cap numbers (500/800) and "last 3 user-runs" rule not restated in plan

**Source:** Agent A Claim #15 / Codex Claim #61. SOT §Detailed Design 2 lines 596-599 specify the exact strategy. Plan D2 names the function but defers numerics to implementation.
**Resolution:** Plan defers detail to implementation per `/team-plan` interface-spec convention. Builder reads design's pseudocode for numeric constants. No fix needed; flagged for builder awareness.

### [P2] G3 / F3 don't explicitly call out "write unhealthy health JSON on prereq failure"

**Source:** Codex Claim #79. SOT implies it (C25); plan G3 only says "exits non-zero." F3 ASSERT covers exit-non-zero behavior.
**Resolution:** Acceptable — health-JSON writing is an E4/F3 implementation detail. Builder can elect to write a one-shot unhealthy snapshot before exit.

## Internal SOT inconsistencies (not drift; SOT itself is layered with cycle-2/3 corrections)

The design document accumulated cycle-2 and cycle-3 review resolutions as a top-of-file table while keeping older body text unchanged in places. The plan correctly follows the cycle-resolution table:

- **C3 broad "every invocation" wording** (early SOT) vs **chat/chat-sdk/webhook narrowed** (cycle-2 Q1 resolution): Plan follows narrowed.
- **C13 PK shape** `(message_archive_id, classifier_version)` (early SOT) vs `(agent_group_id, user_run_first_id, classifier_version, prompt_version, is_orphan)` (cycle-2 M6 resolution): Plan follows latest.
- **agent-to-agent included** (cycle-2 Q1 chat-only narrowing) vs **`channelType === 'agent'` excluded** (M5 cycle-3 detail): Plan follows M5 cycle-3 (agent-to-agent excluded).

These are SOT-internal contradictions, not target drift. Codex flagged them as PARTIAL for transparency; resolved as CONFIRMED at lead level.

## Gate

```
MISSING:  0
DIVERGED: 4 — 1 fixed inline (B1), 3 acked (B2, B3, B4) → effective DIVERGED: 0
PARTIAL:  2 — non-blocking
CONFIRMED: ~110
```

**Gate status: PASS** — proceeding to Step 3 (team creation).
