- 2026-09-22 · #999 · untested production path · Four review rounds, four real defects, and a green suite through every one of them — the tests exercised the code path the author built, never the path production takes, so "all tests pass" carried no information about whether the feature worked. Round 2 found the feature DEAD on every default install: agents reply through the `send_message` MCP tool whenever outcome reporting is on, which is the fleet default (`effectiveOutcomeReporting` returns true unless explicitly disabled, `src/container-config.ts`; the prompt directs it at `destinations.ts`), and that handler writes its chat row straight to `writeMessageOut` (`mcp-tools/core.ts`) without ever entering `sendToDestination`, where the stamping lived. Every test drove the `<message to="…">` envelope, so the majority path had no coverage at all. Round 4 found the same shape one field over: model was read from the provider's RESOLVED value while effort was read from `querySettings.effort`, which `applyFlagBatch` documents as "USER INTENT ONLY" — so a group carrying its effort in `container.json` requested nothing and the line showed no effort, and a sticky effort the resolved model cannot support is clamped away by the provider, so the line named a level the turn never ran at · the decision lives in one function (`stampStatusSubtext`, `container/agent-runner/src/turn-status.ts`), called through `withStatusSubtext` at each call site that writes agent-composed text (`sendToDestination` and `deliverErrorResult` in poll-loop, `send_message` and `edit_message` in `container/agent-runner/src/mcp-tools/core.ts`) and gated on an explicit `agentReply` opt-in, because round 3 showed `kind === 'chat'` is far broader than a reply the agent composed (`send_file` captions and the runner's own `/clear` notice are routed chat rows too); the turn's state is persisted to session state rather than held in memory, because `send_message` runs in a separate MCP subprocess (#1022 — the fifth miss, found only by reading live replies after deploy: 55 of 57 went out unstamped); and a REQUIRED `resolvedEffort` was added to AgentQuery, per the warning above `resolvedModel` in `container/agent-runner/src/providers/types.ts`; requiring it immediately caught the mock provider. Pinned by tests each verified to FAIL without its fix, including one built from the exact row shape `send_message` writes (no envelope, no `sendToDestination`), one driving two turns through a single `processQuery` to catch a per-query clear standing in for a per-turn one, and two reproducing the effort failures from a provider's resolved values (`container/agent-runner/src/poll-loop.test.ts`, `container/agent-runner/src/turn-status.test.ts`, `container/agent-runner/src/mcp-tools/core.test.ts`)
