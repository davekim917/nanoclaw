- 2026-09-16 · #866 · fail-open default · Deleting the last file from `container/agents/` deletes the directory, because git does not track an empty one, so the def-sync's `existsSync` early return had to go or every group's retired `worker-frontier.md` would sit in `.claude-shared/agents/` forever. The replacement wrapped `readdirSync` in a bare `catch` that answered "trunk ships no defs" — which is the ENOENT answer, and also the EACCES, ENOTDIR and transient-I/O answer, and the next line of that function is a prune. A read FAILURE is not an answer about what the source contains, and when the code that consumes the answer deletes files, reading failure as emptiness converts an unreadable directory into a destructive prune · only ENOENT yields the empty set; every other error rethrows to the call site, which already logs and spawns without the roster so the group's files survive (`src/container-runner.ts`), with an EACCES case in `src/provider-surfaces.test.ts` that spies `readdirSync` to fail that one path and asserts the seeded def is still on disk
- 2026-09-16 · #866 · mutation coverage · When a derived list became a hand-stated one, the test written for it asserted that every id it returns satisfies the alias guard — which is true whether or not the guard runs, because the ids committed to the file are valid either way. Deleting both guard loops from `computeReviewerModelIds` left it green. A guard over hand-maintained data needs an oracle that proves the guard is REACHED, and an input the code must refuse is the only thing that does · the two rosters are parameters defaulted to the real ones, so the tests drive the function with an alias in each and expect the throw naming that roster (`scripts/reviewer-models.ts`, cases in `scripts/reviewer-models.test.ts`); deleting either loop turns the matching case red
- 2026-09-16 · #866 · deferred hardening · Deploying this before the bootstrap checkout updates leaves the plugin still shipping `worker-frontier.md`, and the codex-sync watcher runs a full sync on startup while the deploy restarts it — so every group's managed `worker-frontier.toml` is rewritten without the model pin this PR deletes, and the role inherits the parent model until the plugin drops it. Recorded rather than fixed: retaining the deleted map resurrects what the PR removes, and a retired-name prune in `src/codex-sync.ts` is the right end state but is new machinery in a file this diff does not touch (`docs/review-policy.md`, Fix discipline) · #867 (re-raise if the bootstrap orchestrate rework is not installed alongside this deploy, or if any group is observed dispatching `worker-frontier` on Codex after it)
