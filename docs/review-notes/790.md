- 2026-09-13 · #790 · fail-open default · An inbound row outside the recognized prose and question-response shapes was silently omitted, allowing a source-complete extract to look like a no-traffic window. Count it as explicit unknown evidence. · `scripts/operator-attention-evidence.ts`
- 2026-09-13 · #790 · metric definition · `deliveryProcessedUnknown` is defined by a null platform id, not a successful delivery status or a matching outbound row; count every time-windowed delivered row with that null marker before the outbound join and status-specific classification. · `scripts/operator-attention-evidence.ts`
