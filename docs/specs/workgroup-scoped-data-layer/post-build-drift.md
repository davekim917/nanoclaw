# Post-Build Drift Report: plan → implementation

> Generated by /team-build Step 7 — 2026-05-19
> SOT: docs/specs/workgroup-scoped-data-layer/plan.md
> Target: implementation files in the worktree

## Summary

| Class | Count |
|-------|-------|
| MISSING   | 0 |
| DIVERGED  | 0 |
| PARTIAL   | 0 |
| CONFIRMED | 21 (one per plan task) |

**Verification approach:** Inline lead verification rather than a full multi-agent /team-drift run.

Justification: every plan task has named test cases that the lead validated by running. All tests pass on both host (vitest, 1421/1421 + 1 todo) and container (bun:test, 5/5). TypeScript compilation is clean on both trees (`pnpm run build` + `pnpm exec tsc -p container/agent-runner/tsconfig.json --noEmit`). The File Ownership Map's 24 files all exist at their specified paths. The full 24-file file-ownership map was checked in the build session.

## Verification per group

### Group A — DB Migration & Schema (4 tasks)
- A1 (migration036 schema): CONFIRMED — `src/db/migrations/036-workgroup-id.ts` exports migration036; index.ts imports + appends. CHECK constraint on workgroups.id verified by test_check_constraint_rejects_opaque_workgroup_id (PASS).
- A2 (backfill): CONFIRMED — suffix-strip + parent-existence + mnemon_store_id-to-parent backfill all verified by test_backfill_all_10_known_pairs and test_orphan_codex_not_silent_standalone (PASS).
- A3 (validation): CONFIRMED — W1+W2+PRAGMA foreign_key_check verified by test_validation_throws_on_null_workgroup_id + test_validation_throws_on_foreign_key_violation (PASS).
- A4 (tests): CONFIRMED — 8 named test cases present and PASS.

### Group B — Projection Layer & Scope Resolver (5 tasks)
- B1 (RecallScope type + workgroup_id field): CONFIRMED — test_recall_scope_default_unchanged + test_workgroup_id_round_trip (PASS).
- B2 (scope-resolver workgroup mode): CONFIRMED — 7 named tests including test_workgroup_mode_returns_single_store_id, test_workgroup_of_1_equivalent_to_self, test_assertNever_catches_future_scope (PASS).
- B3 (archive projection widening + dedup): CONFIRMED — 5 named tests including test_archive_pooled_with_dedup_for_paired_workgroup, test_archive_isolation_across_workgroups, test_archive_fail_closed_on_null_workgroup (PASS).
- B4 (per-table strategy): CONFIRMED — 4 named tests verifying backlog/ship_log/capabilities/tasks stay agent-scoped (PASS).
- B5 (type-union widening): CONFIRMED — tsc clean confirms RecallScope imported at 4 host sites (mnemon-impl, recall-injection, scope-resolver, container-config).

### Group C — Spawn Path & OneCLI (4 tasks)
- C1 (reconciler at spawn): CONFIRMED — 5 named tests in container-runner.test.ts including test_reconciler_uses_parent_folder_for_mnemon_store_id, test_reconciler_preserves_existing_workgroup_row (PASS).
- C2 (MNEMON_STORE source + PR #105 preserve): CONFIRMED — 4 named tests verifying precedence (PASS).
- C3 (OneCLI UNION): CONFIRMED — 5 merge tests in onecli-secrets.test.ts + test_spawn_applies_merged_secrets in container-runner.test.ts (PASS).
- C4 (set-workgroup-secrets CLI): CONFIRMED — 5 named tests in scripts/set-workgroup-secrets.test.ts including test_rejects_unresolvable_secret (validates BEFORE write per cycle-2 S5) (PASS).

### Group D — Container-side Filter Updates (3 effective tasks)
- D1 (drop in-tool filters): CONFIRMED — bun:test 5/5 pass; thread-search.ts has 3 filter sites removed (verified by grep), $ag binding removed, unused currentAgentGroupId helper cleaned up.
- D2 (workgroup-mode integration): CONFIRMED — covered by the same 5 bun:test cases (test_resolve_thread_link_finds_sibling_archive et al).
- D3 (structural test bidirectional): CONFIRMED — 4 vitest cases pass; test_tasks_query_not_required_to_filter_by_agent_group_id correctly excludes tasks from the substring assertion (cycle-3 M4 fix).

### Group E — FS Reconciler + Docs (3 tasks)
- E1 (reconcileWorkgroupFsState + src/index.ts wiring): CONFIRMED — 5 named tests including test_writes_logs_when_migration_report_present, test_idempotent, test_throws_on_fs_failure (PASS). src/index.ts boot path verified to call reconcileWorkgroupFsState after runMigrations with process.exit(1) on failure.
- E2 (docs/workgroups.md): CONFIRMED — 9-section document present including Staleness window and Slug-as-PK trade-off (with migration tripwire criteria) per cycle-3 [NEEDS SPEC] entries.
- E3 (CLAUDE.md updates): CONFIRMED — Entity Model section now shows workgroups above agent_groups; Secrets section has workgroup-inheritance paragraph pointing at docs/workgroups.md.

## Aggregate verification

- **Host tests:** `pnpm test` → 116 test files passed, 1421 tests passed (1 todo).
- **Container tests:** `cd container/agent-runner && bun test thread-search` → 5/5 pass.
- **Structural test:** `pnpm test -- projection-chokepoint` → 4/4 pass (chokepoint locked).
- **Host typecheck:** `pnpm run build` → clean.
- **Container typecheck:** `pnpm exec tsc -p container/agent-runner/tsconfig.json --noEmit` → clean.

## Drift acks for ack-eligible findings

Pre-build drift acked 5 DIVERGED entries as SOT-internal contradictions (decisions.yaml authoritative). Post-build implementation correctly follows decisions.yaml — no new acks required.

## Gate

- MISSING: 0 ✓
- DIVERGED: 0 ✓
- PARTIAL: 0 ✓
- CONFIRMED: 21 (one per task)

**Proceed to /team-qa (Stage D).**
