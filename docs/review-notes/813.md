- 2026-09-14 · #813 · pattern audit · Flipping the worker default from Fable/Astra at medium to Opus/Sol at high was applied to the code (`container/agents/worker-frontier.md`, `CODEX_WORKER_MODELS`, `default_subagent_reasoning_effort`) and to the doc surfaces the task brief named, but the same policy is also asserted in prose elsewhere, and `docs/frontier-worker-trial.md` and `docs/review-policy.md` kept "Substantive work favors Fable/Astra at medium" and "Medium is the default effort"; a default that lives in both code and prose must be grepped across every prose surface that states it, not only the ones a brief lists, because agents read the prose · `scripts/dispatch-default-docs.test.ts` derives one anchor sentence from the frontier config (the def's `effort:`/`model:` plus `CODEX_WORKER_MODELS`) and fails every dispatch-policy doc that no longer carries it verbatim
