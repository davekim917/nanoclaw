- 2026-09-26 · #1222 · denylist isolation · Shadow host mode shipped as a list of things to switch off, and six review rounds each found one more writer of state the two hosts share: the OneCLI approval queue, the image store, plugin sources, MCP OAuth refresh, host task scripts, the chat-platform tokens, the Slack hot-attach path, and then a boot-time credential check that runtime `.env` re-reads (`backlog-canvas.ts`, `notify-owner.ts`) walked straight past. Inverting it to an allowlist switched off a path no round had named — a container's `start_remote_control` request spawns a host `claude remote-control` process under the operator's login (`src/remote-control.ts`) — which is the evidence the list was never going to converge · every registry that starts work, and every reader of configuration, consults one allowlist where it dispatches (`src/shadow-allowlist.ts`); `src/shadow-allowlist.test.ts` drives each seam with a name no list holds and fails if a shadow admits it, and `src/shadow-allowlist-registries.test.ts` fails a listed name that no longer matches a live registration and checks every live registration the lists omit is refused
