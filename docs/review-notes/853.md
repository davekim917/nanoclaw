- 2026-09-16 · #853 · enforcement scope · Two walks of one mirror denied different plugins, so they could name different owners for one skill name: the drop set walked `~/plugins` with the code-level denials only, while the mirror it filters also denies every workgroup-scoped plugin. A scoped plugin claiming a name ahead of an excluded sub-plugin therefore won the drop set's walk — not excluded, so not dropped — while the mirror, having denied it, had published the EXCLUDED sub-plugin's directory under that name, and the excluded source was copied into the group. An exclusion inverted by a population disagreement, which is the same class as the deny-set and support-dir findings #845 fixed in its neighbours · one `openCodeMirrorSkills(pluginsRoot)` is the population every reader takes, and the spawn path walks once and hands the same array to both readers (`src/opencode-sync.ts`, `src/providers/opencode.ts`), which also removes the second full plugin-tree walk per spawn that #845 r4 recorded; the scoped-plugin-versus-excluded-sub-plugin case is driven through the real sync in `src/opencode-sync.test.ts`, mutation-checked against dropping the deny set and against letting the reader default to an empty population. The remaining direction — a managed entry stale relative to the tree, which needs each entry's own source dir recorded beside the repository root — is #852 
- 2026-09-16 · #853 · pattern audit · Found by the substitute review: the fix threaded the mirror's population through both spawn-path readers and left the enabler's own walk (`scripts/enable-agent-plugin.ts`) deriving one, so the report an operator reads before enabling a plugin could credit it with a skill name a scoped plugin claims ahead of it — the same class, in the report rather than in a group's skills, a few lines above the call that actually publishes the mirror · the enabler reports from `openCodeMirrorSkills` too (`scripts/enable-agent-plugin.ts`), so what it prints is what the sync it then runs will publish
