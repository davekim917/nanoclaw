- 2026-09-23 · #1103 · inherited claim · The skill said the `nanoclaw-group` container label carries the group folder, from one live `docker ps` whose group happens to have an id equal to its folder name; the label carries the agent group id, so for every `ag-…` group a folder filter matches nothing and reads as "no old-image containers". Caught by the substitute closing review and by Codex round 1 · the step-4 text now reads `nanoclaw-group=<agent-group-id>` and cites the writer (`src/container-runner.ts:7552`, fed the agent group id); a live observation is confirmed against the writer before it becomes an instruction
