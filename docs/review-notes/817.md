- 2026-09-15 · #817 · env parity · #812's `account/rateLimits/read` carried an `excludeResetCreditDetails` params map verified against the HOST's codex-cli 0.154.0, whose schema marks that request's params optional; the container runs the codex pinned in `container/Dockerfile` (0.153.4), which deserializes them as unit and refused every bind-time read with `invalid type: map, expected unit`, and because a rate-limit read is telemetry the throw was caught and logged, so the `usage_pull` sample silently stopped fleet-wide (408 push rows, zero pull rows) while the push path kept the table looking alive. A wire shape must be exercised against the binary the CONTAINER pins, not the one in the host's PATH, and a fixture asserting the code's own params proves only that the code agrees with itself · the raw stdin TEXT is asserted to carry no `params` member in `container/agent-runner/src/providers/codex-app-server.test.ts` and, driving the un-injected production `read` against a recording server, in `container/agent-runner/src/providers/codex-rate-limit-tracker.test.ts`; the end-to-end fixture in `container/agent-runner/src/providers/codex.rate-limit-park-integration.test.ts` asserts the same absence, all three mutation-checked against the restored map
