- 2026-09-23 · #1089 · liveness that reports the loop, not the job · All four findings across three Codex rounds were ONE pattern: the renewer's heartbeat said the tick RAN, and the worker read that as "claims are being kept". Round 1: a tick whose gate `progress` calls were refused or timed out still ended `ok`. Round 2: a single failed tick still let the worker claim, and nothing could protect that claim once made (the controller series cannot fire again until its occurrence ends); and alarm keys named the outage kind but not the outage, so a second outage the same day was silent. Round 3: steps past the per-tick renewal cap were skipped without counting, so the tick again ended `ok`. When a heartbeat gates new work, its healthy status must mean every item that needed the job got it, and any eligible item left undone, for whatever reason, is unhealthy; the consumer then refuses new work on the first such signal and saves the threshold for alarming · a tick ends `renew-failed` with a consecutive `failedTicks` whenever any eligible step is not renewed, except where the gate positively saw the run leave its slot (`container/skills/smoke-test/scripts/smoke-controller-renew.sh`); the worker's `degraded` state refuses the poll from the first such tick and alarms from the second, and renewer alarm keys carry the outage onset from `renewer-watch.json` (`container/skills/smoke-test/scripts/smoke-controller-live-worker.py`, `renewer_gate`); pinned by `smoke-controller-renew.test.sh` (heartbeat-renew-failed, heartbeat-run-gone, heartbeat-renew-failed-unreadable-prior, heartbeat-renewal-cap) and `smoke-controller-live.test.sh` r3b/r3c, with 15 mutations each turning a suite red
