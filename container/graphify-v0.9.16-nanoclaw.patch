diff --git a/graphify/detect.py b/graphify/detect.py
index 44698f1..2bbec0c 100644
--- a/graphify/detect.py
+++ b/graphify/detect.py
@@ -1509,8 +1509,13 @@ def save_manifest(
             continue
 
     all_files = [f for file_list in files.values() for f in file_list]
-    with ThreadPoolExecutor() as pool:
-        raw = pool.map(_stat_and_hash, all_files)
+    if os.environ.get("NANOCLAW_GRAPHIFY_SEQUENTIAL") == "1":
+        # NanoClaw's private worker forbids task creation.  Preserve the exact
+        # input order while hashing in the already resource-capped process.
+        raw = map(_stat_and_hash, all_files)
+    else:
+        with ThreadPoolExecutor() as pool:
+            raw = list(pool.map(_stat_and_hash, all_files))
     hashed: dict[str, tuple[float, str]] = {
         r[0]: (r[1], r[2]) for r in raw if r is not None
     }
diff --git a/graphify/extract.py b/graphify/extract.py
index e712449..089cbcb 100644
--- a/graphify/extract.py
+++ b/graphify/extract.py
@@ -4289,53 +4289,64 @@
 def extract(
     paths: list[Path],
     cache_root: Path | None = None,
     *,
+    source_root: Path | None = None,
     parallel: bool = True,
     max_workers: int | None = None,
 ) -> dict:
     """Extract AST nodes and edges from a list of code files.
 
     Two-pass process:
     1. Per-file structural extraction (classes, functions, imports)
     2. Cross-file import resolution: turns file-level imports into
        class-level INFERRED edges (DigestAuth --uses--> Response)
 
     Args:
         paths: files to extract from
-        cache_root: explicit root for graphify-out/cache/ (overrides the
-            inferred common path prefix). Pass Path('.') when running on a
-            subdirectory so the cache stays at ./graphify-out/cache/.
+        cache_root: explicit output root for graphify-out/cache/.
+        source_root: explicit source anchor for portable cache keys and source
+            attribution. NanoClaw passes its immutable repository snapshot root.
         parallel: if True and there are >= _PARALLEL_THRESHOLD uncached files,
             use ProcessPoolExecutor for multi-core extraction.
         max_workers: max subprocess count. Defaults to cpu_count (or the
             value of GRAPHIFY_MAX_WORKERS if set), bounded by len(uncached_work).
     """
+    if os.environ.get("NANOCLAW_GRAPHIFY_SEQUENTIAL") == "1":
+        # The NanoClaw worker is single-process by contract; never construct a
+        # ProcessPoolExecutor even when a caller leaves the default unchanged.
+        parallel = False
     paths = [Path(p) for p in paths]
     _check_tree_sitter_version()
     _raise_recursion_limit()
     # Workspace package manifests/globs can change during watch or repeated extraction.
     _WORKSPACE_PACKAGE_CACHE.clear()
     _XAML_CSHARP_CLASS_CACHE.clear()
 
     # Infer a common root for cache keys (use first diverging segment, not sum of all matches)
-    try:
-        if not paths:
-            root = Path(".")
-        elif len(paths) == 1:
-            root = paths[0].parent
-        else:
-            min_parts = min(len(p.parts) for p in paths)
-            common_len = 0
-            for i in range(min_parts):
-                if len({p.parts[i] for p in paths}) == 1:
-                    common_len += 1
-                else:
-                    break
-            root = Path(*paths[0].parts[:common_len]) if common_len else Path(".")
-    except Exception:
-        root = Path(".")
-    if cache_root is not None:
-        root = cache_root
-    root = root.resolve()
+    if source_root is not None:
+        root = Path(source_root).resolve()
+        for path in paths:
+            try:
+                path.resolve().relative_to(root)
+            except ValueError as exc:
+                raise ValueError(f"source path escapes explicit source_root: {path}") from exc
+    else:
+        try:
+            if not paths:
+                root = Path(".")
+            elif len(paths) == 1:
+                root = paths[0].parent
+            else:
+                min_parts = min(len(p.parts) for p in paths)
+                common_len = 0
+                for i in range(min_parts):
+                    if len({p.parts[i] for p in paths}) == 1:
+                        common_len += 1
+                    else:
+                        break
+                root = Path(*paths[0].parts[:common_len]) if common_len else Path(".")
+        except Exception:
+            root = Path(".")
+        root = root.resolve()
 
     # #1774: the cache is an OUTPUT, so when no explicit cache_root is given it is
@@ -4762,6 +4773,14 @@ def extract(
             ]
             if not candidates:
                 continue
+        # Calls bind in the value namespace. TypeScript can legally import an
+        # interface/type and a function with the same spelling, and the symbol
+        # pass exposes both namespace targets as import evidence. Prefer real
+        # callable definitions before the ambiguity guard so source order cannot
+        # make the type node suppress the value-space call.
+        callable_candidates = [candidate for candidate in candidates if candidate in callable_nids]
+        if callable_candidates:
+            candidates = callable_candidates
         caller = rc["caller_nid"]
         # Resolve the caller's file via the raw_call's own source_file string,
         # which is stable regardless of any caller_nid remap. An indirect
diff --git a/graphify/extractors/engine.py b/graphify/extractors/engine.py
index edc624a..6369051 100644
--- a/graphify/extractors/engine.py
+++ b/graphify/extractors/engine.py
@@ -2235,6 +2235,56 @@ def _extract_generic(
     if config.ts_module == "tree_sitter_swift":
         swift_protocol_names, swift_class_names = _swift_pre_scan(root, source)
 
+    # TypeScript permits a type-space declaration and a value-space function to
+    # share a spelling (and case-distinct spellings still collide because
+    # ``make_id`` case-folds IDs). Without a namespace disambiguator the generic
+    # walk drops whichever declaration it visits second. Pre-scan declarations
+    # so the result is independent of source order, then salt only the colliding
+    # callable ID. Exact-case labels remain unchanged; the call and symbol passes
+    # below route value calls to the callable and type references to the type.
+    type_colliding_callables: set[str] = set()
+    if config.ts_module in ("tree_sitter_javascript", "tree_sitter_typescript"):
+        type_names: set[str] = set()
+        function_names: set[str] = set()
+        declaration_stack = [root]
+        while declaration_stack:
+            declaration = declaration_stack.pop()
+            if declaration.type in config.class_types:
+                name_node = declaration.child_by_field_name(config.name_field)
+                if name_node is None:
+                    name_node = next(
+                        (
+                            child for child in declaration.children
+                            if child.type in config.name_fallback_child_types
+                        ),
+                        None,
+                    )
+                if name_node is not None:
+                    name = _read_text(name_node, source)
+                    if name:
+                        type_names.add(name)
+            elif declaration.type in config.function_types:
+                name_node = declaration.child_by_field_name(config.name_field)
+                if name_node is None:
+                    name_node = next(
+                        (
+                            child for child in declaration.children
+                            if child.type in config.name_fallback_child_types
+                        ),
+                        None,
+                    )
+                if name_node is not None:
+                    name = _read_text(name_node, source)
+                    if name:
+                        function_names.add(name)
+            declaration_stack.extend(declaration.children)
+        type_ids = {_make_id(stem, type_name) for type_name in type_names}
+        type_colliding_callables = {
+            function_name
+            for function_name in function_names
+            if _make_id(stem, function_name) in type_ids
+        }
+
     def add_node(nid: str, label: str, line: int, *, node_type: str | None = None,
                  metadata: dict | None = None) -> None:
         if nid in seen_ids:
@@ -3143,6 +3193,9 @@ def _extract_generic(
                 add_edge(parent_class_nid, func_nid, "method", line)
             else:
                 func_nid = _make_id(stem, func_name)
+                if func_name in type_colliding_callables:
+                    exact_case = hashlib.sha1(func_name.encode("utf-8")).hexdigest()[:8]
+                    func_nid = _make_id(func_nid, "callable", exact_case)
                 add_node(func_nid, f"{func_name}()", line)
                 add_edge(file_nid, func_nid, "contains", line)
             callable_def_nids.add(func_nid)  # function / method def is callable
@@ -3576,8 +3629,14 @@ def _extract_generic(
             continue
         raw = n["label"]
         normalised = raw.strip("()").lstrip(".")
-        label_to_nid[normalised] = n["id"]
-        label_to_nid_ci[normalised.lower()] = n["id"]
+        # A TS type-space declaration and value-space function may share the
+        # exact label. Calls must bind to the top-level function regardless of
+        # declaration/walk order; type references are handled separately by the
+        # symbol-resolution pass.
+        is_top_level_function = raw.endswith(")") and not raw.startswith(".") and "." not in raw
+        if normalised not in label_to_nid or is_top_level_function:
+            label_to_nid[normalised] = n["id"]
+            label_to_nid_ci[normalised.lower()] = n["id"]
 
     seen_call_pairs: set[tuple[str, str]] = set()
     seen_indirect_pairs: set[tuple[str, str]] = set()  # Python indirect_call dedup
diff --git a/graphify/extractors/resolution.py b/graphify/extractors/resolution.py
index 08e91b9..34ef555 100644
--- a/graphify/extractors/resolution.py
+++ b/graphify/extractors/resolution.py
@@ -743,13 +743,24 @@ def _apply_symbol_resolution_facts(
     path_by_resolved = {path.resolve(): path for path in paths}
     source_file_id = {path.resolve(): _make_id(str(path)) for path in paths}
     symbol_nodes: dict[tuple[Path, str], str] = {}
+    callable_symbol_nodes: dict[tuple[Path, str], str] = {}
     for node in nodes:
         source_path = _js_source_path(str(node.get("source_file", "")), root)
         if source_path is None:
             continue
         label = str(node.get("label", "")).strip().strip("()").lstrip(".")
         if label and node.get("id"):
-            symbol_nodes[(source_path, label)] = str(node["id"])
+            key = (source_path, label)
+            node_id = str(node["id"])
+            # Type references prefer the type-space declaration independent of
+            # extraction order. Keep a separate value-space index so a TS named
+            # import can expose both namespaces when an interface/type and a
+            # top-level function legally share the same spelling.
+            if key not in symbol_nodes or _is_type_like_definition(node):
+                symbol_nodes[key] = node_id
+            raw_label = str(node.get("label", "")).strip()
+            if node.get("_callable") and raw_label.endswith(")") and not raw_label.startswith(".") and "." not in raw_label:
+                callable_symbol_nodes[key] = node_id
 
     def ensure_symbol_node(path: Path, name: str, line: int) -> str:
         resolved_path = path.resolve()
@@ -932,6 +943,16 @@ def _apply_symbol_resolution_facts(
             import_fact.line,
             import_fact.file_path,
         )
+        callable_target_id = callable_symbol_nodes.get((origin_path, origin_symbol))
+        if callable_target_id is not None and callable_target_id != target_id:
+            add_edge(
+                source_id,
+                callable_target_id,
+                "imports",
+                "import",
+                import_fact.line,
+                import_fact.file_path,
+            )
 
     # #1146: emit file-to-file imports_from edges for package-form submodule imports.
     for from_path, to_path, line in facts.module_imports:
@@ -950,7 +971,10 @@ def _apply_symbol_resolution_facts(
         unresolved_origin = local_aliases_by_file.get(file_path, {}).get(use_fact.local_name)
         if unresolved_origin is not None:
             origin_path, origin_symbol = resolve_exported_origin(*unresolved_origin)
-            target_id = symbol_nodes.get((origin_path, origin_symbol))
+            if use_fact.relation in ("calls", "indirect_call"):
+                target_id = callable_symbol_nodes.get((origin_path, origin_symbol))
+            if target_id is None:
+                target_id = symbol_nodes.get((origin_path, origin_symbol))
         if target_id is None and use_fact.relation in ("inherits", "implements"):
             # Same-file fallback for HERITAGE only: a base declared in the same
             # file (`class X extends Y`, `interface A extends B`) has no import
