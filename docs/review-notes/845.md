- 2026-09-16 · #845 · enforcement scope · The #826 finding at the second host-side reader of plugin-chosen paths: the OpenCode skill mirror symlinked every child of a plugin's skill dir and COPIED its skill manifest, then the provider copied the mirror into a session XDG with `dereference: true`, so a symlink inside a plugin repository made the HOST read a host-only file and place the bytes where an OpenCode container reads them. The premise that let it sit is the same one r5 named on #826 — a plugin repo is a trusted executable input, which is true and irrelevant, because running code IN the container is not permission to move host-only state INTO it · realpath containment on a separator boundary at both reads, because neither is enough alone: `syncSkillSymlinks`/`mirrorSkillDir` hold each skill dir and each direct child to the skill's own `pluginRoot` (a new required field on `DiscoveredSkill`), which is the only place that can see the skill-manifest COPY, and `copyOpenCodeSkills` re-decides containment for every link it dereferences, since a plugin can repoint an in-repo link between the sync and the next spawn (`src/plugin-skill-discovery.ts` and its container twin, `src/providers/opencode.ts`, `src/opencode-sync.ts`); refusals are RETURNED rather than logged in the twinned module so the parity test still holds, and both callers log them. Discover → mirror → copy end to end in `src/plugin-skill-containment.test.ts`, mutation-checked against dropping either containment and against dropping the separator boundary
- 2026-09-16 · #845 · over-broad guard · Found by the substitute review (r1) on the first head, and the finding that nearly shipped: the copy's containment was the UNION of every plugin root under `~/plugins`, while the mirror writer resolves only each skill's DIRECT children — so a link nested below a skill's top level (under a real in-repo `reference/`, which is symlinked wholesale and never walked) could reach a DIFFERENT plugin, including a workgroup-scoped one the mirror deliberately never published (`scopedPluginNames`, `src/plugin-scopes.ts`). Containing to "somewhere legitimate" is not containment; the boundary has to be the one repository the dir actually came from, and the dir's name is a skill name carrying no provenance · `MIRROR_MARKER` now records the resolved source root (JSON-encoded, so a newline in a real basename cannot truncate it) and `copyOpenCodeSkills` holds each published dir to THAT single root, with a recorded-but-unresolvable root refusing every link rather than widening; only an unmarked dir — operator-placed or natively installed, which no plugin can create in the mirror — falls back to the union, and `resolvePluginRoots` skips dot-entries so `~/plugins/.git` is not a root. `mirrorSupportDir` derives its own containing root the same way instead of taking the union. Nested-into-a-scoped-plugin, sibling-plugin, repointed-checkout and support-dir cases in `src/plugin-skill-containment.test.ts` and `src/opencode-sync.test.ts`, mutation-checked against restoring the union in both writers and against falling back when the recorded root is gone
- 2026-09-16 · #845 · deferred hardening · Also r1, recorded not fixed: the copy's filter resolves a path and `cpSync` then resolves it again to read, which is the check-then-act shape `readRulesetFile` rejected on #826 r7 — closing it means replacing `cpSync({dereference})` on the spawn path with an fd-based walk (`O_NONBLOCK` open, `/proc/self/fd` containment, `fstat`), a larger change than this fix. Winning the race needs write access to `~/plugins` on the HOST, which is operator-level authority, and no group mounts it read-write today · the cheap half of it is taken here — a resolved target must be a regular file or a directory, and `syncSkillMdCopy` requires a regular file, so a plugin-planted FIFO can no longer block the host's event loop. Re-raise if `~/plugins` is ever mounted read-write into a container, or if anything else gains write access to a plugin tree
