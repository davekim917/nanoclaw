- 2026-09-17 · #870 · inherited claim · Found by the substitute review: the PR removed the Anthropic-credential strip from the OpenCode server env and its header stated the consequence as "an `anthropic/*` model can now resolve the container's credential from env rather than auth.json" — a sentence about `buildOpenCodeConfig`, which the diff did not touch, written from recollection of how that provider resolves credentials. Re-read, `buildOpenCodeConfig` throws first whenever the provider is `anthropic` and auth.json carries no anthropic record, so env alone enables nothing; and which credential OpenCode prefers when BOTH exist was verified by nobody. A thrown message in the same file still said env credentials were "intentionally unavailable", which the diff had just made false · the header, the `sdkOptions` comment and the throw text state the fail-closed check and name the env-vs-auth precedence as unverified; the spawn-site comment is a pointer to `buildOpencodeServerEnv` (`container/agent-runner/src/providers/opencode.ts`)
- 2026-09-17 · #870 · upstream drift · Six upstream-owned files (per `src/upstream-ratchet.json`) had grown: two docs sections, an inverted assertion in `container/agent-runner/src/scheduling/task-script.test.ts` (unavoidable), a rewritten `container/agent-runner/src/destinations.ts` comment carrying an uncited claim, a `container/agent-runner/src/index.ts` comment, and doc comments in `container/agent-runner/src/providers/claude.ts`. The operator's rule is that fork customizations must not widen the surface upstream merges can conflict on; a comment is a conflict surface like any other line · the credential model lives once in `container/agent-runner/src/providers/secret-env.ts` (fork-owned); the upstream-owned docs (`docs/agent-runner-details.md`, `docs/isolation-model.md`) carry one-line pointers, and `container/agent-runner/src/destinations.ts`, `container/agent-runner/src/index.ts` and `container/agent-runner/src/providers/claude.ts` are at or below their origin line counts
