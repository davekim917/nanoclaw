- 2026-09-23 · #1066 · fail-open default · Five findings across three Codex rounds and two closing reviews were ONE pattern: a read that could not be made concluded as a verdict. First, a fake gate that did not refuse what the real gate refuses, so a replayed timeout wedged against the real one. Second, a controller predicate (`nonempty_file`, rejecting symlinks) disagreeing with the gate's `[ -s ]`. Third, "any refusal falls back to `finish`", which read a gate that could not SEE its run root as proof. Fourth, a run directory invisible for one fire, read by `run.has()` as "no disposition" and posted as a false BLOCKED over a campaign `main` certified GO. Fifth, a dispatch start file that could not be opened, read as ambiguous. Each was fixed at the reported site and the next appeared one layer over. `has()`/`exists`/`nonempty_file` answer False both for "not there" and for "could not look", and every verdict built on them inherits that. When a check can conclude a verdict, give it a read that distinguishes absent from blind, and gate every verdict on it in one place, not per site · run-tree reads answer `present | absent | blind` (`visibility` and `sight` on `RunView`, `container/skills/smoke-test/scripts/smoke-campaign-controller.py`), with blindness proved by a journaled brief; every verdict is concluded in `pre_finish`, which refuses to freeze, post or send unless the whole run tree is readable; the gate's `challenger-timeout` refusals carry machine-readable `refusal` codes, and only `disposition-filed` changes the terminal verb (`container/skills/smoke-test/scripts/smoke-pr-gate.sh`); pinned by `container/skills/smoke-test/scripts/smoke-campaign-controller-live.test.sh` cases b9-b14 (b9 is the closing reviewer's blip probe, which publishes BLOCKED on the prior head) and by `container/skills/smoke-test/scripts/smoke-pr-gate.test.sh` section 7d, which asserts the real gate emits every refusal code the fake does
