- 2026-09-15 · #821 · enforcement scope · #811 bounded the OAuth `/api/oauth/usage` survey with nothing, and the follow-up fix everyone recorded — "per-slot min-interval sharing the same constant" (`USAGE_PULL_MIN_INTERVAL_MS`) — would have been a NO-OP: `pickCredentialSlotByUsage` is called exactly once per container boot (the single `await provider.pickCredentialSlotByUsage?.()` in `container/agent-runner/src/index.ts`), so module scope is per-process scope and a process that acts once has nothing to throttle. The rate limit it was tripping is per CREDENTIAL and fleet-wide, so each token saw a request per Claude session start (~25/hr, bursting 4-8/min) and every slot came back 429 with `retry-after` decoding to the same second across three separate accounts, while a garbage bearer from the same IP in the same second answered 401 — measured, and the two facts together are what rule out an IP limiter and rule in our own lockstep burst. The fallback cache everyone reached for next was no better: `rate_limit_samples` lives in the SESSION's `outbound.db`, so a new session has none and no session can read another's. A throttle has to live where the limit lives; when the limit is fleet-wide the only place that can hold it is the one process that sees the fleet · the survey moved to the host (`src/slot-usage-survey.ts`, one pull per slot per `SLOT_USAGE_SURVEY_MIN_INTERVAL_MS`, `retry-after` honoured and floored by the ordinary interval), pushed to each spawn as `NANOCLAW_SLOT_USAGE_SURVEY` from the same block that forwards the ring; request volume is pinned as a function of TIME rather than spawn count by a 60-spawn counting-fake test in `src/slot-usage-survey.test.ts` ("holds one pull per slot per interval across a burst of spawns" — 18 requests against #811's 360), mutation-checked against deletion of the interval guard
