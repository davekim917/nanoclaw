- 2026-09-23 · #1013 · unclamped bound · `preToolUseHook` published the model's raw Bash `tool_input.timeout` as `tool_declared_timeout_ms`, and the host takes that value unbounded as both its idle ceiling and its claim tolerance (`Math.max(ABSOLUTE_CEILING_MS, declared)` and `Math.max(CLAIM_STUCK_MS, declared)`). The CLI never runs a Bash call past `BASH_MAX_TIMEOUT_MS` (3600000), but a declared `86400000` or `1e12` plus a wedged CLI or a leaked denied call held off both kills for a day or for ever. A number the model typed describes what it asked for, not what will happen; publish it only after clamping it to what the enforcer allows · `clampDeclaredBashTimeoutMs` (`container/agent-runner/src/providers/claude.ts:758`) clamps to `BASH_MAX_TIMEOUT_MS`, falls back to 600000, and nulls non-finite or non-positive values; pinned by the "declared Bash timeout is clamped to what the CLI enforces" cases (`container/agent-runner/src/providers/claude.toolInFlight.test.ts:153`), 2 of which fail with the clamp removed
- 2026-09-23 · #1013 · mutation coverage · The closing review's mutants M5 and M6 survived. Deleting either `resetToolInFlightTracking()` bound on the in-flight leak (at query creation, or on `result`) left the suite green, because the one reset test called the function directly instead of driving the provider. A hook returning `updatedInput` from the unmatched `preToolUseHook` entry also left it green, because the one-emitter test (#1010) walked only the `matcher: 'Bash'` list, while the CLI runs every matched entry in one concurrent tier. A test for a call site has to drive the call site, and a test for "only one X in the tier" has to enumerate the whole tier · each bound now has a case driven through the provider's own query() (`container/agent-runner/src/providers/claude.toolInFlight.test.ts:244`, `:252`), and `container/agent-runner/src/providers/claude.stdinEmit.test.ts:127` runs every Bash-reachable PreToolUse hook, unmatched entries included, concurrently on one input; each was re-run against its mutant and fails there
- 2026-09-23 · #1013 · uncited behavioural claim · The comment on `publishToolInFlight`'s write-only-on-change guard, and the test oracle beside it, said the host's claim-stuck rule reads `tool_started_at` as "this tool was already running before the claim". On main at the time nothing did: that rule keyed on `claim.status_changed` and the heartbeat alone. The reader existed only in the stacked, unmerged #1014 (`inFlightToolStartedAtMs`), so the sentence described another PR's code as live. The first version of this line then said #1014 adds no such reader. That was wrong too: it rested on a grep of #1014's diff cut off by `head -5` before the one real hit. Both mistakes are the same one, a sentence about another module that nobody read that module's code for. The oracle repeated the first belief, so the test passed without ever checking it · the comment (`container/agent-runner/src/providers/claude.ts:704-718`) and the oracle (`container/agent-runner/src/providers/claude.toolInFlight.test.ts:131-139`) now name every reader, each read before citing: `src/modules/sweep-continuation/decide.ts:42-57`, `src/modules/sweep-continuation/index.ts:336`, `src/host-restart-warn.ts:287`, `src/dashboard/api/threads.ts:251-253`, and, from #1014, `src/modules/sweep-container-health/index.ts:552` used at `src/modules/sweep-container-health/index.ts:645`
