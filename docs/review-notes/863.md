- 2026-09-16 · #863 · test synchronization · The per-key lock's race test held the first post open with a 150ms wall-clock sleep and trusted the second session to reach its lookup inside that window, so it could pass with no lock at all, whenever the second session arrived after the first had already recorded. A barrier has to wait on the event it claims to order, not on a duration · the first post waits on a promise that the test releases only after `_threadKeyLockWaitersForTest` (`src/delivery.ts`) shows the second session queued on the key's lock; it asserts one adapter call so far and throws on a 5s deadline instead of falling through (`src/delivery.test.ts:1969`, 'two sessions of one group posting the same new key concurrently make exactly one root'), mutation-checked by replacing `await prior` with `void prior`
- 2026-09-16 · #863 · mutation coverage · The host test for keyed edits built `{ operation: 'edit', threadKey }` rows by hand, but no tool wrote that shape: `edit_message` and `add_reaction` never carried a key. It passed while exercising a path production could not reach. A delivery test's fixture row must be a shape some producer really writes · `edit_message` and `add_reaction` take an optional `thread_key` (`container/agent-runner/src/mcp-tools/core.ts`), their serialised rows are pinned in `container/agent-runner/src/mcp-tools/core.test.ts:169` and refusal in `:183`, and the host test now feeds exactly that shape (`src/delivery.test.ts:1873`); each serialiser is mutation-checked
- 2026-09-16 · #863 · destroy before replace · A threaded keyed post that failed deleted its anchor record, then tried the root post that was meant to replace it. If that root post also threw, which a transient outage makes likely because both calls go to the same platform, the incident's thread was forgotten and the retry opened a second top-level post for the same incident · no record is touched before the root post; `settleThreadKeyAnchor` (`src/delivery.ts:424`) replaces the record when the root post returns an id and drops it only when the root post succeeded with no id, pinned by 'a transient threaded failure whose root fallback also throws keeps the existing record' (`src/delivery.test.ts:1817`) and the no-id case (`:1836`), both mutation-checked
- 2026-09-16 · #863 · deferred hardening · The keyed path now logs bookkeeping failures after a post has landed, but the unkeyed task day-anchor writes that run at the same point (`setTaskThreadAnchor` and `deleteTaskThreadAnchor` in `src/delivery.ts`) still throw out of `deliverMessage`, so a central-DB error fails the row and the retry re-posts a message the channel already shows. Recorded, not fixed, because #863 is scoped to keyed posts · #864 (re-raise on any duplicate task post whose log shows a task_thread_anchors write error, or on any change that adds a slower write to that path)
