- 2026-09-15 · #819 · deferred hardening · `test_claude_cli_agent_sdk_lockstep` is named for a lockstep it never checks — it asserts only that `CLAUDE_CODE_VERSION` and the `@anthropic-ai/claude-agent-sdk` pin are each `x.y.z`-shaped, never that they correspond, so a one-sided Claude CLI/SDK bump passes every gate silently (#817's skew class, Claude side); its sibling `test_opencode_all_operational_pins_match` does compare its pair. Recorded rather than fixed because the SDK skips versions the CLI publishes (0.3.244, 0.3.249, 0.3.253-256, 0.3.262, 0.3.264 do not exist), so the two `latest` tags legitimately sit at different patches some of the time and an offline equality assertion would refuse a correct bump — the pairing belongs in the audit's item selection, which can warn, not in a test, which can only fail · #820, carrying the re-raise trigger (a pin pair actually shipping skewed on main, or the audit gaining paired-item warnings); the pairing itself is publisher-declared as `claudeCodeVersion` in the SDK's own `package.json` and holds for 112 of 112 published 0.3.x versions
- 2026-09-15 · #819 · drift tests · A container CLI pin is not one line: moving `ARG OPENCODE_VERSION` also moves the exact `@opencode-ai/sdk` pin, the `OPENCODE_CAPTURED_VERSION` constant whose tool list must be RE-DERIVED from the new binary rather than retyped, and the literal `ARG OPENCODE_VERSION=<pin>` a fresh `/add-opencode` install writes — the release adapter's `apply` moves only the first two, so the deterministic flow is not by itself the whole change · `scripts/provider-version-contract.test.ts` derives every one of those surfaces from the Dockerfile ARG and fails on any that lags, which is how the remaining two were found
