- 2026-09-22 · #1025 · undeclared default migration · A second fleet-default move (Opus 5 at `high` to Opus 5.5 at `medium`, plus three image CLI pins) was pushed with no `[BREAKING]` entry and no `versions.json` pins, a week after #839 recorded the same class; substitute reviews caught both, and the migration doc then took five more rounds to become accurate. The CHANGELOG half is still enforced only by review, but the pin half now has a check · `setup/lib/image-version-pins.test.ts` ties each `versions.json` image pin (`claude-code`, `claude-agent-sdk`, `codex-cli`) to the Dockerfile ARG the install RUN sees and to the runner `package.json`, and asserts the claude-code/SDK patch lockstep; mutation-checked red on a moved pin
- 2026-09-22 · #1025 · inherited claim · The migration doc's precedence and restart claims were written from recollection of other modules and each was wrong once: session stickies apply to task fires (they are suppressed for pure task wakes), channel wiring beats `container.json` (`providerConfig` outranks it), and a host restart refreshes containers (running ones are adopted and keep their spawn-time runner and CLI) · every behavioural sentence in the current section of `docs/claude-default-model.md` now names its `file:line` (`container/agent-runner/src/providers/claude.ts:2770`, `container/agent-runner/src/poll-loop.ts:4027-4032`), and each detect command was run on this host before it was written
