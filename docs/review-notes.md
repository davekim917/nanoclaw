# Review notes

Lessons from past reviews, one line each. The author and the reviewer read
this before starting (`docs/review-policy.md`, "Review notes and fix links").
Append a line when a finding is deferred to an issue or a PR is reverted.

Format: `YYYY-MM-DD · PR · class · lesson · structural fix (if any)`

- 2026-09-05 · #401 · merge head · The gate passed one head, a newer one was pushed right after, and `gh pr merge` merged the newer one; merge exactly the head that was gated · `codex-review.sh merge-check --head <sha>`, then `gh pr merge --match-head-commit <sha>`
- 2026-09-10 · #608 · stream lifetime · Ending a task's stream right after its result closes the CLI's stdin, but the CLI can still run a queued turn, and that turn's SDK hook callbacks, answered over stdin, fail open; never end the stream while work may remain · reverted in #610; tests at `container/agent-runner/src/poll-loop.test.ts:390` onward (#613, #619) and `:3416` (#631)
- 2026-09-10 · #611 · URL rewriting · `url.<base>.insteadOf` rewrites the fetch side, so `ls-remote` can query a different repo than the push target · `.husky/pre-push` fails closed when `ls-remote --get-url` differs from the push URL; tested in `scripts/pre-push.test.ts`
- 2026-09-11 · #623/#626 · hook tests · Four test files cover `.husky/pre-push`, each with its own fixture, so a hook change runs all four: `scripts/pre-push.test.ts`, `scripts/setup-typecheck-gates.test.ts`, `scripts/git-hooks-path.test.ts`, `scripts/check-public-boundary.test.ts` · none; #611 passed its own suite, then failed CI on another file's fake git
- 2026-09-11 · #640 · drift tests · A drift test that hardcodes the list it guards passes when the file grows a new entry; derive the list from the file · #641, fixed in #643
- 2026-09-11 · #642 · trusted actors · The gate trusts `github-actions[bot]` to take `risk:high` off, and every workflow that runs PR code with a write token acts as that bot; a trusted actor is only as narrow as the tokens that can act as it · `ci.yml` token read-only (#644); a scope label gone with no `unlabeled` event fails closed (#645)
- 2026-09-11 · #624 · reviewer tier · A Sonnet review called #624's risk-list gap analysis complete; a retroactive Opus pass found five authorization seams it missed · no review of any kind from a Sonnet- or Haiku-tier model (docs/review-policy.md)
- 2026-09-11 · #615 · enforcement scope · A git hook enforces only where `core.hooksPath` resolves: container pushes and other checkouts skip it, and a private Free-plan repo has no branch protection behind it · the `Main provenance` workflow flags a main commit no merged PR produced; the risk list gained the remaining approval seams (#651)
- 2026-09-11 · shadow-review ratchet (rule, not one PR) · A `shadow-review`-labeled issue with a P1 finding on a skipped (unreviewed) PR is evidence that PR's *paths*, not just its diff, are under-covered · move the offending path(s) onto `risk:high` in `.github/labeler.yml`, and add one line here naming the PR, the path, and the shadow-review issue it came from — see `.github/workflows/shadow-review.yml` and `docs/specs/risk-based-review/plan.md`'s rollback condition
- 2026-09-11 · #668 · mutation coverage · Two tests stayed green when the property they name was deleted — the watermark comparison and the fail-closed catch independently cover those paths, so neither test would notice its own guard being removed; "the tests still pass" is never evidence that a guard is redundant · the guards that do bite have single-candidate tests built for them: the watermark-coincidence test for the scope comparison, the symlink-swap test for the `lstat` candidate guard
- 2026-09-11 · #668 · deferred hardening · Four substitute-review findings were recorded rather than fixed (fd-based copy, stamps without `srcPath`, orphan stamps, the worker-shutdown overlap) · each carries a re-raise trigger and is tracked in #676
