- 2026-09-24 · #1138 · stale state · A new early return that closes a run out quietly (finished by its holder, so no post-finish writes) marked the run done but left open any post-finish obligation an earlier controller had already journaled for it, such as a planned or failed freeze-close; XZO #2176 had one planned before the fix. The done fast path skips a run only when no post-finish obligation is pending, so the run would have been revisited on every fire, each time appending another `finished` decision. A path that stops creating an obligation must also settle the ones already in the journal · the quiet-finish path in `post_finish` abandons every open `POST_FINISH_SLOTS` obligation with the reason "run finished by its holder" before returning (`container/skills/smoke-test/scripts/smoke-campaign-controller.py`); pinned by case 7h in `container/skills/smoke-test/scripts/smoke-campaign-controller-live.test.sh`, which seeds a planned and a failed freeze-close and asserts it is abandoned, never run, and that the next fire takes the done fast path; both a removed abandon and a disabled fast path turn it red
