- 2026-09-14 · #811 · secret in error message · `fetchSlotUsage` let a `fetch()` TypeError propagate, and on a malformed slot value that message quotes the whole `Authorization: Bearer <token>` header, which the provider then logged verbatim as `Slot usage pull failed … : ${err.message}`; every error leaving the function that holds the token is now built from a status code or the error's class name only · `container/agent-runner/src/providers/claude-slot-pick.ts` (`sanitizedErrorName`), leak regression in `container/agent-runner/src/providers/claude-slot-pick.test.ts`
