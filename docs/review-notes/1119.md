- 2026-09-24 · #1119 · silent cap · The fix for a truncated `issue list --limit 500` re-queried with a title search that had its own `--limit 100` and then treated a full result as complete, the same defect one hop down: a search is fuzzy, so a generic title can draw more candidates than the cap, and "no exact match among them" is not proof of absence. Every capped listing a decision rests on needs its own at-the-cap branch, including the fallback added to close another's · the fallback search now fetches up to 1000 and, when that too is full with no exact match, returns `unknown` with the cap in its error instead of creating, so the obligation's overdue alarm carries the reason to a human (`container/skills/smoke-test/scripts/smoke-campaign-controller.py`, `_gh`); pinned by `container/skills/smoke-test/scripts/smoke-controller-gh-issues.test.py` (test_a_title_search_at_its_cap_never_files), which fails with the at-cap branch removed
- 2026-09-24 · #1119 · silent cap · The same fallback, fixed for its own cap, still replaced the full 500-issue page with the search result before checking the page's titles, so a duplicate already on the page was lost whenever search missed it: GitHub's search index lags a fresh create, and the query was built with `json.dumps`, which turns non-ASCII into `\uXXXX` and so searches for a different string. A fallback that narrows must add candidates to what is already known, never replace it, and the known page is the stronger evidence because it is newest-first and unindexed · the page's exact-title check runs first and search runs only on a miss, its hits added to the candidates; the query quotes the raw title with double quotes blanked, never a JSON-escaped one (`container/skills/smoke-test/scripts/smoke-campaign-controller.py`, `_gh`); the gh fake's search matches the phrase literally and hides issues listed as unindexed (`container/skills/smoke-test/scripts/testdata/controller-live-fakes.py`); pinned by `container/skills/smoke-test/scripts/smoke-controller-gh-issues.test.py` (test_a_match_on_the_full_page_wins_even_when_search_cannot_see_it, test_a_non_ascii_title_is_searched_as_written), each failing with its fix reverted
