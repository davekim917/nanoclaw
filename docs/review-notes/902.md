- 2026-09-17 · #902 · inherited claim · `hasChatOutboundAfter` excluded every outbound row carrying a `threadKey`, on the comment's claim that "delivery re-parents a keyed post under the key's incident thread". That is only true when the row has no `thread_id`: `keyAddr` requires `baseThreadId === null` (`src/delivery.ts:1575`), and `THREAD_KEY_DESCRIPTION` in `mcp-tools/core.ts` tells the agent the key has "no effect when posting into the current conversation's thread". In a threaded conversation a keyed reply lands in front of the person and was still not counted, so the runner would nudge and the agent would answer twice. I had read that guard an hour earlier while triaging the Codex finding and still wrote the unconditional clause, citing the section by name instead of the line that decides it · the exclusion is now conditional on the debt's `threadId === null`, the comment cites `delivery.ts:1575`, and a test builds the threaded case
- 2026-09-17 · #902 · inherited claim · The same predicate enumerated "what reaches a person" as `kind = 'chat'` from the kinds I happened to see in `outbound.db`, without grepping the writers. `send_card` and `ask_user_question` write `kind: 'chat-sdk'` (`mcp-tools/interactive.ts:93,156`) and a card is a documented way to answer, so a card-only reply produced a false nudge and a duplicate prose answer — worse than main, which never nudged at all. An enumeration of row shapes is a claim about every writer; `grep -rn "kind: '" mcp-tools/` is the check · both kinds count, with the writer lines cited
- 2026-09-17 · #902 · mutation coverage · The first keyed-thread test wrote its keyed row with no `thread_id` against a debt whose `threadId` was null — the one configuration where the unconditional exclusion and the correct conditional one agree. It restated the code's belief and passed either way. Both new cases (card counts; keyed reply in a threaded conversation counts) were run against the reverted predicate and fail there · when a fix narrows a condition, the test must sit on the side of the condition the old code got wrong
- 2026-09-17 · #902 · pattern audit · Round 1 built a conversation-scoped "did the person get a reply" primitive for the `send_message` path, and the text branch of the same `result` handler kept clearing the debt on any delivered block, wherever it went — a second site of the same invariant that did not call the primitive (Codex round 3 finding B, substitute review NB2) · the text branch now settles the debt through `hasChatOutboundAfter`, or on a deliberate send-nothing result
