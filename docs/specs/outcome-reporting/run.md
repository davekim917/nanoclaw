# Outcome reporting implementation record

Default-off outcome reporting keeps routine narration in durable internal records and provides concise, evidence-linked reports for completed work items. Spawn-scoped capability preserves compatibility with older runners. Host-owned receipts deduplicate typed outcomes by canonical work identity; uncertain adapter failures require evidence-based reconciliation. Explicit replies, requested detail, required approvals and material corrections retain their existing routes.

Validation: host/scripting and runner typechecks passed. Host delivery/schema/reconciliation suites passed 101 tests; runner MCP/poll-loop/controller-send suites passed 279 tests. Earlier config/composition/schema/reconciliation coverage passed 134 tests; overlapping suites are not additive. Required lint passed with 0 errors and 1396 warnings. Formatting and diff checks passed. The upstream ratchet was regenerated for the ten affected upstream-owned paths; growth implements the opt-in behavior and its regression coverage. Independent cross-family review reported no remaining MUST-FIX.

Scope: this source contribution does not include installation-specific producers, task prompts, configuration helpers or activation records. Those require separate deployment review. Purpose and completion remain agent declarations; this change provides no semantic evaluator, permission expansion or universal exactly-once guarantee. Existing file, edit, direct publication and legacy message routes retain their established behavior. Source publication, saved opt-in configuration, runtime activation and observed user-visible behavior are separate proof layers.
