- 2026-09-15 · #837 · reformatted artifact · The vendor script renders one string and writes it to two paths held byte-identical under ONE manifest hash, but `.husky/pre-commit` prettier-formats every staged `src/**/*.ts` and `.prettierrc` sets `singleQuote`, so the host copy was rewritten after generation while the runner copy — `container/agent-runner/` is a tree prettier never sees — was not. The head shipped with four red tests (fingerprint, host/runner byte-identity, exported constants, live render) under a PR body claiming a green suite; the artifact belongs outside the formatter's scope, not the check loosened to tolerate two spellings · `src/worker-policy.vendored.ts` added to `.prettierignore`, which covers both the hook and `pnpm format:check`; the byte-identity and live-render cases in `src/workflow-agent-vendor.test.ts` are what caught it and go red again if the entry is removed
- 2026-09-15 · #837 · enforcement scope · Found by review r1: the vendor script's mid-flip refusal compared only `codex.model` against the plugin's generated role TOML, while the CLAUDE half of the policy lands in the def's own frontmatter — the file this script vendors verbatim — so a `claude.model`/`claude.effort` edit with the plugin's sync not re-run would render new constants beside a stale `model:`/`effort:` and pass every drift test, because each artifact agreed with the thing it was rendered from. A guard over a policy with two halves has to bind to both · `assertPluginNotMidFlip` compares both halves and reports every mismatch at once, called before anything is written so a refusal cannot leave a half-vendored tree (`src/workflow-agent-vendor.ts`), with the stale-Claude-model, stale-Claude-effort, stale-Codex-model and all-three cases in `src/workflow-agent-vendor.test.ts` (mutation-checked: deleting either Claude comparison, first-mismatch-only reporting, and defaulting instead of throwing in `claudeRoleDispatch` each kill tests)
- 2026-09-15 · #837 · enforcement scope · Same PR, its own doc: the new "Changing the worker policy" section asserted that the review lane is not governed by the policy and that `scripts/reviewer-models.ts` reads the frontier configuration separately. `computeReviewerModelIds` builds the reviewer allowlist from `container/agents/worker-frontier.md`'s `model:` and `CODEX_WORKER_MODELS`, both of which the same PR made policy-derived, so a flip moves who may approve a merge. A doc claiming a seam is independent has to be checked against the seam, not against the intent · the section now names the derivation and `scripts/reviewer-models-freshness.test.ts`, which fails the flip that skips the regen (`docs/review-policy.md`)
- 2026-09-15 · #837 · pattern audit · Found by review r2: the policy was fed into `CODEX_WORKER_MODELS`, the vendor manifest and both container-config writers, but `src/claude-agent-md.ts` was not grepped for the OTHER place it states the policy — `retargetRunsOnSentence` hardcoded "with high reasoning by default" into the generated Codex role's description, which is a routing signal the orchestrator reads. A `codex.effort` flip would have left the role advertising an effort it does not run at, the same code-and-prose split #813 recorded, one file further in: the audit for a value moving into a constant is every site that STATES the value, not only the sites that compute with it · the effort word is a parameter defaulting to `WORKER_POLICY_CODEX_EFFORT` (`src/claude-agent-md.ts`), pinned two ways in `src/claude-agent-md.test.ts` — driven with an effort the policy does not carry so a re-typed literal fails today, and compared against the `default_subagent_reasoning_effort` the container config renders so a flip that splits them fails then (both mutation-checked)
