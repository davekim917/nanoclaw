- 2026-09-17 · #899 · projection read · The legacy precheck decided which required checks were red from its own reading of GitHub's rules — contexts from rulesets and classic protection, app pins, newest report per name — and each review round found another case where that copy disagreed with GitHub (a pin dropped, a conclusion list that missed `startup_failure`, a status and a check run of one name merged when GitHub requires both); three site patches on one seam were the signal that the check was reading a derived model of the platform instead of the platform's own answer · `required_status_red` reads GitHub's `statusCheckRollup` `isRequired(pullRequestNumber:)` and re-derives nothing (`container/skills/pr-review-loop/scripts/codex-review.sh`), pinned by the rollup-fixture cases under "legacy precheck" in `scripts/codex-review.test.ts`, including a green status over a red same-named check run
- 2026-09-17 · #899 · fail-open default · The rule "a receipt marker hidden in a code fence or HTML comment can block but never clear" was implemented by reading only the FIRST hidden marker in a comment and dropping it when it read clear, so a CLEAR example followed by a real CHANGES under one unclosed fence discarded the CHANGES and the gate deferred; the tests covered one hidden marker per comment, never two, so the selection step itself was never exercised — a block-never-clear rule has to hold over every item it could apply to, not the first · every counting marker in a comment is read and any not-clear one makes the comment a no (`independent_receipt_newest`, `container/skills/pr-review-loop/scripts/codex-review.sh`), pinned and mutation-checked by "hidden markers can only block, and every one of them is read" in `scripts/codex-review.test.ts`
- 2026-09-17 · #899 · projection read · Deciding whether a receipt marker counted meant modelling GitHub's Markdown grammar, and five reviews in a row each found a body the model read differently from the renderer: fences, then first-marker selection, then a visible-branch shortcut, then an HTML comment and tilde fences interleaved — and then, after visibility parsing was replaced by a blacklist of what may precede the marker, a tag whose multiline attribute held the receipt; a hand-rolled parser of another system's grammar is a derived copy that disagrees with it somewhere, and a blacklist of "things that open a hidden context" is still that parser · blocking reads every marker with no parsing at all, and a receipt clears only when its comment STARTS with the marker (after nothing but a byte-order mark and ASCII whitespace) and holds no other, so nothing can precede it to hide it (`independent_receipt_newest`, `container/skills/pr-review-loop/scripts/codex-review.sh`), pinned and mutation-checked by "does not let a CLEAR preceded by …" and the marker-first cases in `scripts/codex-review.test.ts`
