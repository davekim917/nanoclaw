- 2026-09-20 · #952 · inherited claim · The doc comment justifying a new call site asserted the generic shared-dir migrator was "gated on `workgroupNeedsConsolidation`" and that its candidate set was "a readdir of the seed member's folder" — the function does not exist anywhere in the repo, and the migrator has three discovery sources, not one. The two omitted sources are precisely what made the new call unsafe: the `wgDir` crash-recovery scan re-adds any real directory found in the shared root on every boot, so pre-creating `artifacts/` there empty and ahead of the migrator put the name into the migration set for every workgroup, and the move loop's `existsSync(dst)` arm then read a seed's real `artifacts/` as an interrupted move and `rmSync`'d it. A comment asserting another function's gating or inputs must cite the `file:line` that shows it, and a name that appears in no other file is the tell · `SHARED_WORK_DIR_NAME` is in `RESERVED_SHARED_DIR_NAMES` (`src/modules/workgroup/shared-dirs.ts`), which every discovery source already filters and a final defense-in-depth pass re-applies, so the name can never reach the mutating loop; the boot-order regression is pinned by "does not let the shared-dir migrator delete a seed folder holding a real artifacts/" in `src/modules/workgroup/shared-work-dirs.test.ts`, mutation-checked (it fails with the reservation removed)
- 2026-09-20 · #952 · mutation coverage · Six tests for the new function all passed while it destroyed data, because every case called it in isolation and its oracle was built from the same constant the implementation exports: nothing ran it in boot order beside the migrator it interacts with, and renaming `SHARED_WORK_DIR_NAME` would have left all six green while `container/CLAUDE.md` kept sending agents to the literal `artifacts/`. A unit test for a function whose defect is an INTERACTION proves nothing about the interaction, and an oracle imported from the code under test cannot catch a contract that is published as a string somewhere else · the suite now runs `ensureWorkgroupWorkDirs` followed by `reconcileWorkgroupSharedDirs` in the order `src/main.ts` calls them, and a separate case asserts `SHARED_WORK_DIR_NAME` is the literal `artifacts` AND that `container/CLAUDE.md` contains that path, tying the constant to the published instruction (`src/modules/workgroup/shared-work-dirs.test.ts`)
- 2026-09-20 · #952 · fail-open default · The new call was unconditional while the `/workspace/workgroup` mount it depends on is not: the mount is made only when `WORKGROUP_SHARED_FS` is set or a `.migrated` marker exists, and the flag defaults off, so on a default install the symlink addressed a path that does not exist in the container while `container/CLAUDE.md` told every agent to write work products there — into container-local storage that `--rm` destroys. A link is only as good as the mount behind it, and "works on the install that has the flag on" is not a default · the call is gated per workgroup on the same predicate as the mount, with `MIGRATION_MARKER` now one constant rather than a literal repeated per call site, and a case asserting neither directory nor link appears without the flag or the marker (`src/modules/workgroup/shared-dirs.ts`, `src/modules/workgroup/shared-work-dirs.test.ts`)
