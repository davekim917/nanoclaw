- 2026-09-15 · #823 · over-broad guard · The busy hold added for background subagents was latched on one CLI message (`background_tasks_changed`, every non-ambient task) and released on another (`session_state_changed: idle` with the set empty) — and the CLI gates its idle on a NARROWER set than it reports: its idle predicate excludes `local_bash`, `dream`, a parked MCP task, a long-running remote agent and a monitor with no timeout by name (pinned CLI 2.1.272, read out of the binary; sdk.d.ts describes each message accurately on its own and shows no divergence). For those types idle arrives with the set still non-empty, no second idle ever follows the drain, and the hold pins the task container past every reaper until the heartbeat ceiling — the reap this hold was meant to refine — while the new settings-change deferral, which reads the same predicate, parks a `-m`/`-e` message for up to 30 minutes. Found by the substitute review r1 after three Codex rounds had each moved the release point without checking what set the release signal actually described. A latch's raise and release must be shown to describe the same set, from the source that emits them, not from the doc of each · release also at the drain once an idle has been observed with the hold live (`idleSeenWithHold` in `container/agent-runner/src/providers/claude.ts`), the CLI having then shown it will not withhold idle for what is left; `holds through an idle that arrives with work still live, and releases at the drain after it` in `container/agent-runner/src/providers/claude.backgroundTasks.test.ts` fails on restoring the idle-only release
- 2026-09-15 · #823 · fail-open default · Same review, recorded: the hold is raised by a message the CLI emits unconditionally and released by one it emits only behind `CLAUDE_CODE_EMIT_SESSION_STATE_EVENTS`; `hasQueuedWork` eight lines above already gates on `sessionStateSeen` for exactly this reason and the new predicate did not. Not reachable at the pinned CLI (the runner sets the flag), one env-plumbing change from a permanent pin · `hasBackgroundWork` gated on `sessionStateSeen` in `container/agent-runner/src/providers/claude.ts`; pinned by `reports nothing and holds nothing until the CLI has shown it emits session-state events` in `container/agent-runner/src/providers/claude.backgroundTasks.test.ts`
- 2026-09-15 · #823 · over-broad guard · Recurrence one layer in, found by substitute review r2: the fix for the line above recorded "the CLI went idle while the hold was live" as a fact about the QUERY and released at every later drain, but it is a fact about the tasks live at that idle — a subagent joining afterwards withholds idle again, and its own drain (which the CLI reports at terminal status, before the completion notification and the follow-up turn, a window the CLI itself budgets 60s+ for) would have released the hold against a 60s sweep. Evidence taken at one moment must carry the scope it was taken over, and the primitive that consumes it must drop it the moment the scope grows · the idle snapshots the ids it covered (`idleCoveredTasks` in `container/agent-runner/src/providers/claude.ts`) and any membership change adding an id outside it clears `idleSeenWithHold`; pinned by `drops the idle evidence when a task the idle did not cover joins the set` in `container/agent-runner/src/providers/claude.backgroundTasks.test.ts`
