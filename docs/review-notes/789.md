- 2026-09-13 · #789 · env parity · A Python-AST guard that names `ast.Match` directly fails on supported pre-3.10 interpreters before the classifier can produce its fail-closed sizing result; optional AST node classes must be looked up defensively and exercised against an absent-node surface. · `container/skills/smoke-test/scripts/smoke-pr-gate.test.sh`
- 2026-09-13 · #789 · over-broad guard · Conservative alias analysis that treats every NAME-containing derived read as retention forces full campaigns for ordinary eager copies of a validated flat string list; preserve rejection for retained aliases while proving non-retention for bounded eager containers and symmetric concatenation. · `container/skills/smoke-test/scripts/campaign-size-classify.py`
- 2026-09-13 · #789 · mutation coverage · A fixture whose match subject aliases the guarded policy can satisfy a widened reason before it reaches the capture-shadow path it claims to cover; use a non-aliasing subject and retain the narrow shadowing assertion. · `container/skills/smoke-test/scripts/smoke-pr-gate.test.sh`
- 2026-09-13 · #789 · over-broad guard · A reference-free operand is not proof that `+` returns a fresh list: a custom `__add__` can return the imported policy list. Allow reverse concatenation only when its left operand is a literal built-in list, and prove the failure mode with a runtime-widening fixture. · `container/skills/smoke-test/scripts/campaign-size-classify.py`
- 2026-09-13 · #789 · import-time side effect · A test that executes a policy at import must use valid glob-to-regex conversion; compiling `**` as regex syntax raises before the classifier assertion. · `container/skills/smoke-test/scripts/smoke-pr-gate.test.sh`
