- 2026-09-14 · #810 · enforcement scope · Fleet env caps and the compact window were pushed only from the ordinary spawn branch; the wiki branch returned before those pushes, so scrubbing the `settings.json` pin (`DEPRECATED_ENV`) regressed wiki containers to the provider's `165000` fallback. Emit fleet env from the one shared primitive both branches already call, and source `REQUIRED_ENV` cap constants from the same module as the `-e` so a pin can never diverge. · `src/claude-spawn-defaults.ts`, `src/container-runner.ts`, `src/group-init.ts`
