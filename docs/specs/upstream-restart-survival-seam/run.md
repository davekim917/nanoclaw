# Run record: restart survival (convergence seam 4)

Plan: [plan.md](plan.md). One entry per merge/deploy event, newest last. Owner of the series: update-nanoclaw-2b; merges and restarts: update-nanoclaw.

## 2026-09-05 13:24Z — Series A MERGED (#421 → main e74fc53d; coordination accessors + host instance lease, write-only)

- As built: `src/db/coordination.ts` (303 L) and `src/host-instance.ts` (85 L) byte-identical to upstream b76fcb3d (drift tripwire `src/durable-host-seam.test.ts` + `src/durable-host-seam/UPSTREAM-MANIFEST.json`, `scripts/durable-host-seam-manifest.ts --update <sha>` to resync). `main.ts` starts the lease through upstream's own `shadowWrite` immediately after `runMigrations` — a failed `host_instances` INSERT logs and boot continues, never a startup dependency (Codex round 1; upstream's `index.ts` still has the bare await) — and logs the §6 evidence line `Host instance lease started {instanceId, ttlMs}` with the ttl it passed in (Codex round 2); `stopHostInstanceLease()` is the first statement of `shutdown()`'s `finally`. Both pinned structurally in the tripwire test.
- Held byte-identical, filed as #422: the lease predicates compare ISO timestamps as TEXT (every fork operand is `toISOString()`-canonical); renewals can overlap under a slow driver (synchronous-driver premise, same as #416). Candidate upstream PR.
- Evidence on the merged tree: 20 acceptance cases per §7.A green; full host suite 379/386 files with only the #420 set red; ratchet accepts `src/db/coordination.ts`, `src/host-instance.ts` and their two tests (main.ts's insertions match upstream's lines — its divergence shrank).

## 2026-09-05 14:58Z — Series B (#424 → 88b05432) and C (#425) MERGED; restart 1 pending

- **B as built** (head 46a16125): `delivery_attempts` rows are the retry authority — the in-memory map is gone; `recordAttemptRow` runs before the give-up test, `clearAttemptRow` on success and give-up, both swallowing their own failures (a failed record skips give-up and takes the retry branch, which `break`s — ordering preserved). Four Codex rounds: the drain now reads the stored row **before** calling the adapter and gives up from a persisted count without a send (`decidedFrom: 'a count stored before this host started'`); the restart give-up carries the persisted `last_error` verbatim (the runner surfaces it to `send_file` callers); group-delete cascade and single-session delete remove the rows. Round 4 (a delete landing during an in-flight adapter await re-inserts one inert row; no FK in 071) → #430, fork-side fix = host-sweep "coordination-orphans" duty, carried by series A′. Intended behavior change: a message that failed twice under the previous host gives up on its first attempt after the restart — the first `giving up` line after restart 1 is not a regression.
- **C as built** (head fa39ef6e): `CONTAINER_NAME_PREFIX = 'nanoclaw-v2-'` in `src/config.ts` (divergence 1 recorded at the constant: upstream's `ncl-…` grammar is not adopted while the snapshot pruner selects by name prefix), one constant for the spawn name and the pruner filter, pinned to exactly two importers by `src/container-labels.test.ts`; `buildContainerArgs` takes `sessionId` and emits the five `--label` pairs via the exported pure `containerLabelArgs` (workgroup label with an empty value when none). Codex 👍 round 1.
- Gate rule applied to both: full host suite on the final head under the fleet vitest lock (B 385/390, C 384/389 — only #420 red), targeted suites, tsc/eslint/prettier/boundary, `--check HEAD` exit 0, manifest-only rebases after #401 moved main.
- Restart 1 = A + B + C per plan §7 (additive; B the only behavior change, bounded to retry counts). §6 signals to read at +2/+6: `Host instance lease started` exactly 1; lease WARNs 0; `Failed to record delivery attempt` 0; `Failed to clear delivery attempt row` 0; `SELECT count(*) FROM delivery_attempts` ≈ 0 after a few minutes; one `host_instances` row with `stopped_at IS NULL` and the previous boot's row stamped.

## 2026-09-05 17:45Z — Restart 1 DEPLOYED (restart 17:39:12Z on dist b8bc76bb: #414 + series A + B + C); A′ (#438) in review

- Restart came 111 min after BUILD START 15:48Z: the deployer's quiet-window waiter pauses while its session is suspended (second occurrence today; the restart mechanism is being changed by the deployer).
- Gate reads at +2 (17:41:18Z) and +6 (17:45:18Z): preflight ok 1, adapters 12, snapshot 1, quiet cache 1; ERROR 0; nesting/unopenable/write-refused/wake-refused/duty_failed 0; delivered 5 → 25, spawned 2 → 3. **Series A**: `Host instance lease started` exactly 1 (`ttlMs=90000`), lease WARNs 0, one `host_instances` row with `stopped_at IS NULL` and `lease_expires_at` renewing on the 30 s tick (17:42:46Z → 17:46:22Z); no prior-boot row because the 12:53Z boot predates A. **Series B**: `Failed to record delivery attempt` 0, `Failed to clear delivery attempt row` 0, no `giving up` line in the window, `SELECT count(*) FROM delivery_attempts` = 0. **Series C**: every container started after the restart carries `nanoclaw-group`, `nanoclaw-session`, `nanoclaw-workgroup` (both live workgroups) and `nanoclaw-role=agent`; names unchanged. PR-4 async-seam signals all 0. WARN classes unchanged from 75736c04; the one non-zero exit is code 143 from the restart itself.
- **A′ (#438)**, head a7e1b319 on b8bc76bb, in Codex review — three rounds so far, all accepted: the claim refuses a holder whose `host_instances` row is live (upstream's own shape); no claim without a durable host instance id — the `hostname:pid` fallback of plan §7.A′ point 2 is dropped, the spawn path late-starts the lease once and otherwise refuses with a WARN (a central DB that cannot take the lease row could not take the claim row either); round 3 (an expired self lease must not claim — validate the self lease before the CAS, renew once inline, else self-fence the spawn) in progress. Carries the #430 `coordination-orphans` sweep duty (FORK2). Takes restart 2 alone once merged.

## 2026-09-05 19:45Z — Restart 2 DEPLOYED (restart 19:35:29Z on dist 1c11776c: series A′ #438 + seam 3 PR 5a #437); E/F/G building in parallel

- **A′ as merged** (#438, five Codex rounds): claim-first spawn with release on every refusal path and on finish; the claim refuses a holder whose `host_instances` row is live; no claim without a durable host instance id — plan §7.A′ point 2's `hostname:pid` fallback is dropped, the spawn path late-starts the lease once (one shared in-flight promise) and otherwise refuses with a WARN; an expired self lease is renewed once inline, else the spawn self-fences; the `coordination-orphans` sweep duty (#430) as FORK2. Deferred to #439 (close inside series E): the holder-liveness verdict and the incarnation CAS are two statements; the two-host finish bookkeeping.
- Gate reads at +2 (19:37:36Z) and +6 (19:41:36Z): preflight 1, adapters 12, snapshot 1, quiet cache 1; ERROR 0; nesting/unopenable/write-refused/wake-refused/duty_failed 0; spawned 1, delivered 4. A′ signals: claimed-by-another-live-host 0, `Refusing session claim` 0, stale finish 0, orphans swept 0; `session_claims` holds one row = the one running container (incarnation 1, this boot's instance id, its container name). **First durable clean-restart signal**: the previous boot's `host_instances` row is stamped `stopped_at` (graceful) and the new row renews. 5a: `Database not initialized` 0, `[object Promise]` 0, dashboard answers. WARN classes unchanged.
- Operator directive 19:45Z (relayed): the whole sync completes tonight, maximum fan-out. Series E (adoption, code half; #439 items close here), F (`on_wake` reconcile, stop intent, adoption-aware ceiling) and G (narrow `host-restart-warn`) build in parallel on their own branches off 1c11776c; D1 is the third session's lane; the systemd doors are the Codex session's. Batching from here: each restart slot takes what is READY.

## 2026-09-06 01:15Z — Restart 3 DEPLOYED (23:22:29Z on e4b66456: seam 3 PR 5b + series D1 + T-theme merges); F MERGED; E READY; the D1 record

- Restart came 65 min after BUILD START (the deployer's session was suspended; third occurrence — the restart mechanism is being replaced). Gate reads at +2 (23:24:38Z) and +6 (23:28:34Z): preflight 1, adapters 12, snapshot 1, quiet cache 1; ERROR 0; nesting/unopenable/write-refused/wake-refused/duty_failed 0; delivered 8, spawned 1. **D1**: exactly one `Boot quiescence scope` line — 7 workgroups, 1 changed, 0 containers at the door (the shutdown had already stopped them), stopped/survivable/unlabeled/mustStop 0. **5b**: sweep duty failures 0, lease-wait WARNs 0. **A**: lease started 1, `host_instances` chain stamped cleanly across three boots incl. an unexpected 21:13Z restart by another session. **A′**: claims 0 refusals; 18 rows / 1 held (released prior incarnations of live sessions). delivery_attempts 0. Two non-zero exits = code 143.
- **D1 as merged** (#440, the third session's lane; nine Codex rounds, 1–6 and 9 fixed, 7–8 → #455): the startup warn keeps main's order BEFORE the door (#441; §7.D/§7.G amended in patch 060); the bounded runtime probe `ensureContainerRuntimeRunning()` runs between the warn and the door; the door re-evaluates the predicates after the stops are proven and partitions ONCE against that final set, carrying `changedWorkgroupIds` back so main.ts reconciles exactly that set; the returned scope carries `survivableSessionIds`/`mustStopSessionIds` (a container is survivable only with a workgroup label outside the changed set AND a session label — unlabeled fails closed); under D1 the warn's skip set is EMPTY and D2 needs a PRE-stop partition for it; #455 records D2's label-tuple and unique-session validations.
- **F as merged** (#458 → ca903ba9): `reconcileSurvivorWakeRows` (withdraw `agent_host_restart` notes, convert everything else with a re-seq, ack'd rows untouched, existing-mailbox only); durable stop intent stated explicitly by the caller (every `killContainer` site classified; only the three respawning paths record `respawn_after_stop`) and cleared only on a wake that started after the stop settled; `honorPendingStopIntents()` below the startup resets; adoption-aware ceiling. Deferred: #459 (pre-processing_ack window needs a host/runner fence), #463 (admission-queued recovery wake never discharges the intent).
- **E READY** (#456, head 06629177 on ca903ba9, seven Codex rounds): supervision-channel union with the waiter-close truth re-read; `adoptRunningSessions` over the door's `survivableSessionIds` with `holdAsPending` (claim, storage leases, memory reservation, outbound ownership held for every running-but-unadopted survivor until adoption or proven absence), P2 container fence, #439 items 1–2 closed (holder verdict + CAS in one central transaction; stale-finish fence transactional with no unfenced fallback), fail-closed inventory (the seed set is what the door actually left running), door 1 `beginContainerShutdown` with the door's stopping set to the shutdown warn, `markCliServerReady` as the CLI wake-source gate after #453. Deferred as **#462 — a D2 gate** (the pending state is unreachable while D1 stops the survivable partition): exit observer / killContainer routing / mount-quiescence ownership / periodic storage protection / over-budget pending reservation / unstoppable duplicate / pending vs concurrency cap. Full suite 430/434 with only the #420 rows red (row 9 = the T2 stdin-json e2e, identical on main).
- **PR 6 (#460)**: Codex round 1 exposed plan risk R4 — once `centralTransaction` opens async closures, every remaining raw runtime statement must run under the lease; the fix is the `withCentralSync(() => withRawDb(…))` belt on the 19 never-inventoried files plus an AST tripwire; those leaves convert properly in **seam 3 PR 5d** (nine leaves + ten importers, same worker, pin 195 → the seven-file allowlist). PR 6 also carries the three 5c helper families; 5c (#464, merged) kept the two disjoint ones.
- Restart 4 payload: E + the groups systemd drop-in applied live (+ PR 6 if READY). Gate adds: `Reconciled sessions at startup` exactly 1 (adopted 0 under D1), `Re-issuing interrupted restart` 0, `Refusing session claim` 0, `Ignoring stale session finish` 0, `Coordination orphans swept` 0.

## 2026-09-06 03:00Z — E MERGED (#456 → bfcc1ef0); Restart 4 DEPLOYED (02:47:58Z on c159f388: E + the groups systemd drop-in + two T4 PRs); restart 5 is NOT the survival test

- Two restarts between 3 and 4 (01:20:13Z on e3a74c02, 01:30:47Z on bab551c1) were another session deploying an unreviewed observatory branch from the live checkout, not sync payloads; the branch was rescued to `codex/live-main-rescue-0222Z` and live `main` reset. Their +2/+6 reads were green for the seam signals anyway (one `repo-freshness` lock ERROR = fleet repository lock; seven `SIGKILL after grace` at load 17 on the 01:20Z shutdown, load-dependent) and are recorded here only so the restart count reconciles.
- **Restart 4 gate** (drop-in live first: `KillMode=mixed`, `ExecStop=` empty, `TimeoutStopSec=30`; the old, pre-E process still ran `stopAllContainers` at this shutdown — `Stopping` → `Stopped` in 4 s). Reads at +2 and +6 (02:53:28Z): preflight 1, adapters 12, snapshot 1, quiet cache 1; ERROR 0; nesting/unopenable/write-refused/wake-refused/duty_failed 0; delivered 7, spawned 1; `Host instance lease started` 1, one live `host_instances` row. **E's first live run**: `Reconciled sessions at startup adopted=0 stopped=0 pendingClaim=0 fencedInbound=0` exactly once (nothing was left running to adopt); `Re-issuing interrupted restart` 0; `Refusing session claim` 0; stale-finish 0; listing/inventory-failure WARNs 0; `[object Promise]` 0. The 81 `Worktree cleanup: refusing non-linked or mismatched checkout` WARNs are the pre-existing storage-GC class.
- **Hygiene finding (F2)**: `session_claims` carries 22 rows with `stop_intent = 'stop'` written by shutdown kills since 01:31Z and none is ever cleared — `honorPendingStopIntents` skips plain `stop` by design and the wake-side discharge is gated on this process's own respawn promises. One inert row per session, bounded, no control effect; worth a boot-time clear after `honorPendingStopIntents`. Not a gate failure.
- **Restart 5 cannot show survival.** E's door 1 leaves containers running at shutdown, but D1's boot door (`quiesceWorkgroupsForBootMountChange`: "D1: the stop set is the whole install") stops every install container before partitioning, and `main.ts` seeds adoption with an empty set when `stopped == containers`. Restart 5 therefore proves the shutdown half only (containers outlive the host process, `Container shutdown begun — leaving running containers` with running > 0, `Stopping all containers` 0, no new stop-intent rows at shutdown, then the next boot's `Boot quiescence scope` with `stopped == containers` and `adopted=0`). The first real survival read needs **D2** (#462: pre-stop partition, `mustStop` as the stop set, warn skip set) and is not on any lane tonight; its signal list (`stopped < containers`, `adopted=N`, claims re-held with `container_ref` = the surviving container, zero spawns for adopted ids, heartbeats advancing) is in the 02:57Z message to the deployer.
- **PR 6 (#460)** rebased onto c159f388 by this session after its worker hit a usage limit mid-rebase: container-runner.ts resolved as PR 6's `withCentralSync` guard+spawn block carrying E's registry entry shape (the channel object shared with the finalize fence); manifest regenerated (the same 27 upstream-owned files accepted, lease-belt form + forced awaits); one 5b acceptance test now evaluates the wake guard under the lease. Gates on e57ab900: tsc/eslint/prettier/boundary clean, ratchet Δ 0, 73 targeted files / 1,769 tests green. Codex round 2 left three open P1 threads (child `error` listeners must attach inside the lease block — `process.nextTick` beats the `await` continuation; destination revalidation inside the upsert lease; companion destination name allocation serialized with the wiring row) — fixes in flight on the same worker, then the full suite on the final head, then READY. 5d opens after.

## 2026-09-06 05:25Z — Restart 5 DEPLOYED (05:12:46Z on 2b8afb6a: seam 3 PR 6 #460 + 5d #483 + two observatory commits); the shutdown half proven live; D2 (#479) in review

- **PR 6 as merged** (#460 → 7bb7d12e, six Codex rounds): the eleven closures on `centralTransaction`; every runtime raw statement under the lease (`withCentralSync(() => withRawDb(…))` belt + the AST tripwire `src/db/raw-outside-lease.test.ts`); child listeners attached inside the lease block (a `process.nextTick` `error` beats the `await` continuation); destination re-validated inside the upsert lease; companion destination allocated inside the wiring transaction; grant/revoke re-check the caller in the writing block; worktree-cleanup's session inventory under the lease (its allowlist row's "own process only" premise was false — main.ts side-effect-imports it). Round 4+ findings of the stale-snapshot class deferred under the stopping rule: #478, #481, #482. Full-suite evidence was taken on a local merge of main + the PR head (the tree that matters once main carries test fixes newer than the PR's base) — 439/441 files, only the two known-red.
- **Restart 5** was fired by another session's `deploy.sh` during the freeze (two observatory commits pushed straight to main); the deployer had killed its own detached restart so two restarts would not race a half-built dist. Reads at +2 (05:14:48Z) and +6 (05:18:40Z): ERROR 0; preflight 1, adapters 12, snapshot 1, quiet cache 1; nesting/unopenable/write-refused/wake-refused/duty_failed 0; delivered 8 → 23, spawned 3 → 5; PR 6 signals (raw handle inside an open transaction, lease wait > 1 s, transaction watchdog, raw access outside a sync block, escaped callback, `[object Promise]`) all 0; no session with two containers.
- **The shutdown half is proven live** (first run of E's door 1 in the OLD process): `Container shutdown begun — leaving running containers for the next host to adopt running=4 adopted=0 stopping=0 spawning=0`; `Stopping all containers` 0; `Killing container` 0; no stop-intent writes; systemd `Stopping` → `Stopped` → `Started` inside one second with the four container-attach processes reported as "remains running after unit stopped" (KillMode=mixed, ExecStop empty). Then D1's boot door: `Boot quiescence scope workgroups=7 changed=0 containers=4 stopped=4 survivable=4 unlabeled=0 mustStop=0` — all four survivors were stopped by the door and three were respawned on their wakes with accountability notes. `mustStop=0` is the record that under D2 all four would have been adopted.
- Migration 073 (`observatory-signal-workgroup-cascade`) applied at this boot; its FK check surfaced one pre-existing orphan (`pending_channel_approvals` row whose messaging group is gone) → #487, hygiene, not the migration's doing.
- **D2 (#479, seam4/d2-boot-door-partition)**: the door partitions pre-stop and stops only `mustStop` (fail-closed for unlabeled/unknown/unwakeable), re-partitions post-stop for the adoption contract, survivors get no accountability note (the pre-stop survivable set is the warn's skip set), adoption is seeded from the returned survivors, #462 items 1–7 on the live path. Codex round 1 (four threads: pending survivors live for the repository-barrier predicates, second-pass reclassification warned before its stop, seed from the returned survivors, host-side restarts select pending survivors) fixed; round 2 (three P2s on pending survivors vs restart paths: parked `onExit` callbacks on a failed pending stop, unlisted survivors resolved before counting a restart, identity preserved across adoption in the generation check) in progress. Restart 6 = D2, gated with the post-D2 survival list (entry 03:00Z). Standalone follow-ups after restart 6: #474 (clear plain stop rows at boot), #487.

## 2026-09-06 07:30Z — D2 MERGED (#479 → b3831bff); Restart 6 DEPLOYED (07:14:11Z on b3831bff): RESTART SURVIVAL PROVEN END TO END; three off-protocol restarts recorded

- **D2 as merged** (#479, five Codex rounds): the boot door partitions PRE-stop and stops only `mustStop` (mount-changed workgroups plus everything not provably survivable: unlabeled, unknown session, unwakeable), re-partitions post-stop for the adoption contract, warns must-stop sessions before their stop and survivors not at all (the pre-stop survivable set is the warn's skip set), and seeds adoption from the returned survivors; #462 items 1–7 on the live path; pending survivors are first-class (live for the repository-barrier predicates, warned when the second pass reclassifies them, selectable by host-side restarts, parked exit work on an unprovable stop, restart identity by container name, a second listed container for a pending session is the duplicate case and never a re-point). Deferred under the stopping rule: #488, #489, #490, #493 (P1 by consequence, narrow precondition: a container must appear for a changed workgroup during the awaited re-evaluation), #494, and one facet of #455. Full suite on the head itself (main was its ancestor): 444/446 files, 7004 passed, only the two known-red.
- **Restart 6** (deployer's build, quiet-window restart): the survival case, first live run. Shutdown: `Container shutdown begun … running=4 adopted=0 stopping=0 spawning=0`, Stopping-all 0, Killing 0, three attach processes left running by systemd, Stopped→Started within the second. Boot: `Boot quiescence scope workgroups=7 changed=0 containers=4 stopped=0 survivable=4 unlabeled=0 mustStop=0` → `Reconciled sessions at startup adopted=4 stopped=0 pendingClaim=0 fencedInbound=0`. At +2 and +6 (07:16:14Z, 07:20:39Z) the four running containers were the same four created before the restart (06:38–07:05), `session_claims` for the four held by the new host instance with `container_ref` equal to each surviving container's name, zero `Container spawned` for any adopted id, no accountability notes (survivors get none), Adopted-on-wake/refused/listing-failed/inventory-failed/unadoptable/not-proven/Re-issuing/Refusing/stale-finish/duplicate all 0, PR 6 signals all 0, ERROR 0, heartbeats advancing on the two survivors that worked after the boot (idle survivors keep their mtime; the touch is activity-driven). Not yet observed in the window: an inbound message answered by an adopted container without a spawn (no inbound row reached an adopted session by +6; recorded when the first one does).
- **Three restarts outside the deploy protocol**, all by the observatory-branch session from the live checkout, all read against the shutdown-half list and green: 05:12:46Z `deploy.sh` on 2b8afb6a (recorded above as restart 5), 05:48:46Z plain `systemctl restart` on the same dist (door `containers=5 stopped=5 survivable=5 mustStop=0`, adopted=0, PR 6 signals 0, ERROR 0), 06:38:02Z `deploy.sh` on 7a317201 (a five-commit push straight to main that failed the deployer's gate on eslint and the public-boundary check until fixed; door `containers=3 stopped=3 survivable=3`, adopted=0, ERROR 0). The structural fix (that session off the live checkout) is the operator's.
- Follow-ups in flight on one worker: #474 (clear honoured plain stop intents at boot — two of tonight's adopted sessions still carried a stale `stop`), then #493. Remaining D2-review residue stays as issues (#488, #489, #490, #494, #455).

## 2026-09-06 09:20Z — Restart 7 DEPLOYED (09:13:30Z on 3108504a: the door's final inventory #499, the stale stop-intent clear #498, hot-attach #496); survival holds, #474 closed on live data

- **#499 as merged** (Codex-clean on the first round): the door takes its second inventory AFTER the awaited re-evaluation and always partitions a final one, so a container that appears for a changed workgroup mid-await is classified and stopped in the second pass instead of slipping past a short-circuit. The worker's deviation from the brief is recorded in the PR: dropping the short-circuit alone would have turned that newcomer into a boot throw rather than a stop, because the second inventory was being listed before the await.
- **#498 as merged** (five Codex rounds; the boot clear lives inside `honorPendingStopIntents` as a second phase, so the startup order and its AST test are untouched): rows are matched on `(session_id, updated_at)` as read; a plain `stop` on a session ADOPTED at this boot is treated as a stale request and cleared with `Cleared a stale plain stop intent for an adopted survivor` rather than re-issued (re-issuing would kill live survivors on every restart); the clear is chunked below SQLite's bound-variable limit and its failure is logged instead of swallowed. Round-5 P3 (a per-row line can overclaim when the row was rewritten between the read and the conditional clear) → #501.
- **Restart 7 gate** (+2 09:15:33Z, +6 09:19:49Z): ERROR 0; preflight 1, adapters 12, snapshot 1, quiet cache 1; PR 6 signals all 0. Survival held a second time — shutdown `Container shutdown begun … running=1`, boot `Boot quiescence scope workgroups=7 changed=0 containers=1 stopped=0 survivable=1 unlabeled=0 mustStop=0` → `Reconciled sessions at startup adopted=1 stopped=0 pendingClaim=0 fencedInbound=0`, the survivor still the container created before the boot, its claim re-held by the new instance with `container_ref` matching, zero spawns for any adopted id, every adoption/refusal/stale/duplicate signal 0. **#474 closed on live data**: `Cleared honoured stop intents at startup cleared=34 deferredPendingAdoption=0` took `session_claims` from 18 null / 34 `stop` to 52 null / 0 `stop` in one pass, including the adopted survivor's own stale row.
- Still unobserved after two survival restarts: an inbound answered by an adopted container without a spawn — no inbound has reached an adopted session inside a gate window yet. A watcher is on it; it is a read, not a fix, and it does not gate anything.
- Open residue as issues: #455, #478, #481, #482, #487, #488, #489, #490, #494, #501.
