- 2026-09-13 · #776 · fail-open default · A viewport command issued before navigation on a fresh grid session can silently leave a narrow/default visual artifact when its failure is discarded; navigate first, make viewport failure terminal, and assert the command sequence plus no-sheet failure path. · `container/skills/smoke-test/scripts/smoke-contact-sheet.test.sh`
