- 2026-09-16 · #869 · inherited claim · The comment this PR edited already said "a native spawn_agent's own reasoning_effort overrides it per task". That was TRUE of the thing it described — the global `[agents].default_subagent_reasoning_effort`, which a spawn call does override — and I carried the sentence forward onto the per-role `model_reasoning_effort` the PR was adding, without re-reading. It is the reverse there: `spawn_agent` applies the call's effort first and the role second, and the role sets it unconditionally, so the role wins and Codex tells the orchestrator so in as many words. An inherited sentence reads as verified because it once was, of something else; the rewrite is exactly when it stops being checked · every behavioural line in the rewritten block now cites the declaration it rests on (`src/claude-agent-md.ts`), and the two that decide behaviour name the ordering site and the assignment site separately rather than one summary
- 2026-09-16 · #869 · over-broad guard · `formatCodexAgentToml` began emitting a TOML basic string built from frontmatter, and its encoder throws on a newline. A block-scalar `effort:` would therefore throw — and `syncCodexSubagents` wraps neither the format call nor its write, so the throw would not skip one malformed agent, it would abort the loop and leave every role after it unconverted with the prune below it unrun. Refusing one bad input is only fail-closed when the refusal is scoped to that input; inside an unguarded loop it is a blast radius · the malformed value degrades to the no-key state the file already rendered before this mapping existed, with the loop's lack of a try/catch cited at the guard (`src/codex-sync.ts`) and a block-scalar case in `src/claude-agent-md.test.ts`
