- 2026-09-15 · #818 · unobservable failure · #812's Codex rate-limit READ was fail-open telemetry whose only error report was a container log line that dies with the container, and its sibling PUSH path kept writing `rate_limit_samples` rows, so two hours of a half-dead feature read as health fleet-wide (408 push rows, zero pull rows) and a human reading a live container's logs was the only thing that found it; a fail-open path needs a detection surface that does not depend on its own error reporting surviving, and for a two-writer table that surface is the asymmetry between the writers · `scripts/rate-limit-telemetry-health.ts` reports an (agent group, credential_set) pair pushing inside the window with no `usage_pull` row in it, and counts DB-open/query failures and unparsable timestamps separately from zero rows so a swallowed read never reads as "no rows"; cases in `scripts/rate-limit-telemetry-health.test.ts`, mutation-checked
- 2026-09-15 · #818 · unobservable failure · Recurrence inside the detector built for the first one, found by review r1: the DB reads were carefully accounted while the FILESYSTEM walk around them was not, so `catch { return [] }` on a directory listing and an `fs.existsSync` guard (which answers false on EACCES, not just on absence — measured at uid 1001, where `statSync` throws and `readdirSync` on a 0o000 directory throws EACCES) each turned "could not look" into "nothing there", and an unlistable sessions root produced zero groups, zero errors and a gate reporting a healthy fleet. A scanner is subject to the class it scans for, and accounting has to cover every input it reads, not the interesting ones. The same shape in the source text: two literal NUL bytes made `grep`/`rg` treat the whole file as binary and return nothing over it, so searches looked clean because they silently matched nothing · one funnel, `listSubdirectories`, answering a discriminated union rather than a bare array so a failure cannot be spelled like an empty directory, `probeSessionDb` stat-based with ENOENT the only absence, an unlistable root a hard failure rather than a finding, and `KEY_SEP` an escape sequence pinned by a no-literal-NUL test — `scripts/rate-limit-telemetry-health.ts`, with one case per failure path in `scripts/rate-limit-telemetry-health.test.ts` (each asserting the error is counted, named in output and reflected in the gate decision), mutation-checked against the restored bare catch
