- 2026-09-15 · #839 · pattern audit · Separating the `opus` family alias from the group's model was done at the docker boundary (`claudeSpawnEnv`) and not grepped to every other place that writes `ANTHROPIC_DEFAULT_*`: `ClaudeProvider.query` rewrote the resolved model's OWN family alias into `perQueryEnv` for each query, so a group pinned to a non-current opus (`opus48`) had `opus` rewritten back to its own pin and its `model: opus` subagents kept running the group's model — the exact defect the PR claims to fix, surviving one layer in, with the PR body asserting otherwise. Found by review r1 (P2). When a fix redefines what a variable MEANS, the audit is every writer of that variable, not every reader of the one you changed · the per-query family rewrite is deleted in `container/agent-runner/src/providers/claude.ts`; `test_claude_no_subagent_model_pin` in `container/agent-runner/src/providers/claude.configSchema.test.ts` now runs an opus-4.8-pinned group and fails if the query rewrites the alias
- 2026-09-15 · #839 · undeclared default migration · The PR moved every unpinned Claude group from Sonnet 5/`xhigh` to Opus 5 [1m]/`medium` on the next spawn and shipped no `[BREAKING]` CHANGELOG entry, so `/update-nanoclaw` — which surfaces only newly added `[BREAKING]` lines — would have given operators neither warning nor a pre-deploy pin path, and the existing scheduled-task entry still promised Opus at `high`. Found by review r1 (P1). A change to a DEFAULT is a migration for every install that never configured the thing, and those are exactly the installs with nothing in their config to warn them · `[BREAKING]` entry plus `docs/claude-default-model.md` (detect/why/fix/verify/rollback), and the stale effort in the prior entry corrected; `scripts/review-notes.test.ts` registers the class, and `CONTRIBUTING.md`'s rule 1 is the check that a `[BREAKING]` entry names its migration path
- 2026-09-15 · #839 · projection read · The migration doc's detect step read `ncl groups config get`, which returns the `container_configs` row (`presentConfig`, `src/cli/resources/groups.ts:68-74`), while the spawn path reads `groups/<folder>/container.json` (`readContainerConfig`). On an install where the two have drifted — an older DB-only update, a hand edit — the step reports a group as pinned while the next spawn reads an unpinned file and moves it to Opus pricing, i.e. the detection says the exact opposite of what will happen. It also missed the legacy `defaultModel` field entirely. Found by review r2 (P1). A pre-deploy check must read the store the runtime reads, and CLAUDE.md already says which one that is · detect step in `docs/claude-default-model.md` now jqs `groups/<folder>/container.json` for `model // defaultModel`, states why the projection is not authoritative, and was run against the live install (9 Claude groups, 8 unpinned — the PR's own blast-radius number, measured with the corrected command)
- 2026-09-15 · #839 · projection read · Second occurrence, one round later and one layer deeper: the corrected detect step read the authoritative `container.json` but still treated a present `model` as a pin, while `resolveClaudeSpawnDefaults` DROPS a value that is not Claude vocabulary and falls through to the default — so a `gpt-*` id left by `ncl groups config update --provider claude` (which does not clear the previous provider's model) reads as pinned and runs as unpinned, i.e. exactly the silent repricing the doc exists to prevent. Found by review r3 (P1). Two rounds of hand-written checks each got a different half of the runtime's answer wrong; the fix is to stop paraphrasing the resolver and call it · the detect step in `docs/claude-default-model.md` now imports `resolveClaudeSpawnDefaults` and `readContainerConfig` and prints configured value, resolved model and refused values per group, so it cannot drift from the vocabulary or the precedence chain; run verbatim against the live install (9 Claude groups, 8 resolving to the default)
