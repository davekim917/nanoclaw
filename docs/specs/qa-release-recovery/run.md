# Execution evidence

The approved change was recovered from three preserved patches into an isolated
worktree based on the current upstream branch. Private identifiers and unrelated
installation records were removed before publication.

## Shared QA source

Pass markers require nonempty evidence files whose resolved paths stay within
the run directory. Missing, empty, traversal, newline, and nested-symlink escape
paths fail the barrier. Nonpassing terminal markers can record a concrete reason
without manufacturing success evidence.

Recovery redispatch preserves missing original receipts. Before a newer
marker generation replaces the live marker, the scaffold archives the exact
original bytes. Conflicting existing history refuses replacement.

An overdue mandatory floor produces an agent-owned NO_GO for its affected
promotion. It does not automatically become a human decision, clear an existing
hold, or stop unrelated development work. Bounded findings permit an agent-owned
fix, claim correction, or tracked deferral, with dissent retained.

## Verification

The focused scaffold and evidence-barrier suites cover evidence validation,
fresh recovery, original-marker preservation, and conflicting-history refusal.
Independent review found no remaining must-fix in these changes.

Final publication gates and the PR head are reported by the submitting session.
No deployment, service restart, production-checkout change, or application merge
is part of this recovered PR.
