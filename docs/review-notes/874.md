- 2026-09-17 · #874 · pattern audit · The PR replaced the raw read-write bind of the operator's whole host `~/.codex` for a peer container with a staged home, and the round-1 review approved that. Fifteen lines below, in the same `if` block, the `codexAuthFallbacks` branch still bind-mounted each declared host home read-write by exactly the same shape — found only in round 2. The fix was written against the finding's coordinates rather than against the pattern, and one grep for `readonly: false` inside that block would have shown both. A host-home mount is the unit to audit, not the line a reviewer happened to name · both branches go through one helper, `stageCodexAuth` in `src/container-runner.ts`, so a host home reaches a container only as a file-bound auth.json inside a session-owned directory; `src/provider-surfaces.test.ts` asserts the peer fallback mounts neither the host transcripts dir nor any raw host path, mutation-checked against routing it back through `materializeCodexFallbackRuntime`
- 2026-09-17 · #874 · over-broad guard · Staging a credential runs host-side filesystem work, and its guards throw. Applied to a PEER capability — Codex in a Claude container, OpenCode in any — a throw fails the whole spawn, so a single malformed host file would crash-loop every container on the fleet. Worse, `stageOpenCodeAuth` calls `assertValidGroupFolder` as defense-in-depth, and a live `agent_groups.folder` can fail that grammar because `toFolder` (`src/modules/permissions/channel-approval.ts:119`) has no length cap and the v1 migration copies folder names verbatim; the spawn path checks the grammar nowhere else, so such a group spawned fine before this PR. CI caught it as four unrelated suites dying on `Invalid group folder`. The container-side runtime had already written the rule down — `container/agent-runner/src/codex-companion-setup.ts` refuses to convert "peer codex degraded" into "every container crash-loops" — and the host side reached for the opposite default · `isHostStateFailure` in `src/container-runner.ts` splits host-state failures (a syscall errno, the `Unsafe …` and `Invalid group folder` refusals) from bugs: the first withholds the peer credential with a warn and the container boots, the second still throws, and a `provider: codex` session still throws on both because there the credential IS the session; `src/provider-surfaces.test.ts` pins the withhold and the codex-still-throws halves, mutation-checked in both directions
