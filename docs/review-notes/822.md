- 2026-09-15 · #822 · enforcement scope · A deploy's "everything now runs the new code" guarantee bound only to the one unit `scripts/deploy.sh` named, so the fork's second long-running service kept serving whatever it booted with: nanoclaw-codex-sync.service runs `pnpm exec tsx src/codex-sync-watcher.ts`, tsx loads the source once at process start, and a two-day-old watcher re-mirrored the pre-#813 `CODEX_WORKER_MODELS` map over `~/.codex/agents` and all 18 `groups/*/.codex/agents` on every watched change, reverting a hand-fixed value three times. A second hardcoded name would have bound just as narrowly at the third unit, and this host already carries a third Type=simple nanoclaw unit file; the restart set has to be DISCOVERED from the property that actually defines the bug (an active non-`oneshot` service whose `WorkingDirectory` is this checkout) and the discovery has to carry its own control, because a broken enumeration answers "no siblings" in exactly the words a sibling-free host uses. Ordering is part of the scope too: `systemctl restart nanoclaw-v2` SIGTERMs the cgroup the detached deploy script lives in, so a restart appended after it is on a line that never runs and a test asserting the unit name appears in the script would pass over it · `long_running_repo_units` in `scripts/deploy.sh`, with nanoclaw-v2.service as the enumeration's own control (a discovery that loses it refuses the deploy) and a failing restart writing `status: failed` before the handoff; exercised — not grepped — in `src/deploy-sibling-restart.test.ts`, which runs the real tail of the script against a fake `systemctl` that SIGKILLs its caller on the `nanoclaw-v2` restart so anything past that line is unreachable in the harness too (10/10 mutations killed, including the relocated-block and swallowed-failure ones)
