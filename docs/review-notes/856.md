- 2026-09-16 · #856 · enforcement scope · Deferred from #826 and now closed: the always-on composer's sub-plugin walk accepted EVERY non-hidden directory, while both container walkers require a manifest to treat one as a plugin (`hasManifest`, `container/agent-runner/src/providers/claude.ts`; `findCodexSubPlugins`, `container/agent-runner/src/codex-companion-setup.ts`, both layouts each). So a ruleset file in `~/plugins/<repo>/docs/` would be injected into every OpenCode group's standing prompt while no walker mounts, registers or excludes that directory — and `excludePlugins` names plugins, so nothing could withhold it either. A reader that decides "is this a plugin" more loosely than the loaders is a control surface nobody can reach · a sub-plugin must declare itself one with a Claude or a Codex plugin manifest — EITHER, since this composer serves OpenCode and a directory either loader would load is one it must speak for (`src/claude-md-compose.ts`); non-plugin directories in both layouts and a codex-manifest-only sub-plugin are pinned in `src/claude-md-compose.test.ts`, mutation-checked against dropping the gate and against narrowing it to the Claude manifest. Nothing on this install changes: every directory shipping a ruleset file today carries both manifests
- 2026-09-16 · #856 · deferred hardening · The other half of #828, closed as a documented seam rather than code: a repo-ROOT ruleset outliving a sub-path exclusion is not a leak to fix but the meaning of the two entries — a sub-path entry withholds the SUB-PLUGIN's own file, while the root file is the REPO's directive. The reported instance (bootstrap's root NanoClaw override file concatenating the Orchestrate directive that `bootstrap/plugins/orchestrate` was supposed to withhold) was resolved on the plugin side: the repo now ships each directive beside the sub-plugin it belongs to, carries no root file, and its own boundary check refuses the NanoClaw-side override file anywhere. The operator's design call — compose a plugin's own generic root ruleset rather than require a NanoClaw-specific marker in a repo we maintain — is the composer's behaviour as of #826's last round · the seam is pinned as behaviour (a sub-path entry keeps the root ruleset, a top-level entry drops it) in `src/claude-md-compose.test.ts` and stated where an operator reads it, in `docs/skills-model.md` and `.claude/skills/enable-agent-plugins/SKILL.md`, including why a root ruleset that restates a sub-plugin's directive defeats the sub-path entry
- 2026-09-16 · #856 · pattern audit · Found by the substitute review: the gate treated the two manifests as one rule (does the file exist), but each walker holds its own to a different bar — Claude asks only for existence while Codex must PARSE the manifest and read a non-empty `name`, which is what it registers. So a sub-plugin with a broken `.codex-plugin/plugin.json` composed its directive for OpenCode while Codex skipped it entirely. A predicate that stands in for two other readers has to be derived from each of them, not from the one that was easier to state · each manifest is held to its own walker's rule (`src/claude-md-compose.ts`), with unparseable, nameless and blank-name Codex manifests pinned in `src/claude-md-compose.test.ts`, mutation-checked against falling back to mere existence
- 2026-09-16 · #856 · deferred hardening · Also the substitute review, recorded not fixed: this composer and Claude's walker do not walk the same DIRECTORIES — Claude stops descending at a repo root that carries a manifest and descends one level deeper than this does elsewhere, while the composer always walks `<repo>/<sub>` and `<repo>/plugins/<sub>`. The divergence is load-bearing in the direction it currently runs: `bootstrap` ships a root Claude manifest, so adopting Claude's stop-at-the-root rule would withhold `plugins/wwbd`'s directive from every OpenCode group. Matching them would mean changing which side, and that is a question about delivery rather than about this gate · stated on `hasPluginManifest` (`src/claude-md-compose.ts`) so the parity claim reads as "asks the same question of one directory", never "walks the same set"; re-raise if a repo ships a directive at a depth this composer does not reach, or if Claude's walk changes shape
