- 2026-09-15 · #835 · evidence keying · Found by review r1, and a recurrence of #826 r3's class one PR later: the OpenCode XDG drop set decided what to withhold by comparing the NAMES that survived two discovery walks, one with the exclusion list and one without. Discovery keeps only the first plugin to claim a name (`src/plugin-skill-discovery.ts:407-409`, alphabetical) and the mirror being filtered was built from the UNFILTERED walk (`src/opencode-sync.ts:253-256`), so when the excluded sub-plugin is a name's FIRST provider the name survives the filtered walk — supplied by the later plugin — while the bytes in the mirror are still the excluded one's. The difference then read that name as "kept" and copied the excluded source into the group, which is the exclusion INVERTED rather than merely leaked, and every test written for it used a fallback plugin sorting BEFORE the excluded one, so the only order that breaks was the one never exercised. A name is part of an identifier the producer guarantees unique in full (the `skillDir`); comparing the part throws the guarantee away, exactly as the composer's basename fragment keys did · the drop set asks `isExcludedPluginPath` about each discovered skill's own `path.relative(pluginsRoot, skillDir)` instead of differencing names, so one walk answers the question the copy depends on (`src/providers/opencode.ts`), with the inverted-order case — an excluded sub-plugin sorting ahead of an unexcluded plugin claiming the same skill name — pinned in `src/providers/opencode-skill-exclusion.test.ts`, mutation-checked against restoring the name difference
- 2026-09-15 · #835 · deferred hardening · Recorded beside the fix above rather than fixed: the drop set's own walk denies only `DEFAULT_DENY_PLUGINS`, while the mirror it filters also denies workgroup-scoped plugins (`src/opencode-sync.ts:246`), so the two walks can pick different winners for one name and a scoped plugin sorting ahead of an excluded sub-plugin would leave the excluded source copied through. Closing it threads the mirror's deny set through the spawn-path call, which is machinery beyond the accepted finding · #836 (re-raise if a workgroup-scoped plugin ever ships a skill name an unscoped plugin also ships, or the two walks' deny sets diverge further)
