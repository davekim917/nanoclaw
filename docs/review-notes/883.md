- 2026-09-17 · #883 · inherited claim · A comment I wrote for this PR said the container's `McpServerConfig` "knows `instructions` but neither of these", justifying why `serializeMcpServersEnv` strips `displayName`/`description` and keeps `instructions`. The citation came from `grep -n instructions container/agent-runner/src/providers/types.ts`, whose first hit is `systemContext.instructions` at :231 — a different type entirely. The real `McpServerConfig` is at :260-298 and declares none of the three, so the sentence was wrong about the container side in a comment whose whole job was to explain the container side, written in the same session as a reply to a review finding about uncited cross-module claims. A grep hit is not a declaration; the first line a term appears on is rarely the one that defines it. Two more citations in the same PR (`src/container-config.ts:424-455`, `src/container-runner.ts:4855`) were correct when written and stale by the time the PR was read, having moved under this PR's own edits · `src/fleet-mcp-servers.test.ts` pins every file:line this PR's comments cite to a substring that must still be on that line — twelve of them across `src/fleet-mcp-servers.ts`, `src/capabilities.ts` and `src/container-runner.ts`, including the two that had already drifted — so a moved citation fails a test instead of quietly becoming a confident lie
