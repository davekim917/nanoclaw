# Category 5 — Git log cluster scan

Scope per V1_BEHAVIOR_AUDIT_PLAN.md Category 5, execution mode = cluster scan.

## Harvest summary

- Command: `git log --all --format="%h|%s" | grep -iE "^[a-f0-9]+\|(fix|safety|recover|auto|rollback|prevent|guard|ensure|defense|hotfix|repair)(\(|:)"` in `/home/ubuntu/nanoclaw`
- Total matching commits: **435**
- v1 repo is actively maintained through 2026-04-16 (same day as audit), so this history reflects current v1 state.

### Clusters (keyword-bucketed; commits may appear in >1)

| Cluster | Count | Spot-checked | Triage |
|---|---|---|---|
| container-spawn / mount / build | 68 | 6 | load-bearing |
| credentials / OneCLI / OAuth | 51 | 8 | load-bearing |
| delivery / typing / pipe / dispatch | 52 | 10 | load-bearing |
| session-lifecycle / cursor / rollback / watchdog / signals | 46 | 12 | load-bearing |
| db / schema / migration / transaction / lock | 36 | 3 | mixed |
| approval / gate / consent / permission | 34 | 6 | load-bearing |
| router / trigger / sender | 30 | 3 | load-bearing |
| ipc / mcp / git tools | 24 | 4 | load-bearing |
| compact / autocompact | 13 | 3 | load-bearing |
| worktree / cleanupThread / auto-save / workspace | 11 | 11 | load-bearing |
| channels (whatsapp/discord/slack/telegram/signal/gmail) | 99 | 5 | channel-specific, mostly skill-installed in v2 |
| deps / typo / logs / prettier | 40 | 0 | SKIPPED (trivial) |

Channel-cluster commits are largely deferred because in v2 the channel adapters live on the `channels` branch (skill-installed) — audit against that branch is out of scope for this pass.

## Clusters scanned

### worktree (11 commits, 11 spot-checked)

| v1 commit | Behavior | v1 rationale | v2 status | Severity | Port plan |
|---|---|---|---|---|---|
| `cf88c5d` | Auto-commit dirty worktrees on session exit + `git worktree remove` + `git worktree prune` before add. `findGitWorktrees()` with `GROUPS_DIR` containment check against crafted `.git` files. | rmSync alone left dangling `.git/worktrees/` entries; next session's `worktree add` failed with "already checked out". | Auto-commit **FIXED IN `eb90165`** (see V1_BEHAVIOR_AUDIT.md row 1). `git worktree remove --force` + `prune` present in `src/worktree-cleanup.ts:87-95`. Containment check against `GROUPS_DIR` — PARTIAL: v2 composes paths from `GROUPS_DIR + ag.folder` rather than validating externally-supplied worktree metadata. V2 worktree creation lives in `container/agent-runner/src/mcp-tools/git-worktrees.ts` — confirm defensive prune before add. | CRITICAL (auto-commit) / MEDIUM (prune-before-add) | Verify `git worktree prune` fires before every `create_worktree` in v2. |
| `59da4a3` | `cleanupThreadWorkspace` internally acquires `withGroupMutex`; outer `withGroupMutex` removed to prevent non-reentrant deadlock. | Every threaded container exit deadlocked the group mutex permanently. | **N/A in v2** — v2 has no `withGroupMutex`; containers are per-session (single writer), cleanup is a background GC (`startWorktreeCleanup`) with `isContainerRunning(sessionId)` guard, not a mutex. Different concurrency model makes this class of deadlock unreachable. | N/A | Already handled by architecture. Note v2 rationale at `src/worktree-cleanup.ts:11-16`. |
| `23b7d6a` | Two bugs: (1) `/compact` session handler passed `undefined` for `threadId`, so new session persisted under group key not thread key; (2) `prepareThreadWorkspace` cpSync'd all 132 thread dirs (2.8GB) into scratch workspace, blocking spawn 3+ min. Fix: pass `threadId` through; skip `threads/` and `conversations/` in cpSync. | `/compact` loss of session isolation; spawn latency. | `/compact` v2 context: container owns session state via `setStoredSessionId` (`poll-loop.ts:199`), per-session DB is the source of truth. **PRESENT** for /compact, equivalent invariant. cpSync: `group-init.ts:172-182` uses cpSync only for per-group scaffold (skills, agent-runner-src), not per-thread copies, so the size blowup is architecturally avoided. | N/A | No action. |
| `2926206` | `fs.cpSync` with `dereference: false` to preserve symlinks (avoid EINVAL on node_modules/.bin circular self-refs). | Blocked all Illysium Slack threads. | v2 `group-init.ts:172,182` uses default cpSync (**no** `dereference: false`). If per-group folders ever contain node_modules/.bin symlinks or self-symlinks, v2 will hit the same EINVAL. | MEDIUM | Add `dereference: false` defensively to both `cpSync` call sites in `src/group-init.ts` **or** confirm group-init targets cannot contain symlinks. |
| `e83c039` | Worktree cleanup rescues non-git, non-symlinked content (plans/, conversations/, new files) back to group folder before rmSync. Previously only git worktree subdirs survived; agent-created dirs silently deleted. Plus opus alias bump to `[1m]`. | Silent deletion of agent work. | v2 doesn't rmSync worktree parents — the worktree **is** a git worktree at `data/v2-sessions/<agId>/<sid>/worktrees/<repo>`; removal uses `git worktree remove --force` (clean path). Agent-created non-git content outside the worktree lives in the session dir and is not cleaned by this GC. **PRESENT by construction** — but verify that anything the agent creates outside the worktree (e.g. plans/ files) survives session end. | MEDIUM | Confirm v2 doesn't rmSync non-worktree session content at any lifecycle point. |
| `2dc0501` | Prune stale git worktrees and retry rather than silent fallback to direct mount (closes concurrency isolation bypass). Idle-flag plumbing. stderr capture on timeout-after-output. | Silent concurrency bypass; idle-marker precision. | v2 `create_worktree` path (`container/agent-runner/src/mcp-tools/git-worktrees.ts`) — verify prune-on-stale-retry behavior present. Idle semantics are v1-specific (poll-loop is different architecture). | HIGH (if absent in v2 create_worktree) | Inspect `git-worktrees.ts` for prune-retry-on-"already exists". |
| `a0fe86e` | `git fetch origin` before `worktree add` to avoid branching off stale state. | Branching from stale commits caused upstream drift. | Check v2 `git-worktrees.ts:144` onwards — description says "Fetches origin, then checks out" — **likely PRESENT**. Verify. | HIGH | Verify in `container/agent-runner/src/mcp-tools/git-worktrees.ts`. |
| `f0bb9ac` | `cleanupThreadWorkspace` wrapped in `withGroupMutex` (TOCTOU race on CLAUDE.md merge-back, worktree add/remove race); `clearThreadState` takes `threadId` parameter so only the finished thread's emoji state is cleared. | Stuck eyes emoji on thread B when thread A finishes; corrupted CLAUDE.md merges. | Mutex aspect: **N/A in v2** (different concurrency model). `clearThreadState` / per-thread emoji state: channel-adapter level concern, adapters live on `channels` branch — out of scope for trunk audit but worth flagging to channel skill maintainers. | N/A (mutex) / MEDIUM (per-thread state) | DM `host-lifecycle` for worktree race; re-audit in channel-branch pass. |
| `7cb5c86` | Worktree error handler matches both 'already registered' and 'already exists' (git says different things for stale meta vs physical dir on disk); prune+remove+retry; cpSync strips symlinks before copy. | Container agent responses blocked. | v2 `git-worktrees.ts` must handle both error strings on `worktree add`. Verify. Symlink cpSync: see `2926206` row. | HIGH | Verify error-string handling in `git-worktrees.ts`. |
| `3a31f9d` | Tighten source mount allowlist — remove `scripts/` and `prompts/` (scripts/ exposes credential topology). | Agent could read credential path layout. | v2 doesn't mount `scripts/` into containers (check `container-runner.ts` mount list). Verify no equivalent leak. | HIGH (security) | Confirm v2 mount list excludes anything under `scripts/` or `prompts/`. DM `container-side`. |
| `871bfa1` | Use in-tree `.claude-global.md` symlink for global CLAUDE.md `@import`. Claude Code `@-import` only follows paths inside project memory tree (cwd + ancestors); absolute path to `/workspace/global/CLAUDE.md` was silently dropped. | Global CLAUDE.md was silently unloaded — agents had no global context. | v2: check whether the global CLAUDE.md is imported via `@` or via explicit system-prompt injection. Commit `3a31f9d` comment says "global CLAUDE.md is now loaded via explicit system prompt injection, not SDK auto-discovery" — **likely PRESENT via different mechanism**. | HIGH | Verify global CLAUDE.md reaches the container (system prompt or symlink). |

### session-lifecycle / cursor / rollback (12 commits spot-checked)

| v1 commit | Behavior | v1 rationale | v2 status | Severity | Port plan |
|---|---|---|---|---|---|
| `07912f7` | Post-result stall detector (60s) forces `stream.end()` for SDK streaming-input hang; pending-pipe map keyed by sessionKey; container emits `pipe_ack` via `writeProgress` on first assistant message; host commits cursor on ack and rolls back via CAS guard on container exit. | Silent container hangs stranded piped messages permanently. | **N/A in v2** — no piping model. Messages sit in `inbound.db` until container reads them; `seq` is the durable cursor in each session DB. Post-result stall detector however is a v2-relevant concern for stuck containers: verify poll-loop has equivalent watchdog. | HIGH (stall detection) | Verify `poll-loop.ts` has stall / watchdog timer after Result. |
| `a9314b5` | `getInFlightChatJids()`; shutdown rolls back `lastAgentTimestamp` for sessions still `processing=1` before `saveState()`; `setSessionProcessing` called BEFORE cursor advance (closes race with `channel.setTyping`); `runAgent.finally` skips `clearSessionProcessing` during shutdown so `recoverPendingMessages` sees the in-flight set on next startup; `messageLoopShuttingDown` flag. Also: SDK model drift detection (`[1m]` → non-`[1m]` force `setModel`). | SIGTERM mid-run advancing cursor past unanswered message; SDK silently downgrading model. | Cursor aspect: **N/A in v2** (seq-based, no cursor rollback needed). Model-drift: verify v2 `claude.ts` re-asserts model after each Result. Grep `setModel` in `container/agent-runner/src/providers/claude.ts`. | HIGH (model drift, if missing) | Inspect `providers/claude.ts` for model-drift force-reset. |
| `fdad2c5` | `saveState()` deferred until container completes; in-memory cursor only during run; eager saveState removed from pipe path. Startup recovery: if cursor points exactly at a user message, nudge back 1ms to re-process. | SIGTERM mid-run permanently advancing cursor past unanswered messages. | **N/A in v2** — seq-based model, no eager save semantics. | N/A | No action. |
| `e0c4249` / `8d01397` | Keep container watchdog alive while SDK progress events flow. | Watchdog killed containers mid-long-tool-use because no writes happened during SDK thought cycles. | v2 heartbeat is `/workspace/.heartbeat` file touch (per CLAUDE.md). Verify agent-runner touches heartbeat during long SDK streams. | HIGH | Inspect `poll-loop.ts` / SDK event handler for heartbeat updates during progress events. |
| `474346e` / `38009be` | Auto-recover from stale Claude Code session: on exit code 1 with ENOENT on `.jsonl` OR "session not found", clear session ID, retry once with fresh session. Transient errors fall to normal backoff. | Infinite retry loop on corrupt/missing transcripts. | **PRESENT** — `container/agent-runner/src/providers/claude.ts:250` defines `STALE_SESSION_RE = /no conversation found\|ENOENT.*\.jsonl\|session.*not found/i` and `poll-loop.ts:199` calls `setStoredSessionId(continuation)`. Confirm clear-on-stale wired. | — | Verify `clearStoredSessionId()` is invoked on `STALE_SESSION_RE.test()` positive. |
| `b59216c` | Persist SDK session ID across container restarts via outbound.db. Seed on startup, clear on `/clear`, recover from stale-session errors. | Every container restart began fresh SDK session despite `.jsonl` transcript still on disk. | **PRESENT** — this commit is in v2 (`fix(v2)` scope). `container/agent-runner/src/db/session-state.ts` implements `setStoredSessionId` / `getStoredSessionId` / `clearStoredSessionId`. | — | Done. |
| `276bad3` | Preserve model override across `/compact` session command. `/compact` handler creates new `query()` and must re-call `setModel()` before iterating messages. | After `/compact`, SDK defaulted to Sonnet instead of configured Opus. | Verify v2 agent-runner `/compact` handler re-invokes `setModel()`. `formatter.ts:13` lists `/compact` as admin command. | HIGH | Inspect how `/compact` is handled in `container/agent-runner/src/providers/claude.ts` — does it re-set model after session reset? |
| `c4c17e7` / `7a9dcab` | `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=80` propagated to per-session settings.json so SDK doesn't default to ~97% (967K tokens on opus[1m]). | Uncontrolled compaction at token boundary caused context rot. | Verify v2 plumbs `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE` through `container-runner.ts` `requiredEnv` into the container's settings. | HIGH | Grep `CLAUDE_AUTOCOMPACT` in v2 src + container. |
| `561b972` / `03d7e3b` / `b5dc958` | Trim stale pre-anchor messages on thread-enabled recovery; notify users in threads on restart that their message was dropped; ultimately disable thread auto-recovery because Discord bot messages aren't stored in DB → can't detect prior response → duplicate responses. | Recovery replayed unrelated thread messages / double-processed. | **N/A in v2** — inbound.db is per session and durable; container reads from `seq` cursor; no cross-thread contamination possible. Notification-on-restart-drop not needed because messages are not dropped. | N/A | No action; document in architecture doc. |
| `c05d886` | Pre-create thread session projects dir as `ubuntu` UID inside the deeply-nested overlay mount so SDK transcript writes don't ENOENT. | Slack threads stuck in indefinite retry with "Query closed before response received". | v2: session dir is created by host `session-manager.ts` before container spawn — verify ownership matches container UID. Check mount permissions for `.claude` dirs. | HIGH | Inspect v2 session dir creation in `session-manager.ts` / `container-runner.ts`. |
| `44f0b3d` | Defense-in-depth sanitization and error callback to `exec()` in shutdown. | Shutdown hook could throw unhandled on bad args. | v2 shutdown is simpler (`src/index.ts:226-242`) — no `exec()` in shutdown path. N/A. | N/A | — |

### approval / gate (6 commits spot-checked)

| v1 commit | Behavior | v1 rationale | v2 status | Severity | Port plan |
|---|---|---|---|---|---|
| `b38a045` | Gate timeouts increased: container poll 5→30 min (match IDLE_TIMEOUT), host TTL 10→60 min (headroom above container). | Legitimate 20-min approvals timing out silently. | v2 approval timeouts — inspect `src/modules/approvals/` and `container-runner.ts` for poll/TTL values. | HIGH | Verify v2 has ≥30 min container poll and ≥60 min host TTL on approval flows. |
| `ed5dc5e` | Chat-SDK bridge projects `author` into flat sender fields (`content.senderId`, `sender`, `senderName`) required by `router.ts:extractAndUpsertUser`. | Every chat-sdk adapter (telegram, discord, slack, teams, gchat, webex, matrix, resend, imessage, whatsapp-cloud) hit strict gate with userId=null and got dropped for registered owner. | **PRESENT** — this is a `fix(v2/chat-sdk)` commit. Already in v2. | — | Done. |
| `360841a` | Remove dead boolean check on void return in `dispatchGateNotification`. | Duplicate approval loops (interactive gate + text fallback both fired). | **PRESENT** — `fix` with v2-era commit (CLAUDE.md touched implies recent). Verify `src/ipc.ts` / approvals dispatch doesn't double-fire. | — | Verify in v2 approvals dispatch. |
| `2df81e0` | Store title + normalized options on `pending_approvals`; shared `getAskQuestionRender` helper for chat-sdk post-click edit and Discord interaction callback. Both render per-card title and `selectedLabel`. | Post-click card showed "❓ Question" + raw "approve" instead of title + "✅ Approved". | **PRESENT** — `fix(v2/approvals)`. Already in v2. | — | Done. |
| `1d6ffeb` | Email gate extracts flags from `gws` segment; cleanup uses `path.resolve`. | Email-consent gate misparsed paths. | Email gate is Gmail-channel-specific, lives on `channels` branch — audit there. | MEDIUM | DM `cross-cutting` if gmail install is live in v2. |
| `e503b4a` | Review findings: auth check, cleanup guard, `sendFile` re-throw, gate bypass. | Umbrella safety commit. | `sendFile` re-throw: **REGRESSED in v2** — v2 `send_file` (`container/agent-runner/src/mcp-tools/core.ts:134-178`) is fire-and-forget (writes `messages_out`, returns success immediately). No host-side ack, so adapter upload failures (Slack missing_scope, size limit, etc.) are invisible to the agent. This matches the **pre-fix** state that `8df09eb` corrected in v1. | HIGH | Implement query-response pattern for `send_file` in v2 (host writes upload result back via `inbound.db` or seq ack) **or** document as accepted degradation. |

### credentials / OneCLI (8 commits spot-checked)

| v1 commit | Behavior | v1 rationale | v2 status | Severity | Port plan |
|---|---|---|---|---|---|
| `ff24bd9` / `4e6c12b` | Forbid agents from asking users for credentials in chat. Hard rule in global CLAUDE.md. | Agents were telling users to paste API keys in Slack. | v2 global CLAUDE.md — verify rule present. `groups/global/CLAUDE.md` is not in trunk; this is per-install. | HIGH | Include equivalent guardrail in v2's bootstrap global CLAUDE.md template (check `scripts/init-first-agent.ts`). |
| `1ca6de7` | Restore secrets injection into `sdkEnv` lost during auto-merge. Named pattern: upstream deleted a function, git auto-merged the one-sided deletion, we lost it silently. | SDK had no OAuth token; every container returned "Not logged in". | **N/A architecturally** — v2 uses OneCLI credential proxy; secrets flow through HTTP, not `sdkEnv`. But the **auto-merge-silent-delete** pattern is itself the warning: saved to memory as `feedback_upstream_merge_additive.md`. | N/A | Memory already captures this; no code change. |
| `e6737b7` | OAuth token exchanged for temp API key via `POST /api/oauth/claude_cli/create_api_key`, then x-api-key used for direct Anthropic API calls (thread title, topic classifier, search reranker). | Raw OAuth bearer returns 401 on /v1/messages. | v2 doesn't appear to make direct Anthropic API calls (`grep` returns no matches in container for `sdkEnv`/`OAUTH_TOKEN`). Thread-title and classifier calls — if done in v2, must go through the same flow or claude CLI subprocess. | MEDIUM | Grep v2 for direct `api.anthropic.com/v1/messages` calls; if any, confirm OAuth token exchange path. |
| `ce2984f` | Route Haiku calls through `claude -p --model haiku` subprocess instead of raw fetch() for OAuth compatibility. | OAuth doesn't support Messages API. | Same as above. In v2 `llm.ts` exists — check whether it uses SDK / subprocess / direct fetch. | MEDIUM | Inspect `src/llm.ts`. |
| `0b2bb2e` | Restore `GITHUB_TOKEN` env var + scoped variants (`GITHUB_TOKEN_ILLYSIUM`) in container. OneCLI handles `api.github.com` HTTP, but `gh` CLI and git credential helper need env var. | `gh auth login` / `git credential` fail without env var. | v2 container env: check `container-runner.ts` secrets injection path; OneCLI credential proxy + env for CLI auth. | HIGH | Inspect `container-runner.ts` for `GITHUB_TOKEN` in required/optional env. |
| `c0b58bd` | Require `CREDENTIAL_PROXY_HOST` for Apple Container networking. | Apple Container's different network model broke proxy discovery. | v2 runtime switch (`container-runtime.ts`) — verify Apple Container path sets the proxy host. | HIGH | Inspect `container-runtime.ts`. |
| `2583af7` | Ensure OneCLI agents exist for all groups on startup. | Groups registered after OneCLI init had no agent and no credentials. | v2 `container-runner.ts` calls `ensureAgent()` (per CLAUDE.md). Confirm fires on startup for all agent_groups. | HIGH | Verify startup sweep in `src/index.ts`. |
| `a8266d8` | Proactive Granola OAuth refresh every 4h to prevent token chain expiry during idle. | Overnight token expiry silently broke MCP tools. | Granola is an integration on the `channels` branch / OneCLI-managed. Out of scope for trunk. | LOW | DM skill maintainer if granola skill ever wired. |

### delivery / pipe / typing (10 commits spot-checked)

| v1 commit | Behavior | v1 rationale | v2 status | Severity | Port plan |
|---|---|---|---|---|---|
| `ea4f522` | Typing cleanup consolidated in finally block with error flag (❌ vs ✅). Acked pipes rolled back on container crash (treat all pipes as stranded when containerErrored=true since ack means "received" not "responded"). | Piped messages got ✅ with no response when containers crashed 529/400. | **N/A** — no piping model in v2. Crash-vs-response distinction still matters: when container exits without writing `messages_out`, host must signal error. | MEDIUM | Verify host-sweep / delivery handles container-crash-without-output case. |
| `5008a00` | `setTyping(false)` on first output (not just container exit), since sessions persist containers between queries. | Typing indicator ran indefinitely after agent response. | v2 `src/modules/typing/index.ts` — verify typing stops on first `messages_out` write, not on container exit. | HIGH | Inspect typing module. |
| `8e617cc` / `ab71ac2` / `498f2d0` | Guard checks in sendTyping interval closure — verify JID still in active typingIntervals map before calling Discord API. Prevents race where in-flight sendTyping completes after setTyping(false). | ~10s ghost typing events post-response. | Channel-adapter concern (Discord) — on `channels` branch. DM adapter maintainer. | MEDIUM | Out of scope for trunk. |
| `40492e0` | Fallback message when container exits without output. | Container completes without text, typing stops, user sees nothing. | v2 `src/delivery.ts` + `host-sweep.ts` handle post-run state. Verify fallback when `messages_out` has no content for a wake cycle. | HIGH | Inspect host-sweep stale detection. |
| `ca5d7d8` | Preempt idle containers for queued messages on same threadKey; reset idle timer on piped input. | Dropped messages when idle container blocked queued ones with same thread key. | **N/A** — v2 has no group-queue / idle container model; per-session containers. | N/A | — |
| `d656b5c` | Chat-SDK bridge delivery + typing for non-Discord adapters: use `platformId` directly as thread ID (already encoded), not `encodeThreadId(discord-args)`. | Non-Discord chat-sdk adapters had broken delivery/typing. | v2 `src/channels/chat-sdk-bridge.ts` — verify this shape. | HIGH | Grep chat-sdk bridge for `encodeThreadId` usage. |
| `2186208` | Add `senderPn` fallback for WhatsApp LID JID translation; log unmatched. | LID JIDs couldn't resolve back to phone-number-based user rows. | WhatsApp adapter on `channels` branch. | — | Out of scope. |
| `d30ac15` | Content-hash dedup in agent-runner send_file — prevent sending identical screenshots when browser hasn't re-rendered. | Duplicate file delivery. | v2 `send_file` (core.ts) has **no content-hash dedup**. Low-severity miss — visible as duplicate files, no data loss. | LOW | Optional: add hash dedup. |
| `5a309a0` | Only send onboarding message on first wiring, not re-registration. | Duplicate welcome DMs on `/manage-channels` rewire. | v2 `/init-first-agent` / `/manage-channels` flow — verify first-wiring-only check. | MEDIUM | Inspect `scripts/init-first-agent.ts` / wiring logic. |
| `63746df` | Allow agent self-messages without destination row. | Approval follow-up prompts (agent→self) dropped by agent-to-agent auth check. | **PRESENT** — `fix(v2/delivery)` commit, in v2 `src/delivery.ts`. | — | Done. |

### container-spawn / mount / build (6 commits spot-checked)

| v1 commit | Behavior | v1 rationale | v2 status | Severity | Port plan |
|---|---|---|---|---|---|
| `d4aacfe` | Clear per-group `agent-runner-src` before cpSync — cpSync doesn't remove files that disappeared from source. Stale renamed/deleted files fail tsc. | Container exits 2 on compile because of one leftover file. | v2 `src/group-init.ts:172-182` — verify pre-copy cleanup of `agent-runner-src` dir. | HIGH | Inspect `group-init.ts` — does cpSync clear first? |
| `3a31f9d` | Tighten source-mount allowlist; remove `scripts/` (credential topology leak). | Agent could enumerate credential paths. | Already flagged in worktree cluster. Verify v2 mount list. | HIGH (security) | Inspect `container-runner.ts` mount construction. |
| `d2bde16` | Snowflake credential scoping `snowflake:<connection>` — stage `connections.toml` + keys into single dir; filter sections; copy only referenced keys. Avoids readonly parent/sub-mount crash. | Cross-project credential leak; container crash. | v2 Snowflake scoping — check `container-runner.ts` or providers. Likely on-install via OneCLI. | HIGH | Inspect if v2 has equivalent filter for Snowflake (and similar scoped creds). |
| `2a720c9` | Calendar OAuth keys mounted without Gmail tokens (separate GCP key mount). | Calendar-only groups got full Gmail credentials. | Channels-branch concern. | MEDIUM | Out of scope for trunk. |
| `7a9dcab` | `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=80` in container settings.json. | Covered in session-lifecycle. | See session-lifecycle row. | — | — |
| `fdda8ea` | `register_group` IPC handler merged containerConfig instead of overwriting with undefined. `buildAllowedTools` removed SDK-builtin allowlist (redundant with bypassPermissions, silently blocked new SDK tools). | `register_group` wiped container config; new SDK tools invisibly denied. | v2 register-group equivalent is `src/modules/channel-auto-wire/` or DB update paths. Tool allowlist: verify v2 doesn't allowlist SDK built-ins. | MEDIUM | Inspect v2 config-update paths + tool allowlist. |

### ipc / mcp / git tools (4 commits spot-checked)

| v1 commit | Behavior | v1 rationale | v2 status | Severity | Port plan |
|---|---|---|---|---|---|
| `f9f8288` | Remove org restriction from `clone_repo` IPC handler; keep GitHub URL validation + path-traversal guards. | Blocked legitimate cross-org / OSS repo clones. | Verify v2 `clone_repo` MCP tool in `container/agent-runner/src/mcp-tools/git-worktrees.ts` has no org-lock, retains traversal guard. | MEDIUM | Inspect tool. |
| `8df09eb` | `send_file` query-response IPC pattern: container writes query, waits up to 30s for host confirmation; host attempts upload, writes success/error response. Agent learns actual error (e.g. `missing_scope`). Slack `sendFile` re-throws after logging. | Fire-and-forget `send_file` reported success even when upload failed. | **REGRESSED in v2** — see approval cluster row `e503b4a`. v2 `send_file` is fire-and-forget (`core.ts:134-178`). | HIGH | Add ack path to `send_file`. |
| `c18fb49` / `54462ba` | Strip `<thread-title>` from IPC send_message path + all outbound messages via `stripInternalTags`. System prompt only includes the tag in first message of new conversation. | Tag leaked into visible user-facing output. | v2 `text-styles.ts` / formatter handles stripping. Verify `<thread-title>` specifically. | MEDIUM | Grep v2 for `thread-title` tag handling. |
| `4ab204a` | Route IPC `send_message` to existing thread (scan `createdThreadJid` for partial key match) instead of parent channel when `triggerMessageId` absent. | MCP `send_message` landed in parent channel on follow-ups. | Channel-adapter concern on `channels` branch. But: v2 `send_message` routing via `platform_id` + `thread_id` in `messages_out` is architecturally cleaner — the v1 bug pattern doesn't recur. Likely **N/A**. | — | Spot-check on channels-branch review. |

### db / schema / migration (3 commits spot-checked)

| v1 commit | Behavior | v1 rationale | v2 status | Severity | Port plan |
|---|---|---|---|---|---|
| `20d9ee8` | Consolidate `sessions_v2` → `sessions` atomically in `initDatabase()`; DROP old; RENAME; accessor functions as thin wrappers. `getAllSessions()` filtered to `thread_id IS NULL`. | Upstream cleanup script queried stale `sessions` table (11 rows) not `sessions_v2` (206 active rows). | v2 has only `sessions` (`src/db/sessions.ts`), per-session design. N/A. | N/A | — |
| `062b0cb` | `updated_at` column added to `session_state` on older DBs. | Migration safety for pre-existing installs. | v2 migrations system in `src/db/migrations/` — per CLAUDE.md. Structurally equivalent pattern. | — | — |
| `e4d3e02` | Wire db-recovery refactor + web-ui deps into `index.ts`. | Integration commit. | Covered by other rows. | — | — |

### router / trigger / sender (3 commits spot-checked)

| v1 commit | Behavior | v1 rationale | v2 status | Severity | Port plan |
|---|---|---|---|---|---|
| `ddb2787` | Allow allowlisted senders to use session commands in Discord/Slack. | Only DM owner could `/compact` etc. | v2 permissions at `src/modules/permissions/` — verify session-command ACL matches. | MEDIUM | Inspect permissions module. |
| `ed5dc5e` | Already covered (approval cluster). | — | PRESENT | — | — |
| `616c1ae` | Expand fork-sync auto-resolve patterns; add missing forks to dispatch. | CI automation. | Repo-specific CI. N/A to behavior. | N/A | — |

## Clusters skipped (trivial / out-of-scope)

- **deps / bumps / prettier / lint / formatting (40)** — dependency updates, autoformatter churn, log-level changes; no behavior.
- **typo / comment (~20 of the above count)** — docstring/comment edits.
- **channels (99)** — WhatsApp, Discord, Slack, Telegram, Signal, Gmail, Teams, Matrix, Webex, Resend, Linear, GitHub, iMessage, Google Chat, WhatsApp Cloud. Channel adapters live on v2's `channels` branch (skill-installed). A separate audit pass against that branch is needed but is outside Category 5 trunk scope.

## Gaps summary (count by severity)

| Severity | Count | Key gaps |
|---|---|---|
| CRITICAL | 0 net new | Auto-commit already fixed in `eb90165` (row 1 of main audit). |
| HIGH | ~15 | `send_file` regression to fire-and-forget (no error ack); credential guardrail in global CLAUDE.md; GITHUB_TOKEN env injection; gate timeouts; `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE` plumbing; model-drift re-set; watchdog heartbeat during SDK progress; stale-session `clearStoredSessionId` wiring; fetch-before-worktree-add; prune-on-worktree-"already exists"; global CLAUDE.md reaches container; source-mount allowlist security; `agent-runner-src` pre-copy clear; post-result stall detector; typing stop on first output. |
| MEDIUM | ~12 | cpSync `dereference: false`; non-worktree session content preservation on cleanup; `/compact` model re-set; `register_group` config merge vs overwrite; session-command ACL; email gate path parsing; Snowflake credential scoping; first-wiring-only welcome; content-hash dedup for send_file; `<thread-title>` tag stripping; clone_repo traversal guard; send_file dedup. |
| LOW | ~5 | Cosmetic / channel-branch deferrals. |
| N/A | ~20 | V2 architecture makes the invariant unreachable (seq-based cursor model eliminates cursor-rollback, stranded-pipe, thread-recovery-contamination, etc.). |

## Cross-agent flags

- DM `host-lifecycle`: worktree mutex race (`f0bb9ac`) — v2's per-session concurrency model should be sanity-checked against simultaneous cleanup+create; also SIGTERM-in-flight behavior (`a9314b5` cursor rollback equivalent: what happens to in-flight container on host shutdown?).
- DM `container-side`: stale-session `clearStoredSessionId` wiring (`474346e` / `38009be`); `/compact` model re-set (`276bad3`); model-drift detector (`a9314b5`); `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE` plumbing (`7a9dcab`); heartbeat during SDK progress (`e0c4249`); post-result stall detector (`07912f7` bug #3); `send_file` ack pattern (`8df09eb`); content-hash dedup (`d30ac15`); `<thread-title>` stripping (`c18fb49`/`54462ba`); source-mount allowlist (`3a31f9d`).
- DM `cross-cutting`: credential-in-chat guardrail in global CLAUDE.md bootstrap (`ff24bd9`/`4e6c12b`); `GITHUB_TOKEN` env injection (`0b2bb2e`); `CREDENTIAL_PROXY_HOST` on Apple Container (`c0b58bd`); gate timeouts ≥30min/60min (`b38a045`); Snowflake credential scoping (`d2bde16`); ensureAgent-for-all-groups-on-startup (`2583af7`); agent-runner-src pre-copy clear (`d4aacfe`).
- DM `revalidate-prior`: worktree fetch-before-add (`a0fe86e`), prune-on-"already exists" retry (`7cb5c86`), org-restriction-removed in clone_repo (`f9f8288`) — these belong to the Phase 2.11 re-check surface.
