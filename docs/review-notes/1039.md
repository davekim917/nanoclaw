- 2026-09-22 · #1039 · over-broad guard · A fail-closed check on the frozen pair record (added to close a real hole, where `{}` plus a hand-written ok receipt read as ready) demanded a field, `expectedSourceSha`, that one WRITER PATH does not write. It was written against the path in front of the author, and two closing reviews in a row each found another legitimate path refused on every fire, publishing BLOCKED over a clean GO (the class the PR exists to close). First, `refreeze` rebuilt the record and dropped the field. Then `start` run before the PR contract exists (a supported order) never writes it. A third path neither review reported (start, refreeze, then contract) fell out of the same matrix. Separately, the first regression for this was vacuous: its contract listed no required lane, so the barrier exited in its prelude and "identity.json was not refused" passed without the identity block ever running. Before tightening a validator, enumerate every writer × every order of the inputs it depends on, and make every negative assertion prove the code under test was reached · the barrier no longer requires `expectedSourceSha` (its absence is never a refusal; both frozen commits equalling the contract's `sourceSha` is what binds a PR run, and a present field naming another build is refused), and a wrong binding names `refreeze` as its repair (`container/skills/smoke-test/scripts/smoke-evidence-barrier.sh`); `refreeze` also keeps the field (`container/skills/smoke-test/scripts/smoke-pair-identity.sh`); `container/skills/smoke-test/scripts/smoke-pair-identity.test.sh` section 8 drives every writer × contract order × served pair through the real verbs, each accept guarded by `has("phase")` and the record on disk, and was mutation-tested: it fails against the pre-fix barrier (8b) and with the binding check removed (8f)
