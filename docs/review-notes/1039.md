- 2026-09-22 · #1039 · over-broad guard · A fail-closed shape check on the frozen pair record (added to close a real hole: `{}` plus a hand-written ok receipt read as ready) was written against what `start` produces and never against the OTHER writer of the same file: `refreeze` rebuilds identity.json from the live read and carried over only `history`, so it dropped the `expectedSourceSha` the new check requires. Every PR run taking the sanctioned drift recovery (start, ok, redeploy, drift, refreeze, ok) was refused on every fire with no repair path — publishing BLOCKED over a clean GO, the very class the PR exists to close, introduced by its own round-3 fix and missed by three Codex rounds; a fresh-context full-head review found it. Before tightening a validator, enumerate every writer of the artifact it validates, not just the one in front of you · `refreeze` now writes `expectedSourceSha` exactly as `start` does (`container/skills/smoke-test/scripts/smoke-pair-identity.sh`), pinned by `container/skills/smoke-test/scripts/smoke-pair-identity.test.sh` section 8, which drives start → drift → refreeze → ok through the real verbs on a pr contract and asserts the barrier accepts the re-frozen record; verified to FAIL without the fix
