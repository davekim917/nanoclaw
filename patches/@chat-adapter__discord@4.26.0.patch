diff --git a/dist/index.js b/dist/index.js
index 8b490cf67e4d4dcec82404a27ef364f440c617b6..a88e85d48b293fc3c816da66f6e5ffbf936d1c2b 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1658,11 +1658,10 @@ var DiscordAdapter = class _DiscordAdapter {
         this.logger.debug("Ignoring message - Gateway is shutting down");
         return;
       }
-      if (message.author.bot) {
-        this.logger.debug("Ignoring message from bot", {
+      if (message.author.id === client.user?.id) {
+        this.logger.debug("Ignoring self-echo", {
           authorId: message.author.id,
-          authorName: message.author.username,
-          isMe: message.author.id === client.user?.id
+          authorName: message.author.username
         });
         return;
       }
@@ -1687,11 +1686,8 @@ var DiscordAdapter = class _DiscordAdapter {
         this.logger.debug("Ignoring reaction - Gateway is shutting down");
         return;
       }
-      if (user.bot) {
-        this.logger.debug("Ignoring reaction from bot", {
-          userId: user.id,
-          isMe: user.id === client.user?.id
-        });
+      if (user.id === client.user?.id) {
+        this.logger.debug("Ignoring self-reaction", { userId: user.id });
         return;
       }
       this.logger.info("Discord Gateway reaction added", {
@@ -1715,11 +1711,8 @@ var DiscordAdapter = class _DiscordAdapter {
         );
         return;
       }
-      if (user.bot) {
-        this.logger.debug("Ignoring reaction removal from bot", {
-          userId: user.id,
-          isMe: user.id === client.user?.id
-        });
+      if (user.id === client.user?.id) {
+        this.logger.debug("Ignoring self-reaction removal", { userId: user.id });
         return;
       }
       this.logger.info("Discord Gateway reaction removed", {
