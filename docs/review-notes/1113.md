- 2026-09-24 · #1113 · pattern audit · Remapping the controller's shifted citations covered the qualified `smoke-campaign-controller.py:N` form and missed a bare `:N` range on the next line of the same sentence, which cites the same file; Codex caught the one left in the renewer's comment. When line numbers move, every cite whose referent is the moved file needs remapping, including bare ones that inherit the file from an earlier qualified cite in the sentence, and a scan must list those before the commit, not after review · the remap now treats a bare cite as the controller's when the nearest earlier qualified cite in the sentence names it, and one scan over `container/skills/smoke-test` lists every such cite (`container/skills/smoke-test/scripts/smoke-controller-renew.sh`, `container/skills/smoke-test/SKILL.md`, `container/skills/smoke-test/references/controller-owner-router.md`); the router's cites stay pinned by `container/skills/smoke-test/scripts/smoke-campaign-controller-live.test.sh`
- 2026-09-24 · #1113 · stale state · The issue body is written once per payload name and a replay keeps it, so a body that gained the dropped-labels note on a later attempt, after a label listing that failed and then recovered, never reached the issue: the first attempt's body was reused · the payload name carries a digest of the note whenever labels are dropped, so a changed body gets its own file, and the marker search still makes the create land at most once (`container/skills/smoke-test/scripts/smoke-campaign-controller.py`, `_gh`); pinned by `container/skills/smoke-test/scripts/smoke-controller-gh-issues.test.py` (test_a_recovered_listing_still_puts_the_dropped_note_in_the_filed_body), which fails with the digest removed
- 2026-09-24 · #1113 · uncited behavioural claim · The label check exempted `smoke-finding` on the belief that every target repo defines it, and nothing in the repo creates or checks that label, so a repo without it would wedge the create exactly as `P3` did. An exemption inside a validation is itself a claim about the outside world and needs the same evidence as any other; here the evidence was absent, so the label gets no exemption (its effect on dedup is the next line) · `resolve_labels` drops a missing `smoke-finding` like any other label and notes it in the body (`container/skills/smoke-test/scripts/smoke-campaign-controller.py`); pinned by `container/skills/smoke-test/scripts/smoke-controller-gh-issues.test.py` (test_smoke_finding_is_dropped_like_any_label_the_repo_lacks, test_a_repo_without_smoke_finding_still_gets_its_issue)
- 2026-09-24 · #1113 · inherited claim · The round-2 reply said dedup still works without `smoke-finding` because the marker search bounds the create, which was true only of one run's retries: `obligation_key` carries the runId, so the marker never sees an earlier campaign's issue, and the title dedup that does see it listed only `--label smoke-finding`, which an unlabelled issue never matches. A guarantee quoted to justify dropping a safeguard needs the scope it holds over checked against the scope the safeguard covered; here one was per-run and the other cross-run · when the repo lacks `smoke-finding` the title dedup lists its open issues unlabelled, and a repo that has it keeps the scoped query (`container/skills/smoke-test/scripts/smoke-campaign-controller.py`, `_gh`); the gh fake now honours `--label` on `issue list` (`container/skills/smoke-test/scripts/testdata/controller-live-fakes.py`), and `container/skills/smoke-test/scripts/smoke-controller-gh-issues.test.py` pins both (test_a_repo_without_smoke_finding_dedups_across_runs_unlabelled, which fails with the unlabelled query removed, and test_a_repo_with_smoke_finding_keeps_dedup_scoped_to_it)
