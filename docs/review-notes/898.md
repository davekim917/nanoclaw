- 2026-09-17 · #898 · fail-open default · A "full/unknown" fallback that names nothing is an off switch, not a safe default: producers of `selection:"full"` (crashed matcher, unreadable or invalid catalogue, floor history with no run root, a due native floor journey under an unknown range) each returned a well-shaped empty or partial result that was pinned immutably or read as "nothing due", and each was patched one review round at a time until the rule was stated once — an unusable input selects NOTHING and is never pinned, unknown history makes everything due; the enforcing side had the same default one layer down, and patching the LISTING (compgen, then find) drew a finding every round — EACCES, a vanished mount, a symlinked dir not followed, a search-only dir refusing a healthy run — until enumeration was removed: the pin is looked up by the campaign's identity (head from the contract, PR and repoSlug from the gate-authored lease, read by exact name) and probed with errno-preserving lstat/open, where only a confirmed ENOENT under a reachable parent is "absent" · `selection_problem` inside `check_pin` (`container/skills/smoke-test/scripts/smoke-journeys.py`) is the one pinnability rule, called by the gate at candidate acceptance and promotion; pin discovery is `campaign_identity` → `locate_owner` → `resolve_owner` in `container/skills/smoke-test/scripts/smoke-journeys.py` (`_probe_path`: absent | file | unavailable; unavailable refuses a pr-owned run, an incomplete identity is refused only where a catalogue exists), fed by `repoSlug` that `lease_acquire` records in `container/skills/smoke-test/scripts/smoke-pr-gate.sh`; tests 5k-empty-full-refused, 5k-invalid-catalogue in `container/skills/smoke-test/scripts/smoke-pr-gate.test.sh`, 3-missing-root, 5c-native-floor-*, 5b-lease-dir-* in `container/skills/smoke-test/scripts/smoke-journeys.test.sh`
- 2026-09-17 · #898 · enforcement scope · A gate that pins only on its automatic path leaves its manual path unpinned: `check` computes obligations and `claim` took ownership without them, so check-then-claim admitted a freeze with both pin paths absent and the barrier read that as legacy "no pin" — the invariant belongs at admission, one function (`pin_freeze_head`) that `poll` and `claim` both run before ownership — and what a head IS must be read off the immutable head (the freeze commit's own files and parent, `freeze_head_probe`), never off the PR's current file list, which describes whichever head the PR moved on to; likewise WHO a run is must travel in a record with the run's own lifetime — the fenced completion contract (schemaVersion 2: `pr`, `repoSlug`, `sourceSha`), never a directory listing, a lease finish deletes, or a local catalogue a second coordinator lacks, each of which read as "no pin" one round apart. Recovery authored by the party being checked is not a check. "Invalid is not absent" held on the producing side (the gate) and was reversed on the ENFORCING side three ways in a row: the barrier read an invalid pin as no pin; then accepted a run-written "rebuilt" selection, which the run could thin, mislabel or build from a smaller catalogue; and all along judged pins with a looser predicate than the gate, so shape-only bytes the gate refused were adopted by the run. Cadence had the same split — a pass the barrier refuses still reset the floor clock — and a checker that merely failed to run was read as "invalid", which licensed a recovery pin and let the owning pin flip once the checker ran again. Recorded as the trust boundary, not built around: a coordinator rewriting files it authored (its contract, a pin in a dir it writes leases into) is the harness's existing boundary; the challenger is the independence · the run never authors what it is held to: `check_pin` is the single pin predicate for gate, `pin-run` and barrier; recovery is a second gate-promoted immutable pin (`journeys_pin_promote … recovery`, `container/skills/smoke-test/scripts/smoke-pr-gate.sh`); `resolve_owner` (recovery-first, unavailable ≠ invalid) and `lane_problems`+`pass_identity_problems` are shared by the barrier and `last_proven`; `pin_freeze_head` in `container/skills/smoke-test/scripts/smoke-pr-gate.sh` is the one admission step for `poll` and `claim`; the pass-evidence grammar is held once in `evidence_entry_problems`/`finding_clip_problems` (`container/skills/smoke-test/scripts/smoke-journeys.py`, relaxing only file existence for cadence) and pinned to the bash barrier by the parity table 5e in `container/skills/smoke-test/scripts/smoke-journeys.test.sh` — the bash-vs-Python rule drifted three rounds running until a test compared the two on every row; tests 5l-* in `container/skills/smoke-test/scripts/smoke-pr-gate.test.sh`, 5b-*-hand-rebuilt-refused, 5b-*-owner-is-recovery, 5b-unavailable-*, 5b-check-*, 3-mislabelled-api-pass-no-reset, 3-empty-evidence-no-reset in `container/skills/smoke-test/scripts/smoke-journeys.test.sh`, 5k-invalid-*, 5k-both-invalid in `container/skills/smoke-test/scripts/smoke-pr-gate.test.sh`
- 2026-09-17 · #898 · evidence keying · The barrier accepted a run whose selection equalled ANY journeys pin for its head sha, so two campaigns on one commit in the shared lease dir could trade scopes; identity is repo + PR + head, with the PR read from the shared lease, never from the run's own copy, and the pin's contents must agree with its own file name · `container/skills/smoke-test/scripts/smoke-evidence-barrier.sh` resolves exactly one owning pin and refuses an ambiguous repo; tests 5b-sibling-pr-*, 5b-ambiguous-repo, 5b-check-renamed in `container/skills/smoke-test/scripts/smoke-journeys.test.sh`
