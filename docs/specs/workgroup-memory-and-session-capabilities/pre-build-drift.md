# Drift Report: Design → Plan

> Generated by `/team-drift` — 2026-07-25
> Source-of-truth: `docs/specs/workgroup-memory-and-session-capabilities/design.md`
> Target: `docs/specs/workgroup-memory-and-session-capabilities/plan.md`
> Agents: two isolated same-runtime passes; cross-model diversity reduced

## Summary

| Class | Count | Blocking |
|---|---:|---|
| MISSING | 0 | Yes |
| DIVERGED | 2 | Yes; both acknowledged |
| PARTIAL | 6 | Carried into build validation |
| CONFIRMED | 171 | — |
| **Total claims** | **179** | |

The lead reconciled the differently granular extractions conservatively.
Absence confirms a REJECTION claim. The named `ensureMemoryScaffold()` contract
is confirmed structurally: its file is outside all 48 exclusive ownership rows,
so no builder may change it, while A1 routes eligible seeding through canon.

## Blocking Mismatches

### [B1] Substantive stores require operator migration

- **Claim:** Startup/group-init/spawn collision-import a pre-existing real tree
  before linking (`design.md:132-135`).
- **Class:** DIVERGED
- **Target:** A1 leaves substantive trees untouched and A2 alone may snapshot,
  import, and cut them over (`plan.md:115-148,193-220`).
- **Status:** Acknowledged. Reverting would weaken permanent-snapshot and
  customization-preservation guarantees.

### [B2] Preview inventory may precede quiescence

- **Claim:** No migration snapshot or authoritative hash occurs until strict
  install-container absence is proved (`design.md:208-216`).
- **Class:** DIVERGED
- **Target:** Read-only `inventory` may produce a preview report while live;
  `apply` strictly proves absence, creates/verifies the permanent snapshot, and
  rehashes before any cutover (`plan.md:193-220,252-259`).
- **Status:** Acknowledged. Preview bytes authorize no mutation; a changed byte
  makes `apply` abort, while requiring shutdown for a read-only preview would
  make safe inventory unnecessarily disruptive.

## Partial Matches

These are mandatory build-validation risks, not scope additions:

1. B1 retrieval must use headings as well as sorted paths/content.
2. A2 must snapshot/report opaque provider sources without activating them as
   another authority.
3. B1 scan-budget measurement must cover cold and warm incident turns.
4. If scan budget fails, optimization remains in-process with direct source
   verification and degraded notices; no persistent authority.
5. B1 corpus tuning covers tokenization, lexical score, source preference, and
   bounds.
6. C4 provider evaluation must score recalled instructions as evidence-only
   even when no tool/action attempt appears.

## Confirmed Claims

The other 171 claims confirm the one canon, source precedence, bounded paired
context, owner-safe capabilities, provider parity, safe-write helper, reversible
migration, permanent rollback, evaluation gates, and rejected
coordinator/schema/daemon/semantic-store designs.

## Extraction Disagreements

- Agent B found only B1 and no missing/partial claims.
- Agent A additionally raised B2 and six partials. The lead retained the more
  conservative classification.
- Agent A called `ensureMemoryScaffold()` missing. The lead confirmed it from
  the ownership map: its source file is deliberately unowned and therefore
  immutable during this build.

## Gate

- [x] All MISSING mismatches resolved
- [x] All DIVERGED mismatches acknowledged in `drift-acks.json`
- [x] All PARTIAL matches logged as build-validation risks

**Gate result: CLEAR. Effective DIVERGED = 0.**
