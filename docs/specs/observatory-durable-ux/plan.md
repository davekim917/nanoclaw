# Durable Observatory work UX

Restore the reviewed work-first Observatory interface on top of current published main, preserving subsequent data-validation, review-history, permissions, pagination and async database fixes. Publish the complete change to the deployment source before activating it. A local-only artifact is not completion.

## Acceptance

- Decisions lead with a focused queue and selected question, context, evidence and explicit action. Changed release blockers stay in the leading group.
- Agent pages lead with bounded reported work and blockers. Conversation links are secondary.
- Exact thread routes open a work brief before optional transcript access. Instruction retries retain immutable text and recipient across navigation.
- The configured workspace's Saturday develop-to-main release calls are prominent. The panel only appears when that workspace is included by the authenticated API; source flags are not represented as fresh release-readiness verification.
- Overview requests never trigger full schedule-history assembly. Missing fresh schedule data is explicit.
- Preserve all current source validation, per-user scope checks, review version/evidence checks, draft safety, and pagination. No invented completion claims.
- Existing and restored regression tests pass; actual desktop and mobile browser workflows render without overflow. Production GET-only checks prove restricted workspace isolation.
- Final source is reachable from remote main, deployed build provenance identifies that published revision, and public asset hashes match the build.

No changes to brand direction: retain flat charcoal, ivory and amber; no purple or gradients. No application actions are submitted during live verification.
