- 2026-09-20 · #949 · enforcement scope · The inverse of a declare helper is not the helper with the sign flipped when the read side merges several stores. `ensureSecretUndeclared` edited the one file `ensureSecretDeclared` writes, `groups/<folder>/container.json`, but the spawn grants the UNION of that and `workgroups.onecli_secrets`, where neither list can subtract from the other — so deleting the vault secret afterwards would have left seven workgroup rows declaring a name that no longer resolved, aborting every spawn in the fleet rather than the one group the command named. An undo that covers one of the places the thing lives reads as complete and is not; before writing one, enumerate every store the runtime reads · `--delete-secret` refuses instead, scanning all three places a dependency can live (every workgroup row, every other group's file, every other integration's bearer) in both spellings a declaration accepts, and deletes nothing when it finds one (`src/modules/mcp-oauth/service.ts`); the refusal and each scan source are pinned by mutation-checked cases in `src/modules/mcp-oauth/login.test.ts`
- 2026-09-20 · #949 · inherited claim · Having decided that "whether `reviewDecision` reflects `require_extra_approval_for_unattributed_changes` is unverified, so keep that check fail-closed", I deleted the fail-closed check on `required_approving_review_count` — the sibling parameter in the same `pull_request` rule object — on the assumption that `reviewDecision` covers it. The evidence went the other way: an active ruleset with the first parameter on, and an open PR against that branch with three unattributed commits, reports `reviewDecision: null`. Applying a verification standard to one field and not to its neighbour is the same unverified premise wearing a fact's clothes; when a rule object's fields are read from one source, they stand or fall together · both authorities are kept, each fail-closed and read straight off the ruleset, and the measurement is quoted in the comment as fact rather than hedged (`container/skills/pr-review-loop/scripts/codex-review.sh`), with the fail-open shape pinned by a case that refuses a positive count while GitHub says APPROVED (`scripts/codex-review.test.ts`)
- 2026-09-20 · #949 · over-broad guard · Tightening `never_started_runs` to disqualify a workflow on any red run, I borrowed `ci_verdict`'s red standard as `conclusion != "success"` and left behind the condition it is attached to: that standard is `ci_verdict`'s only for REQUIRED workflows, and a non-required one whose earlier run was `neutral` or `skipped` is green there. The result refused merges the gate used to allow, on ordinary PRs, during the outage the excuse exists for. A predicate copied out of its caller has to carry the branch it sat in, not just its body · the required-workflow list is passed in as the third argument and each caller gets its own predicate — `ci_verdict` its per-required-ness one, `required_status_red` the lenient one its own green set implies (`container/skills/pr-review-loop/scripts/codex-review.sh`); both directions are pinned, strict-for-all and lenient-for-all each killing two cases in `scripts/codex-review.test.ts`
- 2026-09-20 · #949 · enforcement scope · I reported "tsc --noEmit clean" for a change whose new code was entirely in `scripts/`, and the root project's `include` is `["src/**/*"]` — so the compiler never opened the files under review, and the lane CI actually runs was red with eight errors. A green tool says nothing about files outside the glob it was pointed at; a verification claim has to name the project or path set that produced it, and match it against the one the gate runs · the claim to make is the full command list in `.github/host-ci.sh`, which `scripts/host-ci-declaration.test.ts` pins to `.github/workflows/ci.yml`'s PR lane so the two cannot drift
- 2026-09-20 · #949 · deferred hardening · Three round-3 findings were recorded rather than fixed, because the PR had to stay diff-clean for the reviewer's receipt: a run cancelled while queued still disqualifies its workflow on no evidence (it needs a "did this run execute" predicate, not a branch move, since a cancelled run's jobs conclude `cancelled` and `run_never_started` wants a `failure`); the `""` argument that keeps `required_status_red` on the lenient standard is load-bearing and survives mutation to `"CI Gate"` with all 550 tests green, so the fix for the bug above is itself unguarded; and the third scan source is documented as group-scoped while the loop is fleet-wide, which is the safer behaviour and the wrong comment · each carries a re-raise trigger and is tracked in #960
