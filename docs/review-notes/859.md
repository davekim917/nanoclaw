- 2026-09-16 · #859 · enforcement scope · Raised as a P2 during #826 review and asked to be folded into #835, which merged without it: the spawn-admission existence walk iterated `containerConfig.excludePlugins` RAW while the mount denial three dozen lines above already held the normalized set from `splitExcludedPlugins`, so a redundant-descendant shape like `["mono", "mono/plugins/alpha"]` was one entry to one reader and two to the other. Two readers of one field normalizing differently is the class #826 r3 recorded for the composer's fragment keys, and it is a defect where the outcomes agree as much as where they do not. The one shape where they did not is also the rule misapplied: refusing a spawn because an absent descendant sits under an EXCLUDED ancestor, when the ancestor already withholds the whole subtree — that line withholds nothing whether or not it exists, so there is nothing for a typo there to fail open. The rule exists for an entry that withholds nothing because it matches NOTHING; this one withholds nothing because its ancestor withholds EVERYTHING · the walk reads `[...topLevel, ...subPaths]` from the same split the denial reads (`src/container-runner.ts`), with the redundant-descendant pair and the same descendant WITHOUT its covering ancestor — which still refuses, so this is a normalization change and not a relaxation — driven through `buildMounts` in `src/provider-surfaces.test.ts`, mutation-checked against restoring the raw list ; the substitute review added the second covering shape — an ancestor that is itself a sub-path, which `splitExcludedPlugins` resolves through its prefix loop rather than through `topLevel`, so a regression honouring only top-level coverage passed the first pair — plus the case where that sub-path ancestor is itself absent and the spawn still refuses
