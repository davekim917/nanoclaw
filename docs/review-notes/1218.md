- 2026-09-26 · #1218 · provenance proxy · The first cut announced a queued setting change for any non-task row carrying `flagIntent`, taking that as "a person typed a flag and was told ⚙️", but support-thread dispatch writes `flagIntent` on system rows to pin the poller's model, and no ack is ever posted for them, so a busy support thread would have received a queued notice for a change nobody made. It also routed the notice with `extractRouting` over the whole batch, which prefers a task row and could send it to a task's channel. A field that several writers set cannot prove which of them wrote a row · the router stamps the settings line it actually posted as `flagAck` (`src/router.ts`) and the runner announces only a row carrying one, routed from that row alone (`findAckedFlag` in `container/agent-runner/src/poll-loop.ts`); pinned by the support-thread, task and mixed-batch routing cases in `container/agent-runner/src/poll-loop.test.ts` and the `flagAck` stamp case in `src/router.test.ts`, each red when its piece is removed
