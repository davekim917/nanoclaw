- 2026-09-20 · #957 · check-then-act · The consolidation pass claimed "nothing is ever overwritten" while implementing it as `existsSync(dst)` seventeen lines before `renameSync(src, dst)` — and `rename(2)` replaces an existing regular file silently. The shared tree is bind-mounted read-write into every sibling container and this code runs before `runBootMountQuiescence` proves container absence, so a sibling writing that name inside the window lost its file with no trace, no warning and no `.from-` copy. An existence check is not a reservation; only the kernel can make one · `claimSharedName` creates the destination with `openSync(dst, 'wx')` for a file and a non-recursive `mkdirSync` for a directory — both fail `EEXIST` atomically — and the move then renames over this function's own claim (`src/modules/workgroup/shared-dirs.ts`). The oracle observes the difference the only way it is observable: it spies `renameSync` and asserts the destination ALREADY EXISTS at every move, which a check-based implementation cannot satisfy ("claims the destination name before moving, rather than checking it is free", `src/modules/workgroup/shared-work-dirs.test.ts`, mutation-checked against the reverted check-then-act)
- 2026-09-20 · #957 · fail-open default · `sameFilesystem`'s `catch { return false }` is documented "unknown → assume different → caller uses the safe copy path", which is true for the directory migrator it was written for and inverted for this caller: here copy is the branch whose source removal spans the whole `cpSync`, so bytes written into the source during it are lost, while rename is atomic. A transient `statSync` failure therefore silently selected the destructive strategy. A shared helper's idea of the safe default belongs to its original caller, not to the next one · `moveStrategy` returns `rename`/`copy`/`null` and the consolidation declines the member for this boot on `null` rather than copying, retrying next boot (`src/modules/workgroup/shared-dirs.ts`), pinned by "leaves the member alone when the move strategy cannot be proven" with `statSync` made to throw for exactly that path
- 2026-09-20 · #957 · mutation coverage · Rewriting #952's F1 regression case for the new consolidating contract silently cost it its kill: with the name reservation removed, that case failed on origin/main and passed at this head, because consolidation now drains the seed folder before the migrator runs so there is nothing left for its interrupted-move arm to eat. Worse, the PR itself makes the F1 shape ROUTINE — an entry that cannot move leaves a real `artifacts/` in the member folder — and nothing covered that state, where the reservation is the only protection and the destroyed file is the only copy. When a change alters what an existing regression case exercises, re-measure the mutation it was written for; a green rewrite is not the same test · "a partly-consolidated member survives the migrator with its unmoved work" builds that exact state and runs the migrator over it (`src/modules/workgroup/shared-work-dirs.test.ts`), restoring the kill — removing the reservation now fails 2 cases again
- 2026-09-20 · #957 · stale state · Round 2 found the claim's own failure mode: a move that threw left the reservation behind as a zero-byte file or empty directory under the entry's REAL name, forever. No later boot reclaimed it, the content went to `.from-<member>` instead, and an agent following `container/CLAUDE.md` to `artifacts/<name>` read zero bytes rather than getting an error — a worse shape than a missing file, and reached by every handled failure (a crash, `ENOTEMPTY`, `EACCES`), not just a process death. The doc comment's "the next boot retries whatever is left" was true of the content and false of the name. A reservation needs a release on every path that does not consume it · the move's catch releases with `rmdirSync` for a directory and `unlinkSync` for a file, so a sibling that filled a directory claim keeps its content (`ENOTEMPTY` refuses), pinned by "releases the claim when a move fails, so the real name is still free next boot" and "releasing a claim never takes content a sibling put inside it" (`src/modules/workgroup/shared-dirs.ts`, `src/modules/workgroup/shared-work-dirs.test.ts`), both mutation-checked
- 2026-09-20 · #957 · check-then-act · Recurrence of the same class inside the fix: the cross-device branch dropped its own claim with `rmSync(dst, { recursive: true, force: true })` before renaming the staging copy into place, so the name was unreserved again for one syscall — reopening exactly the overwrite window the claim closed — and the recursive delete took any content a sibling had written into the claimed directory, which is the "deletes nothing it did not copy" promise broken a second time. A claim is only worth what the mover does with it: every path must rename OVER the claim, never around it · that line is deleted; the staging rename goes straight over the claim in both shapes (file onto file, directory onto empty directory) and fails `ENOTEMPTY` when a sibling filled it, and the staging path is removed in a `finally` because it is this process's alone by pid (`src/modules/workgroup/shared-dirs.ts`)
- 2026-09-20 · #957 · check-then-act · Round 3, third recurrence of the class in one PR, now at the RELEASE rather than the move: a claim reserves a NAME and says nothing about what is inside it by the time the release runs, and only the directory half had a guard — `rmdirSync` refuses `ENOTEMPTY`, while the file half was a plain `unlinkSync`, so a sibling writing into the zero-byte claim between the failed rename and the release lost those bytes. The guarding test was also named for more than it built ("releasing a claim never takes content a sibling put inside it") while constructing only the directory shape, so the gap read as covered. A reservation's release needs the same refusal its acquisition had, on every shape it can hold · `releaseClaim` unlinks a file claim only while `statSync(dst).size === 0`, the directory branch keeps `rmdirSync`, and the case is split into a directory shape and a file shape whose names say which they build (`src/modules/workgroup/shared-dirs.ts`, `src/modules/workgroup/shared-work-dirs.test.ts`), the file one mutation-checked against an unconditional unlink
