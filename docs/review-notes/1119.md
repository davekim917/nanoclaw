- 2026-09-24 · #1119 · silent cap · The fix for a truncated `issue list --limit 500` re-queried with a title search that had its own `--limit 100` and then treated a full result as complete, the same defect one hop down: a search is fuzzy, so a generic title can draw more candidates than the cap, and "no exact match among them" is not proof of absence. Every capped listing a decision rests on needs its own at-the-cap branch, including the fallback added to close another's · superseded: the search fallback this fixed was deleted in #1119 (last line)
- 2026-09-24 · #1119 · silent cap · The same fallback, fixed for its own cap, still replaced the full 500-issue page with the search result before checking the page's titles, so a duplicate already on the page was lost whenever search missed it: GitHub's search index lags a fresh create, and the query was built with `json.dumps`, which turns non-ASCII into `\uXXXX` and so searches for a different string. A fallback that narrows must add candidates to what is already known, never replace it, and the known page is the stronger evidence because it is newest-first and unindexed · superseded: the search fallback this fixed was deleted in #1119 (next line)
- 2026-09-24 · #1119 · silent cap · Three Codex rounds on one fallback found three adjacent gaps, each opened by the last fix: a full search result read as complete, a page discarded before checking, then query length and an uncited ordering premise. Every gap came from the title search itself (index lag, tokenizing, escaping, the 256-character query limit). The premise behind keeping it was also wrong: at gh v2.101.0 `issue list --label` already runs through that search API (`pkg/cmd/issue/list/list.go:235-244`) and stops at 1000 (checked live: `--label bug --limit 1500` returned 1000, unfiltered returned 1500 newest-first). A fallback that keeps producing adjacent gaps gets deleted, not patched · dedup is one unfiltered `gh issue list --limit 5000 --json number,title,url,labels` over the GraphQL listing, with the `smoke-finding` scope applied client-side from each issue's labels, exact normalized-title equality over the whole set, and a listing at the cap returning `unknown` instead of creating (`container/skills/smoke-test/scripts/smoke-campaign-controller.py`, `_gh`, `ISSUE_DEDUP_LIMIT`); the gh fake refuses `--label`/`--search` on `issue list` (`container/skills/smoke-test/scripts/testdata/controller-live-fakes.py`); pinned by `container/skills/smoke-test/scripts/smoke-controller-gh-issues.test.py` (test_dedup_lists_every_open_issue_without_search, test_a_listing_at_its_cap_never_files), with four mutations each turning it red
