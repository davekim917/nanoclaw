- 2026-09-16 · #851 · writable-sidecar · Found by the substitute review: the first draft committed the group config by temp-file-plus-rename, the textbook atomic write, and it was wrong here because `groups/<folder>/` is bind-mounted read-write into the agent container while the config itself is protected only by a nested read-only mount of that one file — a sibling temp file inherits none of that, so an agent could overwrite it between the host's close and its rename and have its own bytes installed as the authoritative config. The lock the issue asked for was the whole fix; the rename was an unasked-for improvement that opened a forgery path · the write is in place again, and a test asserts nothing is left beside the config after a write (`src/container-config.ts`, `src/container-config-lock.test.ts`); the lock file itself moved out of the mounted folder to a host-only directory under the data dir (`containerConfigLockPath`, `src/container-config.ts`), which no container can reach
- 2026-09-16 · #851 · mutation coverage · Also from the substitute review, and a claim of my own it falsified: the cross-process lost-update test was reported as non-vacuous on the strength of a lock-removed run made against an EARLIER version of the test; a later edit had the holder announce readiness before it read, so the test passed with the lock short-circuited. A stale verification reported as current is worse than none, because it stops the next reader from checking · the holder reads first, then announces; the test was re-run in both directions at the head that shipped (fails with the lock short-circuited, passes with it restored), and the in-process ordering test asserts non-interleaving rather than which mutator wins the kernel's flock race (`src/container-config-lock.test.ts`)
