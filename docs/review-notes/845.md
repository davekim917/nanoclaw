- 2026-09-16 · #845 · enforcement scope · The #826 finding at the second host-side reader of plugin-chosen paths: the OpenCode skill mirror symlinked every child of a plugin's skill dir and COPIED its skill manifest, then the provider copied the mirror into a session XDG with `dereference: true`, so a symlink inside a plugin repository made the HOST read a host-only file and place the bytes where an OpenCode container reads them. The premise that let it sit is the same one r5 named on #826 — a plugin repo is a trusted executable input, which is true and irrelevant, because running code IN the container is not permission to move host-only state INTO it · realpath containment on a separator boundary at both reads, because neither is enough alone: `syncSkillSymlinks`/`mirrorSkillDir` hold each skill dir and each direct child to the skill's own `pluginRoot` (a new required field on `DiscoveredSkill`), which is the only place that can see the skill-manifest COPY, and `copyOpenCodeSkills` re-decides containment for every link it dereferences, since a plugin can repoint an in-repo link between the sync and the next spawn (`src/plugin-skill-discovery.ts` and its container twin, `src/providers/opencode.ts`, `src/opencode-sync.ts`); refusals are RETURNED rather than logged in the twinned module so the parity test still holds, and both callers log them. Discover → mirror → copy end to end in `src/plugin-skill-containment.test.ts`, mutation-checked against dropping either containment and against dropping the separator boundary
- 2026-09-16 · #845 · over-broad guard · Found by the substitute review (r1) on the first head, and the finding that nearly shipped: the copy's containment was the UNION of every plugin root under `~/plugins`, while the mirror writer resolves only each skill's DIRECT children — so a link nested below a skill's top level (under a real in-repo `reference/`, which is symlinked wholesale and never walked) could reach a DIFFERENT plugin, including a workgroup-scoped one the mirror deliberately never published (`scopedPluginNames`, `src/plugin-scopes.ts`). Containing to "somewhere legitimate" is not containment; the boundary has to be the one repository the dir actually came from, and the dir's name is a skill name carrying no provenance · `MIRROR_MARKER` now records the resolved source root (JSON-encoded, so a newline in a real basename cannot truncate it) and `copyOpenCodeSkills` holds each published dir to THAT single root, with a recorded-but-unresolvable root refusing every link rather than widening; only an unmarked dir — operator-placed or natively installed, which no plugin can create in the mirror — falls back to the union, and `resolvePluginRoots` skips dot-entries so `~/plugins/.git` is not a root. `mirrorSupportDir` derives its own containing root the same way instead of taking the union. Nested-into-a-scoped-plugin, sibling-plugin, repointed-checkout and support-dir cases in `src/plugin-skill-containment.test.ts` and `src/opencode-sync.test.ts`, mutation-checked against restoring the union in both writers and against falling back when the recorded root is gone
- 2026-09-16 · #845 · deferred hardening · Also r1, recorded not fixed: the copy's filter resolves a path and `cpSync` then resolves it again to read, which is the check-then-act shape `readRulesetFile` rejected on #826 r7 — closing it means replacing `cpSync({dereference})` on the spawn path with an fd-based walk (`O_NONBLOCK` open, `/proc/self/fd` containment, `fstat`), a larger change than this fix. Winning the race needs write access to `~/plugins` on the HOST, which is operator-level authority, and no group mounts it read-write today · the cheap half of it is taken here — a resolved target must be a regular file or a directory, and `syncSkillMdCopy` requires a regular file, so a plugin-planted FIFO can no longer block the host's event loop (the r1 claim that the COPY hangs on one was wrong — measured on node 22, `cpSync` throws and fails the spawn; the copy refuses it to keep the rest of the mirror, and the blocking case is `copyFileSync` in the manifest copy). Re-raise if `~/plugins` is ever mounted read-write into a container, or if anything else gains write access to a plugin tree
- 2026-09-16 · #845 · over-broad guard · Recurrence of this PR's own r1 class, found by r2: recording the source root closed the nested-link escape only for dirs published AFTER the change, and only for SKILL dirs. `mirrorSupportDir` wrote no record at all, and every dir on a live install predates the record — and nothing re-runs the mirror sync at boot or at spawn (`syncOpenCodePluginSkills`'s only callers are the plugin-update path and the enable script), so "self-heals on the next sync" was a promise nothing kept. Both fell to the unmarked-dir fallback, which is the union this fix exists to remove, and the comment asserting that only an operator could create an unattributed dir was false — the mirror writer made one every run · provenance moved out of `MIRROR_MARKER` into its own `MIRROR_SOURCE_ROOT_FILE` so the support-dir writer can record it too without reading as a managed skill mirror (`isManagedMirror` keys on the marker, and the cleanup pass deletes a managed dir whose name is no desired skill's), and the copy now answers in four steps — the dir's own record, else the CURRENT walk's root for that name (`mirrorSourceRootsByName`, which attributes every legacy dir without any sync having run), else refuse if the dir is managed but unattributable, else the union only for an unmanaged one (`src/plugin-skill-discovery.ts` and its twin, `src/providers/opencode.ts`, `src/opencode-sync.ts`). Legacy-dir, stale-managed-dir, support-dir-nested and record-forgery cases in `src/plugin-skill-containment.test.ts` and `src/opencode-sync.test.ts`, mutation-checked against dropping the support-dir record and against restoring the union for unattributed dirs
- 2026-09-16 · #845 · fail-open default · Also r2: both mirror writers took their own owned filenames from the source, so a plugin shipping a file named like the provenance record would be symlinked into the slot the moment one of our writes failed (every write there is in a bare catch), after which the copy would read a PLUGIN-authored record — and `/` as a recorded root is wider than the union it replaced. A guard that reads a file it also writes has to own the name in both directions · `MIRROR_OWNED_CHILDREN` is skipped when mirroring source children in both writers, the record is written through an `lstat` that replaces a non-regular entry rather than writing through it, and the reader requires a regular file so a symlink standing in that slot is not followed (`src/plugin-skill-discovery.ts`, `src/opencode-sync.ts`); pinned by a plugin shipping both owned names and by a symlinked record in `src/plugin-skill-containment.test.ts`
- 2026-09-16 · #845 · check-then-act · Found by r3, and introduced by r2's own hardening: `lstatSync(<dir>/.nanoclaw-source-root, { throwIfNoEntry: false })` suppresses ENOENT and nothing else, so a top-level FILE or file-symlink in the shared mirror — a path whose directory component does not exist as a directory — throws ENOTDIR (measured on node 22) out of a `cpSync` filter and fails the spawn for every OpenCode group using that mirror. A stat call whose path has a caller-supplied directory component is not a total function, and `throwIfNoEntry` reads like one · both the record read and its write catch, and attribution starts by `lstat`-ing the entry itself and answering for a non-directory without looking inside it (`src/plugin-skill-discovery.ts` and its twin, `src/providers/opencode.ts`) — two guards for one property, so the regression case (a loose file and a file-symlink at the mirror's top level, `src/plugin-skill-containment.test.ts`) only fails when BOTH are removed, which is what a mutation check on it must do
- 2026-09-16 · #845 · fail-open default · Also r3, and the sharpest of the round: the record's `lstat` gate covered only the final component, so a top-level SYMLINK in the mirror — the pre-mirror-dir legacy shape, pruned only when a sync runs — had its "record" read from inside the plugin repository it pointed at, letting the PLUGIN nominate its own containment root; a planted `/` is wider than the union the record replaced. Second half of the same finding: a record was honoured for any path that merely resolved, so a stale one naming a directory that is no longer a plugin repository was trusted · attribution reads a record only from a real directory, and accepts it only when it names a root still present in `allowedRoots` — a claim about WHICH repository, never about what counts as one (`src/providers/opencode.ts`); symlinked-entry and non-repository-record cases in `src/plugin-skill-containment.test.ts`, mutation-checked against dropping the plugin-root check; the directory gate is pinned by the same cases only for throw-safety, since the plugin-root check already refuses a planted root
- 2026-09-16 · #845 · enforcement scope · Also r3: the attribution walk added for legacy dirs used the DEFAULT deny set while the mirror is written with the scoped plugins denied too, and discovery keeps the first plugin to claim a name alphabetically — so a legacy dir published from an unscoped plugin could be attributed to a workgroup-scoped one that sorts ahead of it, refusing its own links and admitting links into the scoped repository. A reader that derives its own population is a second definition of the same set · one `openCodeMirrorDenyPlugins()` used by both walks (`src/opencode-sync.ts`), pinned by a same-name scoped/unscoped pair in `src/opencode-sync.test.ts`, mutation-checked against dropping the deny set; the drop set's own walk is still the third population and stays #836's to fix, together with the two full plugin-tree walks this spawn path now makes
- 2026-09-16 · #845 · deferred hardening · Also r3, recorded not fixed: `mirrorSupportDir` has no cleanup pass, so a support dir published before its plugin was workgroup-scoped keeps a valid record and is still copied into every group. Containment is not the control for scoping, and pruning a set the writer does not track needs the per-entry provenance the record now makes possible · #848, which also names the cleanup shape the new provenance record makes possible; re-raise when a scoped plugin ships a support dir
- 2026-09-16 · #845 · enforcement scope · Found by r4 and reproduced against a temp HOME: `collectSiblingSupportDirs` takes `dirname(skillDir)` as a skills root, and a SINGLE-SKILL repo's skill dir is the repo root itself (discovery rule 3) — so the PLUGINS ROOT became a skills root, and every sibling repository without a root `SKILL.md` was published whole into the shared mirror under its own folder name. Nothing on the support-dir path consults the deny set, so a workgroup-scoped repository was included; `humanizer` is that shape on this install and the live mirror already carries nine such whole-repo dirs (`design-artifact-loop`'s `node_modules` among them). Pre-existing, but the provenance record this PR added would have made the copy ACCEPT them, since their record names a directory that genuinely is a plugin root — the containment answers "which repository", and the wrong repository was never in question · a skill whose `skillDir` IS its `pluginRoot` contributes no skills root, and a support dir is attributed by `supportDirRoot`, the one function the writer records from and the session copy's fallback walk reads (`src/opencode-sync.ts`, `src/providers/opencode.ts`), so the two cannot name different owners. Driven through `syncOpenCodePluginSkills` with a single-skill repo beside a scoped one in `src/opencode-sync.test.ts`, mutation-checked against restoring the repo-root skills root. The nine already-published dirs are not pruned by this change and keep behaving exactly as they do today, on the union fallback they are on now — pruning them is #848
