# Historical specifications

Specifications in this directory record the constraints and decisions in force when each feature was designed. Their historical references to pnpm's former three-day `minimumReleaseAge` policy are intentionally preserved and are superseded, as of 2026-07-19, by the latest-stable policy in `docs/SECURITY.md` and `docs/dependency-updates.md`.
