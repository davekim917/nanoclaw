- 2026-09-14 · #810 · early-return spawn branch · Scrubbing a `settings.json` env pin (`DEPRECATED_ENV`) without emitting the replacement `-e` from every spawn branch regresses any branch that returns before the ad-hoc pushes — the wiki branch fell from the 1M compact window to the provider's `165000` fallback; emit fleet env from the one shared primitive both branches already call, and put fleet constants in `REQUIRED_ENV` from the same source so a pin can never diverge from the `-e`. · `src/claude-spawn-defaults.ts`, `src/container-runner.ts`, `src/group-init.ts`
