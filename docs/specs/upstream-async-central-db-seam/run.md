# Run record: upstream async central-database seam (seam 3, upstream #3334)

## 2026-09-04 19:05Z — plan drafted (revision 1)

- Decision to port #3334 first as seam 3 taken by `update-nanoclaw` after the feasibility read (orchestrator scratchpad `seam3-3334-feasibility.md`, spot-verified: upstream `getDb()` still sync; 4 of 6 new upstream migration names already in the fork's ledger; fork eslint lacks `projectService`/`no-floating-promises`/`no-misused-promises`; 9 `hasTable` truthiness sites).
- Reverses the 2026-09-02 decline on fork issue #234 (comment posted 2026-09-04).
- PR 0 (lint rules) started in worktree `wt-pr0-lint` on branch `seam3/pr0-promise-lint` under the standing delegation; independent of plan approval.
- Plan-stage cross-model review: pending (Codex gpt-5.6-sol high, vendored adversarial prompt).

## 2026-09-04 19:40Z — plan-stage cross-model review, round 1 (revision 1)

- Reviewer: Codex, external model family. Transport exactly per the cross-model contract: `codex exec --ignore-user-config --model gpt-5.6-sol -c 'model_reasoning_effort="high"' --ephemeral --yolo --output-schema <vendored schema> --output-last-message <json>`, vendored adversarial prompt with the four markers filled, plan text on stdin, foreground, 3 600 000 ms timeout. Process log reported model `gpt-5.6-sol`, effort `high` (matches). Wall 5 m 44 s, exit 0. Classification: `completed`. Raw verdict: `needs-attention`, 5 findings, all `high` → recorded as `must_fix`.
- Findings, each traced to source by the lead and **accepted**:
  1. Raw `*Sync` twins can interleave into an open driver transaction on the shared better-sqlite3 handle (upstream's `activeTransaction` gate covers driver `access()` only). Fix: no twins (§4.2 → leaf + all importers per PR); driver transactions land last (PR 6) so PRs 1–5 hold zero driver transactions; `withRawDb` throws on `db.inTransaction` (§4.1); tripwire that `db.transaction(` has no fork callers until PR 6 (R4).
  2. The cited primitive (`withExistingNanoclawOutboundSync`'s `SyncActionOnly`) is not what the write guard runs through — `withExistingMailboxSession`'s action admits promises (`session-manager.ts:522`); the `expectTypeOf` case restates the alias. Fix: `evaluateGuardSync` with a runtime thenable check at every evaluation site + AST test that no `await` sits between it and the insert/spawn (§4.5 I-1, §8.6).
  3. Seeding the ledger with the four shared names proves the delta, not the schema. Fix: live-upgrade fixture = frozen `origin/main` registry then the reconciled registry, schema-diffed against a fresh install; same diff in the deploy rehearsal (§8.2).
  4. The watchdog is a `Promise.race`: rollback without aborting the closure body. Fix: watchdog = detection; DB-only closures enforced by an AST test; per-site post-commit-effect tests (§4.4, §8.6).
  5. SQL-constant parity is text parity, not behavior parity. Moot under fix 1 (no twins).
- Peer input folded into the same batch (update-nanoclaw-3's read-only scout, verified): upstream deleted the duty-registry sweep (153-line reconcile driver) → PR 5b is a conversion, not a merge; `withQuietInvalidationSync` joins the sync allowlist; the exclusive `session:health` window is a mailbox-level claims ordering, not a central transaction; F-14.1 ceiling 1,419/1,450 re-measured in PR 5b; mixed `vi.mock`/`initTestDb` test files convert per file.
- PR 0 evidence folded in: no hook or CI runs lint; `pnpm run lint` red on `origin/main` (92 errors, all `src/`); `projectService` cannot see `tsconfig.scripts.json` → type-aware block scoped to `src/**/*.ts`; new PR 0b fixes the 92 and installs the `pre-push` + build-guard gate (§4.5 I-2).
- Rejected findings: none. Coverage: other-family review completed; revision 2 goes to a second Codex pass scoped to the redesigned sections (§4.1, §4.2, §4.4, §4.5, §8.6) because the correction changed the design, not just the text.

## 2026-09-04 20:10Z — plan-stage cross-model review, round 2 (revision 2) → revision 3

- Same transport, same flags; model `gpt-5.6-sol`, effort `high` (log-confirmed). Wall 7 m 23 s, exit 0, `completed`. Raw verdict `needs-attention`, 3 findings (2 high, 1 medium) → `must_fix`. All three traced to source and **accepted**:
  1. "Expected zero" refusals under `withRawDb` was false: driver `get/all/run` are microtask suspension points, so an already-queued guard evaluation can run while `inTransaction` is true and a legitimate write/wake would be refused. Fix: fork-level central lease (`src/db/central-lease.ts`): `centralTransaction` and `withCentralSync` are mutually exclusive; sync blocks wait, never refuse; `withRawDb` becomes an unreachable belt; cross-context overlap test in §8.6 exercises the real write and wake paths between two awaited driver statements. (Also confirms migration 068's contract text: "in the same synchronous turn as that write" — preserved by running `withQuietInvalidationSync` inside `withCentralSync`.)
  2. `cli/resources/wirings.ts:333-339` calls a messaging-groups leaf export inside a raw IMMEDIATE transaction with a parent-plus-companion rollback invariant; PR 4 converting that leaf async would break it. Fix: leaf exports reachable from a raw transaction closure stay synchronous (no async form) until PR 6 converts them with their closure (§4.2); rollback test for the wiring case added in PR 4 (§8.4).
  3. The zero-driver-transaction tripwire could not distinguish raw from driver receivers by text. Fix: TypeScript-checker receiver resolution in `src/db/transaction-closures.test.ts` from PR 1, with positive fixtures for both receiver types (§4.4, R4).
- Rejected: none. Stopping rule: two rounds on the raw/driver-coexistence class; the lease is the reframe at the primitive. No third automated round — revision 3 goes to the deployer for the operator's approval with this record.
- Operator directives relayed 20:05Z: every merge to main and every build window HOLDS until the operator's explicit go; seam 4 (after this seam) = #3653 + `adoptRunningSessions()` + boot-time workgroup shared-FS reconcile redesign, a committed must-have.
- 20:20Z: operator's explicit go for the sync relayed by `update-nanoclaw`; hold released. Order: PR 0 → PR 0b → this plan (patch 039) → PR 1.

## 2026-09-04 21:20Z — PR 0 MERGED (#377 → main f3521bf7, --no-ff); rides tonight's restart

- Scope as built: `eslint.config.js` type-aware block for `src/**/*.ts` (`projectService` + `tsconfigRootDir`; `no-floating-promises`, `no-misused-promises`, `await-thenable` = error). `scripts/` keeps the non-type-aware config: `projectService` discovers only a file literally named `tsconfig.json`, so `tsconfig.scripts.json` (PR #335 exclusions) is invisible and all 81 files under `scripts/` failed to parse under the unscoped form; upstream never lints `scripts/` at all, so the scoped form is still a superset of upstream. Violations fixed: 115 → 0 (no-floating-promises 97, no-misused-promises 17, await-thenable 1) across 33 files, no rule-level disables. Lint wall time ~57 s.
- Behavior changes beyond lint, deliberate and recorded: (1) `src/delivery.ts` `pollSweep` gained the try/catch its sibling `pollActive` already had — without it one throw from `runSweepDeliveryCycle` skipped the reschedule and killed the 60 s sweep-delivery loop for the process lifetime (dormant bug); (2) previously-unhandled rejections now route to the module logger at `chat-sdk-bridge.ts` gateway-listener start, `codex-sync-watcher.ts` SIGTERM close, `topic-title.ts` rename IIFE, `bash-gate` timeout, `self-mod/index.ts` `onDeny`; (3) post-mutation notifications in registry-dispatched handlers are best-effort (`void Promise.resolve(notify(...)).catch(log.warn)`) at 11 sites — dispatch.ts ×2, bash-gate ×1, channel-config ×2, permissions/grant.ts ×3, remote-control ×2, self-mod/apply.ts ×1 — because an added `await` there turned a detached notify rejection into a handler failure that could leave an already-executed non-idempotent `ncl` command replayable (approval row never deleted).
- Findings and fixes: Codex reviewed 6a9d970a (P2: the replay hazard above → fixed, and generalized by an audit of every added `await` in registry-dispatched handlers, 11 sites) and 6f54f17e (P1: ratchet manifest not regenerated → answered by the ratchet commit). Test-shape regression caught by `dispatch.test.ts`: a `setImmediate` closure changed the observable `(fn, taskId, groupId)` call shape → kept Node's argument forwarding. Sync test mocks (`vi.fn()` returning undefined) broke `.catch()` on the wrapped notify → `Promise.resolve(...)` wrap, correct for any callee. Codex silent >15 min on the later heads → fallback reviewer (worker-codex, gpt-5.6-sol high) on e80d6e052: approve, no findings (serialization/timing, new failure paths, swallowed errors, sync-throw safety of the wraps — all callees are `async` declarations, setImmediate forwarding, eslint scoping incl. `src/**/*.test.ts`).
- Evidence on the final head 7ec83aaad: `tsc --noEmit` exit 0; targeted suites one file at a time under `ionice -c3 nice -n 19 … --maxWorkers=1 --pool=forks`: delivery 57/57, self-mod/request 15/15, bash-gate 4/4, orchestrator-dispatch/dispatch 19/19 (and 51/51 in cli/dispatch), reconciler 5/5, permissions/grant 24/24, llm 16/16, webhook-server 4/4, upstream-ratchet 13/13, upstream-ratchet-core 35/35; ratchet report `728 divergent files, 127,590 diff lines vs b76fcb3d (Δ 0)` after accepting growth in 9 upstream-owned files (+79 lines, each listed in the PR body); boundary check from the worktree root passed (identifiers from main checkout). Coverage gap, known: `modules/channel-config/` and `modules/remote-control/` have no test files; their fixes are lint/tsc-verified only. CI job died in 2 s with no runner (Actions quota) — not a check result.
- Gate finding carried forward: no hook or CI runs lint; `pnpm run lint` red on main with 92 pre-existing `src/` errors → PR 0b (#380, deployer) fixes them and installs `eslint src/ --quiet` in pre-push and the build guard. PR 1 starts when #380 merges.

## 2026-09-04 21:50Z — PR 0 + PR 0b DEPLOYED (restart 21:40:46Z on dist 3221df3a, bundled with #372 #376 #379 #255)

- +6 min read (21:46:49Z): preflight ok 1, adapters 12, runner snapshot 1, quiet cache warmed 1; ERROR 0; nesting/unopenable/write-refused/wake-refused/duty_failed 0; PR 0's best-effort-notify WARN 0; delivered 2, spawned 5. WARN 106, every class present in the prior boot (22cd029e baseline: worktree-cleanup boot burst 90 vs 88, sibling-real-entry 11, container exit 2, canvas 1, mount-allowlist key 1, plugin auth 1). "Archive row-marks schema created" absent = schema persisted (logs only on creation). Nothing attributable to the bundle.
- PR 0b (#380, deployer): the 92 pre-existing `src/` lint errors fixed at the root; `eslint src/ --quiet` now gates in husky pre-push, the build guard, and CI. Invariant I-2 (§4.5) is structural from this restart on. PR 1 (driver + registry + `getRawDb` rename + tripwires) started from ab22fe3e in worktree `wt-pr1-driver`, branch `seam3/pr1-driver`, runner-mailbox session.

## 2026-09-05 00:05Z — PR 1 (#393) + PR 2 (#400) MERGED and DEPLOYED (restart 23:54:12Z on dist 46f3399a; DB backup pre-pr1pr2-20260904T232140Z); PR 3 (#402) mergeable

- **PR 1 as built** (head cdc2fb75, merged 5d6aaeb4): nine upstream driver files byte-identical (`driver.ts` replacing the type-only shim, `driver-registry(.test).ts`, `drivers/{shared,sqlite,sqlite.test,sqlite.conformance.test}.ts`, `testing/driver-conformance.ts`, `compose.ts`); `SqliteDriver` wraps an already-open handle — `compose.ts` opens the file (WAL, foreign_keys) and registers via `registerDbDriver`; `connection.ts` imports `compose.js` for that side effect, so the storage worker thread's own `initDb` finds a driver. `getDb()` → sync `DbDriver`; `getRawDb()` = `sqliteRaw(getDb())`; `hasTableRaw` = the old sync `hasTable`. Rename: 1,148 `getDb()` occurrences in 169 files; 12 `hasTable` sites in 7 files; boot sites awaited (main.ts, worker thread, 20 entry-point scripts at module level). Non-test signature changes: exactly `initDb/initTestDb/closeDb/hasTable`; 289 test hooks became async. Tripwires: `raw-db-ratchet.test.ts` pins 219 importer files (shrink-only); `transaction-closures.test.ts` resolves receiver types with the TS checker — **11 central raw sites** (the plan's 10 + `migrations/index.ts`) and 16 non-central SQLite sites pinned separately. Upstream's conformance suite passes on better-sqlite3 11 → R3 closed, no bump. Verdict: Codex 👍, 0 threads (a local fallback was started before the 👍 was visible — the poll missed the PR-level reaction; rule corrected: 👍/findings IS the verdict, fallback only after 15 min silence, poll all three signals). Deviations recorded: the worker ran `git stash push --keep-index` once (nothing lost; rule restated); found a pre-existing red test on main from #372 (`ensureRepositoryLock` missing from the worktree-cleanup import manifest) → fixed on main at 8e0f8baa.
- **PR 2 as built** (head ecb9d93e4, merged 46f3399a): ledger reconciled by `name` — 019 adopted byte-identical (inert `sqliteOnly` field added to `Migration`), 065→020 and 066→023 adopted (SQL identical), 045 kept as the strict superset with upstream 021 not imported, 070 `messaging-group-detached-at` and 071 `host-coordination` net-new with zero writers (024's `up` is pure DDL). **Runner stays synchronous** (plan §4.3 amendment): upstream's async runner would place a `DbDriver.transaction` before PR 6. Tests: registry 3 incl. the live-upgrade-vs-fresh schema diff (frozen pre-seam3 fixtures; proven non-vacuous by moving 070 earlier), db-v2 46, transaction-closures 6, boot-order 3. Deploy rehearsal by the deployer on a `.backup` copy of live v2.db: applied delta exactly the two names, 5 ms, integrity ok, raw-DDL diff vs fresh identical to main's pre-existing 17/6 (runtime-created `repo_operation*` tables + ALTER-history text). Verdict: Codex 👍 at 23:16Z, 0 threads.
- **Deploy read** (+6 min, over rotated + current logs — logrotate fired at 00:00Z mid-window): `Running migrations count=2`, both `Migration applied` lines at 23:54:14Z; `Central DB initialized dialect="sqlite" role="runtime"` ×2 (main + worker thread); preflight 1, adapters 12, snapshot 1, quiet cache 1; ERROR 0; seam counters 0; WARN 116 all known classes.
- **PR 3 (#402, head 8cd6296a5)**: usage, provider-health, dropped-messages on the driver with all importers (no signature ripple — every call site was already async); scheduled-tasks unchanged (its only central access is the pinned raw transaction); kept sync per §4.2: `rollupSessionUsage`(+`getWatermark`), `markProviderUnavailable`(+`getProviderHealth`, `isProviderUnavailable`), `resolveAndValidateDestination`. Named-object params pass through `SqliteDriver` unchanged. Ratchet Δ -29 with zero accepts (`dropped-messages.ts` now byte-identical to upstream). New `dropped-messages.test.ts`. Verdict: Codex 👍 at 23:41Z, 0 threads. Awaiting merge.
- PR 4 (big-four leaves + ~90 importers) building on `seam3/pr4-big-leaves`; guard-path reads get their private raw read of the leaf's exported SQL constant now (§4.5 I-1), PR 6 wraps them in `withCentralSync`.

## 2026-09-05 02:03Z — PR 6a MERGED (#403 → main; central lease primitive, no callers, no restart)

- Five Codex rounds on one file, then the stopping rule. Accepted and fixed: the raw handle escapes the block (→ epoch-scoped `RawDb` facade + `NoLiveSqlite`); a prepared `Statement` still escapes (→ `RawStatement` facade, no `iterate`/`pluck`); a callback can leave `BEGIN` open past the lease (→ rollback postcondition in `withRawDb` and at the end of `withCentralSync`, `RawBlockLeftTransactionOpenError`, the callback's own error never masked); TypeScript `private` fields are enumerable (→ `#private`); `isThenable` had a `typeof function` hole (→ closed); a thenable that reaches a synchronous seam could reject unhandled (→ `disownThenable` before the contract throw). Every fix carries a fail-on-revert test.
- Deferred under the stopping rule: round 5 (async `withRawDb` callbacks rejected at the callback boundary) → issue #408, lands with PR 6 alongside the first production callers. Runtime today: the facades are epoch-scoped, so a resumed async callback is refused, never executed inside a later transaction.
- Head af3ba9fb; tsc/eslint/prettier clean; 39/39 across `central-lease`, `raw-db-ratchet`, `transaction-closures`.

## 2026-09-05 02:15Z — PR 4 OPENED (#411, head 29dea5fe on a0996819); resumed by update-nanoclaw-2b

- The first worker's 147-file conversion survived only on disk (checkpoint 12ce8cd1 → rule: checkpoint-commit AND push at every gate). Its "2 tsc errors" was a parser halt; the true remainder was 472 (424 in 24 test files → 4 workers on disjoint files; 48 in 20 source files → session lead), then 461 eslint errors (413 in 71 test files → 4 more workers; 38 in source).
- Decision (lead-approved): the guard decision seam `guard()` stays SYNC (§4.5 I-1) — the first pass had made it async, which breaks `guard(a2aSend)` inside the a2a WriteGuard and `guard(threadsClose)` inside thread-close's one-synchronous-decision block. Its four central reads execute the leaves' exported SQL constants on the raw handle; `guard/guard.ts`, `cli/guard.ts`, `agent-to-agent/guard.ts`, `write-destinations.ts` join the raw-db pin as the plan's allowlisted sync blocks (PR 6 wraps them in `withRawDb`).
- Hoisting rule applied wherever an await had landed between a precondition read and its write: `RecallCentral` (provider + `SessionServicesCentral`) for the recall build (`buildPreTurnContext`, `resolveRecallLifecycle`, `buildSessionServicesSnapshotFrom` synchronous again; the write guard is proved with nothing awaited before the insert); `admitDueTaskContextsFor(…, central)` for the dashboard's run-now action; per-session timezone for the scheduled board, the resume verb and tasks-update (which had also spread an un-awaited `wallClockUpdate()` promise); the agent-group row for the orphaned-fence pass; the storage pass synchronous with a raw configured-image read. Retained async session callbacks (no guard between read and write): `writeSessionRouting`, startup `admitPendingUpgradeContexts` (await first), sweep `prepareDueWake`.
- Fixture class that fell out of the sync guard reads: tests that mocked a leaf the guard now reads raw needed a real row (`createSession`, an in-memory `container_configs` table) or a raw-handle stub (`guard.test`, `host-sweep-registry`'s SQL-aware fake) — recorded because PR 5a/5b/6 will meet the same class.
- Evidence on 29dea5fe: tsc host+scripts 0; `eslint src/ --quiet` 0; boundary check passed; upstream-ratchet regenerated (18 accepts, all await/async insertions or the sync guard blocks; Δ −1,232); §8.4 acceptance tests present and failing on revert; targeted suites green (table on the PR). Pre-existing red on main, not this PR: `onecli-preflight.test.ts` boot-wiring (`indexOf` hits a `main.ts` comment).
- Restart: alone, after an explicit operator go (§5). Codex verdict pending.
