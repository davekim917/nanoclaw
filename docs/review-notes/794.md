- 2026-09-13 · #794 · enforcement scope · A restricted-runtime early return bypassed the global egress-lockdown decision, so a configured security boundary no longer reached a maintenance spawn · `assertWikiEgressAllowed` in `src/container-runner.ts`
- 2026-09-13 · #794 · env parity · A supported API-key-only host configuration could not start a restricted Claude actor because its model-auth adapter accepted only OAuth · `wikiModelAuth` in `src/container-runner.ts`
- 2026-09-13 · #794 · fail-open default · A publication notice without an explicit channel instance could silently resolve through a default or sibling bot · `src/wiki-admission/policy.ts` and `src/modules/wiki-admission/index.ts`
