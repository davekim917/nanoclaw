- 2026-09-24 · #1128 · inherited claim · The effort-migration Detect step reused a plain grep for `"effort"` in `container.json`, which also matches a Codex fallback's effort key; on the deciding install all 9 hits were Codex-fallback false positives, so an operator would have read unpinned groups (e.g. main) as already pinned to medium and been surprised when they moved to high. Caught by the substitute closing review · Detect now prints effort by key path with jq, listing only the layers that apply to Claude (`effort`, `defaultEffort`, the providerConfig effort on a Claude primary; the fallback effort only where the fallback is Claude, per `container/agent-runner/src/config.ts`); it was run on the install (no output) and on a Codex-primary fixture with a Claude fallback (reports only the fallback) before it was written into the doc
