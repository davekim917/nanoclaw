- 2026-09-15 · #826 · over-broad guard · Two individually-valid `excludePlugins` entries compose into a spawn refusal: a sub-plugin mask is an EMPTY read-only bind, so listing both `bootstrap/plugins` and `bootstrap/plugins/orchestrate` emits a mask whose mountpoint no longer exists inside the ancestor's mask, docker cannot create one under a read-only bind, and the container fails to start instead of excluding the plugin — blocking all the group's work over a redundant line in its config. The always-on composer already resolved ancestors correctly (`isExcludedSubPath`), so the two consumers of the same field disagreed about what a nested pair means; a field whose entries can contain one another needs the covering relation resolved once, at the seam both consumers read, not at each consumer · `splitExcludedPlugins` drops every sub-path an excluded ancestor covers, in both ancestor shapes (a top-level entry and a shallower sub-path), so no consumer can see a redundant descendant (`src/container-config.ts`); the split's own table and a `buildMounts` regression asserting exactly one mask mount for a nested pair live in `src/container-config.test.ts` and `src/provider-surfaces.test.ts`, both mutation-checked against restoring the unconditional add
- 2026-09-15 · #826 · over-broad guard · Recurrence of this fragment's own class, one round later and found by review r2: `excludePlugins` had no validation at all before it grew sub-paths, and the validator added for the sub-path shape also imposed a `[A-Za-z0-9._-]` slug allowlist on every segment, including the top-level names the field has always held. Those names are directory basenames nobody chose — `scripts/enable-agent-plugin.ts`'s `resolvePluginDir` accepts ANY direct child of `~/plugins` (it checks only "is a directory, parent is the plugins root") and `applyOptOut` writes that basename straight into the list — so a clone named `foo+bar` or `c++-tools` would make `readContainerConfig` throw on every read and take the whole group's spawn down over a config that already worked, which is the guard refusing a legitimate state rather than a bad input. Adding validation to a field that had none narrows an accepted set by definition; the rule has to be derived from what actually breaks (traversal, separator confusion, control characters) rather than copied from a sibling constant whose input is operator-authored · the segment rule refuses only empty, `.`, `..`, a backslash and `\p{Cc}` (`src/container-config.ts`), with the enabler's real basename shapes and the control-character cases in `src/container-config.test.ts`, mutation-checked against restoring the slug allowlist and against dropping the control-character exclusion
- 2026-09-15 · #826 · unescaped interpolation · Found by review r3, and created by r2's own fix: widening the segment rule to stop refusing legitimate directory names also let a colon through, and a sub-path entry — unlike a top-level one — is interpolated into a mount, where `readonlyMountArgs` serializes it as `${hostPath}:${containerPath}:ro` (`src/container-runtime.ts:29-30`) and docker reads the colon as a `-v` field separator, so the spawn fails. The asymmetry is the whole point and is what the earlier round missed in both directions: a top-level entry is only ever tested for Set membership against a `readdirSync` name and never reaches a mount argument, so it must keep accepting every name it accepted before, while a sub-path is a value pasted into a delimited format and has to be refused for that format's delimiter. Relaxing a validator is not a single decision — it has to be re-derived per consumer, because the consumers of one field do not all interpolate it into the same place · the colon refusal is gated on `segments.length > 1`, so it binds exactly the entries that become mounts (`src/container-config.ts`), with the sub-path refusal and the top-level acceptance pinned together in `src/container-config.test.ts`, mutation-checked against deleting the guard and against dropping the top-level carve-out
- 2026-09-15 · #826 · evidence keying · Found by review r3: the always-on composer keyed each sub-plugin's fragment by `plugin-<repo>-<basename>.md`, but `subPluginDirs` walks BOTH sub-plugin layouts, so a repo carrying the same name under `plugins/` and at its root (`bootstrap/plugins/twin`, `bootstrap/twin`) produced one key for two rulesets — and the collision branch kept the first and dropped the second behind a `log.warn`, so a group silently lost a standing directive it had opted into. A derived key is a correctness surface, not a label: deriving it from part of an identifier that the producer already guarantees unique in full throws away the guarantee, and "warn and keep the first" turns the resulting ambiguity into data loss instead of an error. The fix must also not trade a collision for a traversal — the full path contains `/`, which is safe only for as long as no consumer turns a key into a filename, so that has to be asserted against every consumer rather than assumed · the fragment is keyed by the full `subPath`, unique by `subPluginDirs`'s own `seen` set and always containing a `/` that a `readdirSync`-derived top-level key never can, making the two key spaces disjoint and the collision branch unreachable (deleted rather than left as a silent drop) (`src/claude-md-compose.ts`); the comment there enumerates every write and both reads of `desired` by line, and the two fixed output paths, because the `/` is only safe while that holds; twin-survival and twin-exclusion tests in `src/claude-md-compose.test.ts`, mutation-checked against restoring the basename key and against disabling the sub-path exclusion
- 2026-09-15 · #826 · fail-open default · Found by the substitute review (r4) on the whole diff, and the strongest finding of the four rounds: the sub-plugin mask works only because what it binds is EMPTY, and `emptyPluginMaskDir` proved nothing — `mkdirSync(recursive: true)` succeeds on an existing directory whatever it holds, and on a symlink pointing elsewhere. Content in that one directory is not a mask that leaks, it is an exclusion INVERTED: every walker finds a manifest at exactly the path the operator declared excluded and cannot tell it from the real sub-plugin, one inode backs every mask in the fleet, and the control's documented purpose includes withholding a CLI carrying the host's OAuth session. The earlier rounds all reasoned about the mask's CONTENT-DELIVERY path and none asked whether the source satisfied the property the whole mechanism rests on; "created by the host under `data/`" was treated as equivalent to "empty", which is provenance standing in for a state nobody read · `emptyPluginMaskDir` asserts what it returns — `lstat` (never `stat`, so a symlink is refused rather than followed) plus a directory that reads empty, failing the spawn otherwise (`src/container-runner.ts`), with contaminated-source and symlink-source refusals driven through `buildMounts` in `src/provider-surfaces.test.ts`, mutation-checked against neutralising the emptiness read and against relaxing `lstat` to `stat`
- 2026-09-15 · #826 · over-broad guard · Third occurrence of this fragment's registered class, found by the substitute review: r3 established that a sub-path is interpolated into a mount while a top-level entry is only compared for Set membership, and gated the COLON rule on that distinction — but left the backslash refusal and the control-character rule applying to both, so a real directory named `back\slash` or carrying a newline still threw in `readContainerConfig` and blocked the group's spawn, contradicting the test that claimed to accept "any real directory name". A distinction discovered while fixing one rule has to be carried to every rule that shares its premise in the same commit, or the next round finds the same class at the rule that was left behind · the character rules are selected by `isSubPath`, with top-level entries held to `EVERY_SEGMENT_RE` (shape only — never empty, `.` or `..`) and only sub-paths to the backslash/control/colon rules (`src/container-config.ts`), pinned by real backslash, newline and DEL basenames in the accepted set and their sub-path counterparts in the refused set (`src/container-config.test.ts`), mutation-checked against restoring the shared rule
- 2026-09-15 · #826 · deferred hardening · Two substitute-review findings recorded rather than fixed: `bootstrap`'s root `.nanoclaw-always-on.md` still concatenates the Orchestrate directive, so an OpenCode group excluding `bootstrap/plugins/orchestrate` keeps that standing directive even though the plugin bytes are masked; and `subPluginDirs`'s root-layout walk accepts any non-hidden directory rather than requiring a plugin manifest, so a non-plugin `always-on.md` would compose. Both are directive-content, not a destructive category, and each carries its own re-raise trigger · #828
- 2026-09-15 · #826 · enforcement scope · Found by the substitute review (r5) on the head that fixed r4, and the defect that most nearly shipped: making compose read a plugin's OWN `always-on.md` — instead of only the operator-authored `.nanoclaw-always-on.md` it read before — moved the reader's input from a file the operator wrote to a path the PLUGIN chooses, and `statSync`/`readFileSync` follow symlinks while `subPluginDirs` walks through directory symlinks, so an `always-on.md` symlinked at `~/.codex/auth.json`, or a sub-plugin directory symlinked at `/etc`, was read BY THE HOST and written into the group's `AGENTS.md`, which is mounted into the container. The trap is the premise everyone accepted, including the round that deferred the adjacent finding: "plugin repositories are already trusted executable inputs" is true and irrelevant, because running code IN the container is not permission to move host-only state INTO it. Changing who authors a path the host reads is a trust-boundary change even when the directory it lives in has not moved · containment enforced at the one place the bytes are read, `readRulesetFile` taking the plugin repo root and comparing `realpathSync` of both sides on a separator boundary (`src/claude-md-compose.ts`), so a symlink anywhere on the path — final component or walked parent — cannot leave the repository while an in-repo symlink still composes; file-symlink, directory-symlink, prefix-sibling and in-repo-symlink cases in `src/claude-md-compose.test.ts`, mutation-checked against dropping the check, against dropping the separator boundary, and against checking the unresolved path
- 2026-09-15 · #826 · pattern audit · Also r5: the same commit that taught the composer "Codex fires plugin hooks, so do not compose its directive twice" left the skill's own opening line saying Codex and OpenCode fire no plugin hooks at all, so `.claude/skills/enable-agent-plugins/SKILL.md` stated both and an agent reading it could derive either enablement model. Hook delivery is conditional on the manifest — Claude always, Codex only for a plugin shipping `.codex-plugin/plugin.json` that declares a hook, OpenCode never — and bootstrap's own `orchestrate-agents` is the case that proves the qualification matters. A behavioural fact restated in more than one place in a document must be re-derived at every one of them in the commit that changes it, not only where the change is being written · the opening line now states the conditional and names the registerable-but-hookless case (`.claude/skills/enable-agent-plugins/SKILL.md`)
- 2026-09-15 · #826 · deferred hardening · Round 6 returned the third consecutive P1 in one subsystem — a container-absolute symlink inside a plugin repo is absent to the mount builder's host `statSync`, so the mask was skipped as "nothing to mask" while the container resolved the path fine and the declared exclusion silently did not apply. Severity flat across three rounds is the policy's stop-and-diagnose signal, and the diagnosis was that no single guard was missing: the MECHANISM required the host to predict what three independent in-container walkers would resolve, and each round found another place the two namespaces disagree. The operator's call was to remove the mechanism rather than patch the seventh site. So the empty-directory mask, its shared source under `data/`, and the mask emission in `buildMounts` all came out, together with the validator rules whose only justification was the mount interpolation — a colon refusal and a backslash/control-character rule left standing would have been comments citing a code path the value no longer reaches, which is the false-premise shape #583 cost four rounds to. A sub-path entry now validates and drives the always-on composer and nothing else, stated plainly in the field's own doc comment, the enablement skill and `docs/workgroups.md` so the gap cannot be mistaken for a working exclusion · the mount path honours top-level entries only, pinned by a `buildMounts` case asserting a sub-path exclusion emits NO mount and does not disturb the repo's own (`src/provider-surfaces.test.ts`), mutation-checked against reinstating a sub-path mount; container-side exclusion, where each walker honours the same list in its own namespace with no host prediction, is the follow-up
- 2026-09-15 · #826 · pattern audit · Recurrence of this fragment's own registered class, and the second time in this PR that fixing one site of a documented rule left its siblings: r5 corrected the enablement skill's opening line about conditional hook delivery, and r6 found the operational step 3 still telling the operator to author `.nanoclaw-always-on.md` for every plugin with a SessionStart hook — with the deterministic script independently producing the same instruction. That override is for a plugin we do NOT control; beside a plugin that ships its own generic ruleset it delivers the directive twice, on OpenCode as both files and on Codex on top of the plugin's own hook. Prose and the script that implements the same prose are two sites of one rule, and the script is the one an operator actually follows · the enabler derives the plugin's own ruleset across both sub-plugin layouts and the repo root and drops it from the next-steps condition (`scripts/enable-agent-plugin.ts`), with the skill's step 3 and its two provider tables restated to match; driven as a subprocess against a fixture HOME in `scripts/enable-agent-plugin.test.ts`, mutation-checked against dropping the condition and against checking only the repo root
- 2026-09-15 · #826 · deferred hardening · Also r6: plugin rulesets were read with no size bound, so a repo could carry an arbitrarily large `always-on.md` at a path the composer reads synchronously on the spawn path, paying it as spawn latency and host memory before the project-document cap can look. Containment says where a file may be, not how big it is · a size bound read from the stat before the file is opened refuses rather than truncates, since half a standing ruleset is a directive with its carve-outs cut off (`src/claude-md-compose.ts`), with an oversized-sibling case in `src/claude-md-compose.test.ts`, mutation-checked against removing the bound
- 2026-09-15 · #826 · pattern audit · Fourth occurrence of this fragment's registered class, found by review r7: cutting the mount mask out updated the field's doc comment, the enablement skill, `docs/workgroups.md` and the mount code, and missed `docs/skills-model.md`, a container-side comment in the Codex registration walker, and four test titles/comments still framed as "the host masked it". Those tests pin real walker behaviour — a manifest-less or skills-less directory is skipped — so the framing was the defect, not the assertions, and the framing is what an operator reads to decide whether an exclusion works. Removing a mechanism has the same audit obligation as fixing one, and the surfaces that describe it are not a subset of the surfaces that implement it · the walker tests are retitled to pin the skip on its own terms and name the follow-up that will hang the exclusion list on it; the Codex walker's comment now states plainly that a sub-plugin entry is NOT handled anywhere yet (`container/agent-runner/src/codex-companion-setup.ts`), and `docs/skills-model.md` carries the same two-distances table as the field's doc comment
- 2026-09-15 · #826 · check-then-act · Also r7, and the residual left by r5's own fix: `readRulesetFile` resolved the path, then let `statSync` and `readFileSync` resolve it AGAIN, so containment and the new size bound were decided about a string that any component — leaf or walked parent — could have stopped meaning by the time the read happened. Measuring the alternatives is what made the answer clear rather than arguable: a hard link defeats containment in BOTH forms, because a hard link is not an indirection and `realpath` and the fd path both answer with the in-repo name (verified on this host), so no amount of path resolution closes it and `nlink` is the property that actually differs — the same answer this repo already reached for the canonical-git sentinel. Equally, no deterministic test distinguishes an fd-based containment check from a path-based one from outside the function, so the first test written for it passed under both mutations and was deleted rather than shipped: a test that cannot fail for the reason it names is worse than none · the read opens once with `O_NONBLOCK` (a plugin-planted FIFO would otherwise hang the host's event loop before any check could reject it) and answers containment from `/proc/self/fd/<fd>`, size from `fstat` and the bytes from that same descriptor, with `nlink !== 1` refused (`src/claude-md-compose.ts`); the hard-link case is pinned in `src/claude-md-compose.test.ts` and mutation-checked, and the pattern is `readContainedFile`'s in `src/dashboard/api/attention-fs.ts`
- 2026-09-15 · #826 · check-then-act · Recurrence inside r7's own fix, found by r8: the read opened the file once but resolved the repository ROOT by pathname AFTER that open, which is a second lookup the first cannot constrain — swap the repo-root symlink for one resolving to a parent between the two and a descriptor holding a host credential measures as contained by the freshly-resolved root. Its no-`/proc` fallback re-resolved the file by pathname for the same reason, reinstating exactly the lookup the descriptor check exists to avoid. Two lessons, and the second is the one worth keeping: r7 concluded that no deterministic test could distinguish an fd-based containment check from a path-based one and deleted its test, which was right about the test and wrong about the conclusion — an ordering claim wants an ordering PROBE, not a value assertion that holds either way, and an `fs` spy drives both the fallback and the call order · the root is resolved before the open so the descriptor is always judged against the root that was intended, and a descriptor that cannot be identified fails closed rather than falling back to a pathname (`src/claude-md-compose.ts`); `src/dashboard/api/attention-fs.ts` keeps its weaker fallback because it serves a live dashboard where emitting nothing is a visible outage, while here the cost is that a macOS dev checkout composes no plugin rulesets. Pinned by a spy-driven no-`/proc` case and a per-read ordering probe asserting every open is preceded by its own root lookup (`src/claude-md-compose.test.ts`), both mutation-checked
- 2026-09-15 · #826 · fail-open default · Also r8, and the cost of the previous round's relaxation: dropping the sub-path character rules left the segment rule shape-only, which accepts a NUL. JSON can express one and a POSIX filename cannot hold one, so an entry carrying it PASSES validation and can never match anything — `codex` with a trailing NUL lands in the top-level exclusion set, fails to match the real `codex` directory, and the plugin mounts, turning a credential-withholding exclusion into credential delivery on the very field whose documented purpose includes withholding a CLI that carries the host's OAuth session. Removing a rule is a decision about every value it used to refuse, and this one needed re-deriving per byte rather than per rule: backslash, newline and DEL are legal bytes in a real Linux directory name and stay accepted, NUL has an independent filesystem justification and does not · the segment rule refuses NUL alone (`src/container-config.ts`), with top-level, sub-path and bare cases in `src/container-config.test.ts`, mutation-checked against restoring the shape-only rule
- 2026-09-15 · #826 · pattern audit · Also r8: the enabler's new `hasOwnRuleset` counted a repo-root `always-on.md`, and the composer never reads one — `subPluginDirs` returns `<repo>/plugins/<sub>` and `<repo>/<sub>`, never the repo itself. So a maintained single-plugin repo with its ruleset at the root had the operator override suppressed while nothing composed the ruleset either, and an OpenCode group got neither. The test shipped asserting that exact shape as correct, which is an oracle written from the same belief as the code and therefore incapable of disagreeing with it. An eligibility check that mirrors another module's set has to be derived from that module's set, not from a superset that looks plausible · the predicate is exactly the composer's two layouts (`scripts/enable-agent-plugin.ts`), with the repo-root shape asserted to STILL require the override in `scripts/enable-agent-plugin.test.ts`, mutation-checked against re-adding the root case
- 2026-09-15 · #826 · fail-open default · Recurrence of r8's own class one round later, found by r9: the impossible-filename guard stopped at NUL and left an unpaired UTF-16 surrogate accepted. JSON can express one, node re-encodes it as U+FFFD on the way to a syscall, and the entry then never equals the real `readdirSync` name — so a top-level `codex` exclusion carrying a lone surrogate silently fails and, with `codexHostAuth`, the host's Codex OAuth mount is admitted alongside the plugin the operator meant to withhold. The lesson is the framing rather than the byte: r8 fixed "NUL is not a filename character", when the invariant is "an accepted entry must be a string a filename could BE", and a rule written as a list of banned characters cannot tell you what else belongs on the list. Written as a property, both cases fall out and so does the boundary that keeps backslash, newline, DEL and an emoji accepted — those name real directories · the segment rule excludes `\p{Surrogate}` under the `u` flag, which matches a LONE surrogate only since a valid pair is one astral code point (`src/container-config.ts`), with unpaired-high, unpaired-low, sub-path and valid-astral-pair cases in `src/container-config.test.ts`, mutation-checked in both directions — against dropping the surrogate class and against narrowing to ASCII
- 2026-09-15 · #826 · fail-open default · Third instance of this class on one PR, found by r10: after NUL (r8) and lone surrogates (r9), a segment longer than `NAME_MAX` (255 bytes on this host) still validated, and no basename can be that long — so the entry matches nothing, the plugin mounts, and with `codexHostAuth` the host's Codex OAuth mount is admitted alongside the plugin the operator declared withheld. Three rounds each added one more item to a list, and the list has no end: "cannot name a file" is not enumerable from the string, which is why reframing the rule as a property in r9 caught the next byte but not the next LENGTH. The consequence is what is actually enumerable — an exclusion that matched nothing — and that question is only answerable where both sides are present, which is the mount builder, not the validator. A typo, an uninstalled plugin, and a byte no filename can hold are indistinguishable from the config alone and identical in effect. The first fix REPORTED the fact and the operator corrected it to REFUSE: a withholding control that was accepted and can never match is a silent fail-open, and a warning nobody reads preserves exactly that — an unresolvable `onecliSecrets` name aborts a spawn for the same reason · every accepted entry must equal an entry on disk, compared as the exact string `readdirSync` returned and walked segment by segment so sub-paths are covered too; an unmatched entry throws and names the group and the entries (`src/container-runner.ts`). The byte-length refusal stays as a cheap config-read-time gate with a precise message (`NAME_MAX_BYTES`, `src/container-config.ts`). NUL, lone-surrogate, over-length, case-mismatched and nonexistent-sub-path entries are all driven through `buildMounts` in `src/provider-surfaces.test.ts`, mutation-checked against turning the refusal back into a warning and against checking only the first segment
- 2026-09-15 · #826 · enforcement scope · The enabler and the composer each had a notion of "this plugin ships its own ruleset" and they were not the same set, which is a defect in whichever one you call wrong: `subPluginDirs` returns children only, so a single-plugin repo whose directive is a repo-root `always-on.md` was counted by the enabler and read by nobody, and the operator override was suppressed for a repo whose ruleset nothing composed. Both narrowing the enabler and widening the composer close the reported gap, and they are not equivalent — narrowing leaves a repo we maintain still needing a NanoClaw-specific `.nanoclaw-always-on.md` to reach OpenCode, which is the property the operator required a plugin repo not to have. The wider fix was taken deliberately rather than by default · the composer reads the repo root's own generic ruleset file under the same containment and the same `plugin-<name>.md` key, so an operator override present alongside it wins by being written first, and the enabler counts the root again (`src/claude-md-compose.ts`, `scripts/enable-agent-plugin.ts`); root-composition, override-precedence, top-level exclusion and the Codex gate are pinned in `src/claude-md-compose.test.ts` and the enabler's side in `scripts/enable-agent-plugin.test.ts`, mutation-checked against dropping the root read and against letting it overwrite the override
- 2026-09-15 · #826 · env parity · Recorded, not fixed here: the composer withholds a plugin's own `always-on.md` from Codex because Codex fires the plugin's SessionStart hook — true only for a plugin whose `.codex-plugin/plugin.json` declares hooks AND whose hook identity is trusted, and container-side hook trust is what #827 installs. Before #827, Codex plugin hooks report `untrusted` and are never dispatched, so a maintained plugin's directive reaches a Codex container by neither route. Composing for Codex in the meantime would deliver the text twice the day #827 lands, so the ordering is the fix rather than the code: #827 merges first and both deploy in one restart, making the premise true at the moment this code goes live · the dependency is stated in the composer's own comment, `docs/skills-model.md` and the PR body; re-raise if #826 is deployed before #827, or if #827 ships without trusting the hooks these statements assume
