- 2026-09-22 · #999 · test-proves-the-path-you-wrote · Four review rounds, four real defects, and a green suite through every one of them — the tests exercised the code path the author built, never the path production takes, so "all tests pass" carried no information about whether the feature worked. Round 2 found the feature DEAD on every default install: agents reply through the `send_message` MCP tool whenever outcome reporting is on, which is the fleet default (`effectiveOutcomeReporting` returns true unless explicitly disabled, `src/container-config.ts`; the prompt directs it at `destinations.ts`), and that handler writes its chat row straight to `writeMessageOut` (`mcp-tools/core.ts`) without ever entering `sendToDestination`, where the stamping lived. Every test drove the `<message to="…">` envelope, so the majority path had no coverage at all. Round 4 found the same shape one field over: model was read from the provider's RESOLVED value while effort was read from `querySettings.effort`, which `applyFlagBatch` documents as "USER INTENT ONLY" — so a group carrying its effort in `container.json` requested nothing and the line showed no effort, and a sticky effort the resolved model cannot support is clamped away by the provider, so the line named a level the turn never ran at · the decision moved to the one seam BOTH reply paths cross (`db/messages-out.ts` → `stampStatusSubtext`), gated on an explicit `agentReply` opt-in rather than on `kind === 'chat'` — round 3 showed that kind is far broader than "a reply the agent composed", since `send_file` captions and the runner's own `/clear` notice are routed chat rows too — and `AgentQuery.resolvedEffort` was added as a REQUIRED field, not an optional one, per the warning already written above `resolvedModel` in `providers/types.ts` that an optional field is a forgettable one; requiring it immediately caught the mock provider, which would otherwise have let tests keep passing while production rendered a blank. Two habits would have replaced all four rounds: enumerate every writer at the seam you are hooking BEFORE choosing where to hook (round 3's finding is a list of `writeMessageOut` callers anyone could have produced in five minutes), and never let a manual reproduction stand in for an end-to-end run — the live Slack and Discord posts in this PR went through the platform APIs directly, proving rendering while bypassing the agent, the runner and the delivery path, and so could not have caught either of the two bugs that made the feature useless · pinned by tests each verified to FAIL without its fix, including one built from the exact row shape `send_message` writes (no envelope, no `sendToDestination`), one driving two turns through a single `processQuery` to catch a per-query clear standing in for a per-turn one, and two reproducing the effort failures from a provider's resolved values (`container/agent-runner/src/poll-loop.test.ts`, `turn-status.test.ts`)
