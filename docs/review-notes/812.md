- 2026-09-14 · #812 · check-then-act · `CodexRateLimitTracker.read()` captured nothing before its `await`, then assigned the full response over `this.snapshot` — a sparse `account/rateLimits/updated` push merged during that await was overwritten by the older answer, its rows double-sampled and the park decision recomputed on stale data; a merge across an await has to prove the state it read is the state it writes over · monotonic `pushSeq` captured before the await and compared after, discarding a superseded read (`container/agent-runner/src/providers/codex-rate-limit-tracker.ts` `read()`), with the held-read-then-push regression in `container/agent-runner/src/providers/codex-rate-limit-tracker.test.ts`
- 2026-09-14 · #812 · over-broad guard · Recurrence inside the fix for the first one: the round-1 fix compared one whole-snapshot `pushSeq` and discarded the ENTIRE read when ANY field had an intervening push — a sparse primary-only push mid-read then blanked out the read's own fresh secondary reading too, at initial bind (`snapshot=null`) leaving a weekly bucket at 96% unparked and unchecked for a full cadence interval; a check guarding one field must not also discard the fields it was never asked about · per-field `fieldPushSeq` (via `codexRateLimitSnapshotUpdatedKeys`, `container/agent-runner/src/providers/codex-rate-limits.ts`) lets `read()` keep the read's answer for every field a push did not touch during the await (`container/agent-runner/src/providers/codex-rate-limit-tracker.ts` `read()`), with the sparse-unrelated-field and stale-secondary-refresh regressions in `container/agent-runner/src/providers/codex-rate-limit-tracker.test.ts`
