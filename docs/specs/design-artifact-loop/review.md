# Review: design-artifact-loop (Cycle 3 — partial)

> Generated by `/team-review` under `/team-auto` — 2026-06-24
> Reviewers run: A (architecture/codebase-fit, Claude) ✅ · B (best-practice) ⏸ deferred · C (Codex adversarial) ⏸ deferred
> B/C deferred: Reviewer A surfaced a **product-scope** blocker (F5) that, once resolved, materially changes the design — a fresh review is required after the scope decision, so running B/C on the current design now would be wasted (per `/team-review` "a changed design requires a fresh assessment").

## Verdict: MUST-FIX > 0 → escalated to user (scope decision) + spec revisions queued

## SOUND (verified correct against source)

- **S1 — MCP tool cross-provider invocability:** a new `design_review` tool registered in `container/agent-runner/src/mcp-tools/` (barrel `index.ts`) is invocable by all three providers via the single `nanoclaw` stdio MCP server (Claude SDK `mcpServers`; Codex `[mcp_servers.nanoclaw]` TOML; OpenCode `{type:'local'}`). `send_file`/`render_diagram` are exact precedents. (A7 structurally satisfied.)
- **S2 — render env:** Dockerfile pins match the design (agent-browser@0.28.0, chromium `/usr/bin/chromium`, single shared image). C2 genuinely resolved.
- **S3 — skill auto-surfacing:** `container/skills/` bind-mounts and `syncAgentSkillsMirror` mirrors into the shared `~/.agents/skills` discovery root for all runtimes — a new dir surfaces to all three (caveats in F3).

## Findings

| ID | Severity | Class | Summary |
|----|----------|-------|---------|
| F1 | significant | MUST-FIX | Cross-round cap/carry-forward state CANNOT live in MCP-tool module memory — on Codex the app-server (and its child MCP process) is spawned per query and torn down (`codex.ts:731-736,978-982`), resetting `{round,findings[]}` mid-loop. Breaks C1 parity + R6 carry-forward on Codex. Fix: rehydrate round/cap state from a disk file every call (read-modify-write); make the trace file authoritative. |
| F2 | significant | MUST-FIX | `/workspace/outbox/<id>/` is the host delivery sink — deleted after delivery (`session-manager.ts:772`) AND not in `send_file`'s source allowlist (`core.ts:23-29`). The design homes the trace/artifacts there (design.md:219-220, 279-281). Fix: author/iterate + store state/trace under `/workspace/agent/` (valid send_file source, persistent); stage into outbox only at send time (render_diagram pattern). |
| F3 | significant | SHOULD-FIX | OpenCode has NO native skill *loader* — the skill surfaces as TEXT in AGENTS.md, not an invocable `/skill` command (`plugin-skill-discovery.ts:177-181`). "Consumed identically" (OQ4) is wrong for OpenCode. Fix: spike must verify OpenCode *follows* the loop from AGENTS.md text; state that OpenCode delivery is text-injection. |
| F4 | significant | MUST-FIX (risk) | The L1 critic's screenshot ingestion is UNPROVEN on any provider: the inbound formatter passes images as TEXT references (`[image: … saved to …]`, `formatter.ts:480-493`), not vision content blocks. The model only "sees" pixels via a provider/tool-specific vision read, nowhere demonstrated. Also OpenCode has no present same-provider subagent spawn primitive (`opencode.ts:131-135` calls it "future") — its only separate-process critic is `codex exec` (= L2, deferred). Fix: spike must explicitly prove vision ingestion per provider and name `codex exec --yolo` as the concrete cross-process critic; stop asserting L1 is achievable on all three. |
| **F5** | **significant** | **ESCALATED (scope)** | An existing `container/skills/frontend-engineer/SKILL.md` already implements render-grounded iteration (agent-browser screenshots desktop/tablet, 3-iteration cap, WCAG-AA, no-overflow, consistent empty/loading/error states, semantic HTML/a11y, anti-pattern denylist, "never done until verified in a browser"). VERIFIED by direct read. design-artifact-loop's render-verify loop massively overlaps it; both would fire on "design me a UI". **Positioning (new skill / extend frontend-engineer / compose) is a product-scope decision → escalated to user.** |
| F6 | minor | SHOULD-FIX | Under `NANOCLAW_EGRESS_LOCKDOWN`, chromium can't reach a font CDN (no `--proxy-server` on the screenshot args) — the fallback chain always renders. Reframe "CDN MAY fetch" as a delivery-time nicety, not a render-time variable. |
| F7 | minor | SHOULD-FIX | `decisions.yaml` C2 `verified: false` is stale vs design.md's validated C2. Reconcile the verification ledger. |
| F8 | minor | SHOULD-FIX | C8 linter should flag ANY network-capable construct (inline `<script>`, `fetch`, `XMLHttpRequest`, `<img src=http…>`, `<link rel=preconnect>` beyond the font host) — not just external `<script src>`. Inline beacon could exfiltrate. |

## Disposition

- **F5** → user scope decision (this escalation). Blocks meaningful progress; resolving it changes the design.
- **F1, F2, F8** → mechanical/spec revisions (the design omitted detail); apply on resume — within allowed `/team-auto` revisions.
- **F3, F4** → calibration revisions (state the OpenCode text-delivery + L1 limits honestly; harden the spike to prove vision ingestion). F4 is the highest-risk unknown; the existing mandatory spike is the right gate but must be made explicit about vision.
- **F6, F7** → minor clean-up on resume.
