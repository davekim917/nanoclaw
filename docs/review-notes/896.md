- 2026-09-17 · #896 · check-then-act · Four review rounds on one seam, each patching what the freeze-range pin stored rather than where and how long it lived: a baseline-only pin, then a whole-result pin in ONE mutable PR-state slot (a slow poll holding an old head's result overwrote a newer head's pin), then per-head files that were still private to one coordinator's state dir (a run resumed through the shared lease recomputed), trimmed (racing the claim that would have protected them) and read as absent when truncated or symlinked (recomputed smaller, or unofferable forever). The invariant was never the record's contents: a pin must outlive and out-reach everything that can recompute it — same reach as campaign ownership, no deleter that cannot see run state, and invalid is not absent · `range_pin_lookup`/`range_pin_promote` (`container/skills/smoke-test/scripts/smoke-pr-gate.sh`) are the only pin code: immutable `ln`-created files keyed by repo+PR+head in the shared lease dir, no trimming, `invalid` ⇒ unknown/`full` and still offered, no shared dir ⇒ not offered; tests 5j-iv..5j-viii in `smoke-pr-gate.test.sh`
- 2026-09-17 · #896 · pattern audit · A range-sized JSON value was passed to `jq` as `--argjson`. Linux caps one argument at 128 KiB, the live range was already ~41 KB, and past the cap `jq` fails to exec — the pin candidate vanished and a settled freeze was skipped forever with nothing logged. The first site named was one of six carrying the same value · every range-sized value reaches `jq` through `--slurpfile` from a builtin `printf`, or stdin (pin candidate, facts emit, `check` output, settle-candidate line, settled wake); test 5j-iii drives a 5,200-path range end to end and fails against the prior commit
