# Post-Build Drift Report: plan.md → implementation

> Generated by `/team-drift` — 2026-04-24
> Source-of-truth: `.context/specs/create-agent-provider/plan.md`
> Target: Implementation files produced by the build (Groups A + B)
> Agents: A (Claude Sonnet) · B (Codex) · C (Gemini — skipped, 2-agent mode)

## Summary (reconciled)

| Class | Count | Blocking |
|-------|-------|---------|
| MISSING | 2 (distinct; 5 sub-findings from Codex flatten to 2 underlying gaps) | Yes — **but both are Group C artifacts explicitly scoped out of this build** |
| DIVERGED | 0 | — |
| PARTIAL | 2 | Review |
| CONFIRMED | ~77 | — |

Agent A (Claude) extracted 81 finer-grain claims; Agent B (Codex) extracted 29 coarser-grain claims. Both agents agree on the substance. The 5 Codex MISSING findings (codex.ts file, codexConfigSchema, registration, CodexProvider class, codex test file) all correspond to the same 2 underlying artifacts Claude flagged: `container/agent-runner/src/providers/codex.ts` and `container/agent-runner/src/providers/codex.configSchema.test.ts`.

---

## MISSING — 2 distinct artifacts (both Group C, both out-of-scope)

### [B1] `container/agent-runner/src/providers/codex.ts` schema additions absent

- **Claim (SOT):** Task C1 specifies the Codex provider file must export `codexConfigSchema`, call `registerProviderConfigSchema('codex', ...)`, and stash validated config in the `CodexProvider` constructor.
- **Target:** Not present. The file `container/agent-runner/src/providers/codex.ts` does not exist in this fork's current working tree at all.
- **Context:** Group C was explicitly **scoped out** of this build per the user's fork-first decision — the trunk PR covers Groups A + B only, and Group C is a separate future PR targeting `origin/providers` (after syncing from `upstream/providers`). This is documented in the plan's Group C header ("Pre-conditions: ... this group's changes land on `upstream/providers`, not on `main`"), the Known Risks section ("Cross-branch scope | Yes — Group C targets `upstream/providers` branch"), and the pre-build drift acknowledgment `drift-acks.json` entry B2.
- **Found by:** Agent A (#80), Agent B (#25, #26, #27, #28).
- **Status:** **Out-of-scope for this build, not a defect.** Per plan's Implementation Surface, Group C is a linked follow-up PR. Not blocking the trunk PR.

### [B2] `container/agent-runner/src/providers/codex.configSchema.test.ts` absent

- **Claim (SOT):** Task C1 specifies a test file with at least 9 test cases for the Codex schema.
- **Target:** Not present — same reason as B1.
- **Found by:** Agent A (#81), Agent B (#29).
- **Status:** **Out-of-scope for this build, not a defect.** Will land with the Group C follow-up.

---

## PARTIAL — 2 findings

### [P1] `registerProviderConfigSchema` call placement in `claude.ts`

- **Claim (SOT):** Task A3 — "`registerProviderConfigSchema('claude', claudeConfigSchema)` called at module top level, immediately after `registerProvider('claude', ...)`."
- **SOT says:** Schema registration immediately after provider factory registration at module top level.
- **Target says:** Schema registration IS at module top level and IS immediately after `registerProvider('claude', ...)` (both calls are adjacent at lines 1258–1259 of the concatenated target, which is the bottom of `claude.ts`). However, `claudeConfigSchema` itself is *declared* near the top of the file (line 335) — far from where it is registered (line 1259). The plan's pseudocode implied declaration and registration would be co-located near the top.
- **Gap:** Schema declaration and registration are separated by ~900 lines of class body. Functionally equivalent (module-level side effects run in file order; both calls execute before `createProvider` is called). Cosmetic.
- **Found by:** Agent A finding #21.
- **Status:** Open. Low-priority polish. The acceptance criterion literal text ("immediately after `registerProvider`") IS satisfied. The "near the top of the file" intent is not. Can fix in `/team-qa` by moving the schema declaration + registration to a dedicated block at the bottom of the file next to `registerProvider`, or accept as-is.

### [P2] Orphan-cleanup `notifyAgent` message is generic, not orphan-specific — **FIXED post-gate 2026-04-24T14:38**

- **Claim (SOT):** Task B2 test case `test_create_agent_rollback_failure_notifies_orphan` — "notifyAgent called with orphan-cleanup message (text contains 'create_agent failed' and names the group)."
- **SOT says:** When the rollback `fs.rmSync` itself fails (both DB and FS paths fail), the user-facing notification should surface the orphan-folder risk so the user knows manual cleanup may be needed.
- **Target says:** In the code (`src/modules/agent-to-agent/create-agent.ts`), `safeRemoveFolder` logs the rollback failure (`log.error('create_agent: rollback fs.rmSync failed — orphan folder', ...)`) but the `notifyAgent` call in the calling code uses the generic `create_agent failed: database insert failed for "${name}".` message — same message whether the rollback succeeded or failed. The "orphan" language only appears in the log, not in user-facing chat.
- **Gap:** User cannot distinguish "DB failed cleanly, no orphan" from "DB failed AND rollback also failed, manual cleanup needed" without inspecting host logs.
- **Found by:** Agent B finding #22.
- **Status:** Closed. Fix applied post-gate per user approval: `safeRemoveFolder` now returns boolean, `orphanSuffix` helper appends `"(orphan folder at groups/X — manual cleanup may be needed)"` when rm fails. Both rollback paths (updateContainerConfig catch + createAgentGroup catch) use it. Test assertion tightened to require the orphan-specific text — would have failed against the pre-fix implementation. Full suite still 317 pass / 0 fail / 1 todo. Typecheck clean.

---

## CONFIRMED (~77)

All other plan claims — file creations, interface signatures, ASSERTs, acceptance criteria, named test cases (31 total across Groups A+B), negative ASSERTs (no zod import on host, no `provider === 'claude'` branch), legacy-call byte-identical preservation, sequenced-write-with-rollback ordering, envelope guard, Zod 4 idiom (`z.strictObject`), 5-value effort enum matching SDK, and every Group A+B test case by identifier — are all CONFIRMED. Full agent outputs retained in `.claude/tmp/` for audit.

---

## Gate (adjusted for intentional scope)

- [x] CONFIRMED claims: ~77 of 81 (Claude grain) / 23 of 29 (Codex grain). Consistent across agents.
- [x] DIVERGED: 0.
- [ ] MISSING: 2 distinct artifacts (Group C files). **Flagged as intentional scope-out**, not an implementation error.
- [ ] PARTIAL: 2 findings logged for `/team-qa` consideration.

Per `/team-drift` gate rules, MISSING is blocking and NOT eligible for drift-acks. However, both MISSING findings correspond to Group C, which was **explicitly scoped out of this build** by user decision:

- Plan's Group C header: "This group's changes land on `upstream/providers`, not on `main`."
- Plan's Overview: "Cross-branch scope | Yes — Group C targets `upstream/providers` branch."
- Pre-build drift-acks.json B2: Group C deferred to follow-up PR per fork-first policy.

**Decision required from user:** Accept the 2 MISSING as out-of-scope scope-carry-forward (consistent with pre-build ack B2), or re-scope the build to include Group C (which requires syncing `origin/providers` from `upstream/providers` first, plus a separate worktree/branch for providers-branch work).

Recommendation: **Accept as out-of-scope.** The build delivered exactly what was scoped (Groups A + B, trunk PR). Group C's "missing" status is by design, not by defect.

---

**Status:** GATE pending user decision on the 2 MISSING (Group C scope-out).
**Next step:** User approves, then proceed to `/team-qa` (or close the session and merge the trunk PR).
