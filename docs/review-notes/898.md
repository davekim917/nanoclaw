- 2026-09-17 · #898 · fail-open default · A "full/unknown" fallback that names nothing is an off switch, not a safe default: four producers of `selection:"full"` (crashed matcher, unreadable catalogue, catalogue failing validation, floor history with no run root) each returned a well-shaped empty or partial result that was then pinned immutably or read as "nothing due", and each was patched one review round at a time until the rule was stated once — an unusable input selects NOTHING and is never pinned, unknown history makes everything due · `JOURNEYS_PINNABLE` guards candidate acceptance and `journeys_pin_promote` (`container/skills/smoke-test/scripts/smoke-pr-gate.sh`); the matcher exits non-zero on any unusable catalogue; tests 5k-empty-full-refused, 5k-invalid-catalogue, 3-missing-root in `container/skills/smoke-test/scripts/smoke-journeys.test.sh` and `container/skills/smoke-test/scripts/smoke-pr-gate.test.sh`
- 2026-09-17 · #898 · fail-open default · "Invalid is not absent" was applied on the producing side (the gate reports an invalid pin as `full`, still offered) and then reversed on the ENFORCING side, where the barrier treated the same invalid pin as no pin and an unrelated terminal lane cleared a run with zero journey coverage; a test asserted that outcome. Enforcement also keyed on the lane's kind rather than the journey's declared evidence, so a floor browser journey scaffolded as a plain lane passed on a text file · `read_gate_pin` + the rebuilt selection `pin-run --catalogue` writes, and the single `journey_lane_problem` helper for matched and disposition-linked lanes (`container/skills/smoke-test/scripts/smoke-journeys.py`); tests 5b-invalid-pin-*, 5c-* in `container/skills/smoke-test/scripts/smoke-journeys.test.sh`
- 2026-09-17 · #898 · evidence keying · The barrier accepted a run whose selection equalled ANY journeys pin for its head sha, so two campaigns on one commit in the shared lease dir could trade scopes; identity is repo + PR + head, with the PR read from the shared lease, never from the run's own copy · `container/skills/smoke-test/scripts/smoke-evidence-barrier.sh` resolves exactly one owning pin and refuses an ambiguous repo; tests 5b-sibling-pr-*, 5b-ambiguous-repo in `container/skills/smoke-test/scripts/smoke-journeys.test.sh`
