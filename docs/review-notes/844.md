- 2026-09-16 · #844 · unaudited clear · Found by the substitute review: `ncl tasks update --model "" / --effort ""` now removes a per-fire pin, and the update loop's audit row only ever recorded `recurrence` and `processAfter`, so the pin an operator just destroyed was gone from the row AND from the trail — irreversible, with `repin` auditing the very same change through `detail.repin.from/to`. Worse, the change that made `""` mean clear also made an unset shell variable (`--model "$MODEL"`) a silent clear, where before it was a no-op · the loop records `pin: { from, to }` whenever `flagIntent` changes, both axes, read from the same `before` row it already loads, and a test asserts the `scheduled_audit` row after a clear (`src/cli/resources/tasks.ts`, `src/cli/resources/tasks.test.ts`)
