- 2026-09-15 · #835 · evidence keying · Found by review r1, and a recurrence of #826 r3's class one PR later: the OpenCode XDG drop set decided what to withhold by comparing the NAMES that survived two discovery walks, one with the exclusion list and one without. Discovery keeps only the first plugin to claim a name (first-plugin-wins in `discoverPortableSkills`, `src/plugin-skill-discovery.ts`, alphabetical) and the mirror being filtered was built from the UNFILTERED walk (`syncOpenCodePluginSkills`, `src/opencode-sync.ts`), so when the excluded sub-plugin is a name's FIRST provider the name survives the filtered walk — supplied by the later plugin — while the bytes in the mirror are still the excluded one's. The difference then read that name as "kept" and copied the excluded source into the group, which is the exclusion INVERTED rather than merely leaked, and every test written for it used a fallback plugin sorting BEFORE the excluded one, so the only order that breaks was the one never exercised. A name is part of an identifier the producer guarantees unique in full (the `skillDir`); comparing the part throws the guarantee away, exactly as the composer's basename fragment keys did · the drop set asks `isExcludedPluginPath` about each discovered skill's own `path.relative(pluginsRoot, skillDir)` instead of differencing names, so one walk answers the question the copy depends on (`src/providers/opencode.ts`), with the inverted-order case — an excluded sub-plugin sorting ahead of an unexcluded plugin claiming the same skill name — pinned in `src/providers/opencode-skill-exclusion.test.ts`, mutation-checked against restoring the name difference
- 2026-09-15 · #835 · deferred hardening · Recorded beside the fix above rather than fixed: the drop set's own walk denies only `DEFAULT_DENY_PLUGINS`, while the mirror it filters also denies workgroup-scoped plugins (`scopedPluginNames`, `src/opencode-sync.ts`), so the two walks can pick different winners for one name and a scoped plugin sorting ahead of an excluded sub-plugin would leave the excluded source copied through. Closing it threads the mirror's deny set through the spawn-path call, which is machinery beyond the accepted finding · #836 (re-raise if a workgroup-scoped plugin ever ships a skill name an unscoped plugin also ships, or the two walks' deny sets diverge further)
- 2026-09-15 · #835 · pattern audit · Found by review r2: the per-walker table this PR added to the sub-plugin section of `docs/skills-model.md` was correct, but the sentence introducing it — "a whole entry drops the mount, so the plugin is absent before the container starts" — was left standing as an absolute, and it is not one on OpenCode, whose skills come from the host's per-sibling mirror rather than the mount, and whose session XDG copy is filtered for sub-path entries only. `.claude/skills/enable-agent-plugins/SKILL.md` states that exception in both of its tables, so the repo documented a plugin boundary two ways and the doc an operator reads to decide whether an exclusion is complete carried the overclaiming one. Writing the new half of a comparison is not licence to leave the old half unread: the sentence that frames a table is part of the table's claim · the sentence now names the OpenCode exception, the reason it is deliberate (every OpenCode group on this install carries top-level entries, so widening the filter would withdraw live skills) and the function that implements the narrowing, and links the skill that already stated it (`docs/skills-model.md`)
- 2026-09-15 · #835 · pattern audit · Recurrence of this fragment's own class one round later, and of #826's fourth occurrence of it — found by review r3. The PR implemented container-side exclusion in four walkers and updated the two surfaces it was writing (`docs/skills-model.md`, the enablement skill), but left three others still stating the mechanism it had just replaced: the field's own doc comment saying a sub-path entry withholds the standing directive "and NOTHING ELSE", `docs/workgroups.md` saying container-side exclusion "lands in a follow-up", and the mount builder's comment saying the same. Each of those was written by the PR this one is the follow-up TO, which is the trap: the surfaces that describe a mechanism are not a subset of the surfaces that implement it, and the ones most likely to be missed are the ones the previous PR wrote to record the gap — they read as settled prose rather than as a claim with a shelf life. An operator reads exactly those three to decide whether an exclusion is complete · all three restated to describe registration-withholding through the shared predicate, grepped for the whole family of stale phrasings rather than only the sites the review named (`src/container-config.ts`, `docs/workgroups.md`, `src/container-runner.ts`)
- 2026-09-15 · #835 · fail-open default · Found by the substitute review (gpt-5.6-sol high) after the connector's round cap was spent, and the strongest of its three findings: the container's `excludePlugins` reader was written to fail closed — ENOENT means "nothing declared", every other read, parse or validation failure throws — and then read the field off whatever `JSON.parse` returned. `[]`, `42`, `"oops"` and `true` all parse, so `.excludePlugins` answered `undefined` and the reader returned "nothing excluded" with no failure anywhere; the reachable shape is an array wrapping the real config, where the operator's entries are sitting in the file and every walker would register what they withheld. A guard's contract has to cover the inputs that SUCCEED at the step before it: parsing is not the same as being a config, and a fail-closed reader that only checks whether the read threw has verified the channel, not the document · a root that is null, an array, or any non-object throws before the field is read (`container/agent-runner/src/excluded-plugins.ts`), with all five scalar and array roots plus the array-wrapping-a-real-config shape in `container/agent-runner/src/excluded-plugins.test.ts`, mutation-checked against removing the root check
- 2026-09-15 · #835 · over-broad guard · Also the substitute review, and the mirror image of the r1 P1 in the same function: the drop set names a SOURCE in the plugin tree, but it was applied to mirror entries by NAME, and `syncSkillSymlinks` preserves a directory it did not create rather than overwriting it, so an operator-placed or natively-installed mirror dir sharing a name with an excluded plugin's skill was withdrawn from the session copy — an exclusion taking content nobody excluded. The marker that writer drops in every directory it publishes is the only provenance the mirror carries, and it is exactly the predicate the writer itself uses to decide what to preserve, so the consumer had a ready answer it was not asking · the copy drops only a dir carrying the writer's managed marker (`src/providers/opencode.ts`), pinned by an unmarked-dir case in `src/providers/opencode-skill-exclusion.test.ts`, mutation-checked against dropping on the name alone; the opposite direction — a MANAGED entry stale relative to the tree — needs per-entry source provenance written by `syncSkillSymlinks`, which is a mirror Codex shares, and is recorded in #836
- 2026-09-15 · #835 · fail-open default · Second pass of the substitute review, and the defect that made this fragment's own first `fail-open default` line unreachable: the container's new root-shape check can only refuse a document the container gets to READ, and the spawn path rewrites the file first. `readContainerConfig` is tolerant by design, `ensureRuntimeFields` is a read-modify-write that runs on every spawn (`src/container-runner.ts`), and composed they replace a root the reader could not understand with the reader's materialized default — so the operator's own entries are destroyed on disk, the deny policy silently becomes "nothing excluded", and the fail-closed reader downstream never sees the shape it was written to refuse. Two individually reasonable behaviours, tolerance in a reader and identity-sync on a writer, compose into config data loss; a guard placed downstream of a normalizing write is a guard on data that no longer exists, so the check has to sit at the write that destroys the original · `writeContainerConfig` refuses to overwrite a file whose root parses to a non-object, leaving the bytes intact (`src/container-config.ts`), with the array-wrapping-a-real-config case, every scalar root, and the deliberate carve-outs (an object root and unparseable bytes both still write, since neither has a field to have lost) in `src/container-config-overwrite.test.ts`, mutation-checked against removing the assertion
- 2026-09-15 · #835 · over-broad guard · Inverted: the third substitute pass argued a carve-out OUT, which is the rarer direction for this class and the one worth recording. The overwrite refusal added a round earlier exempted bytes that do not parse at all, reasoning that refusing them would strand a group with a corrupt config no repair path could rewrite. That reason did not survive being checked: a truncated write leaves the realistic corruption, entries visibly in the file and the tolerant reader answering "none", and automatic replacement was never a repair path, because every writer is read-modify-write over that same reader and would persist its guess. The only caller that legitimately creates a config returns before writing when one exists, so nothing about setup depended on the carve-out. A carve-out is a claim about what would break, and it has to be verified against the callers exactly like the guard is — the reviewer enumerated them and none needed it · absence is distinguished from a failed read rather than inferred from one, and every other state of an existing file is refused with the repair named in the message (`src/container-config.ts`), with the truncated-config case, a read failure, and first-time initialization over a corrupt file pinned in `src/container-config-overwrite.test.ts`, each mutation-checked
- 2026-09-15 · #835 · deferred hardening · Fourth substitute pass, and the round where the answer was to stop patching: the reviewer found the same invariant at the same seam again, now temporally — `writeContainerConfig` validates the file and then writes it with no lock, version or compare-and-swap, and three of the four mutators of that file are read-modify-write over the tolerant reader, one of them in a separate process, so a concurrent write can silently drop an `excludePlugins` entry. Verified pre-existing and outside this PR's diff: the diff changes no writer's read-modify-write structure, and the refusal added during review shares the window the lost update already had rather than introducing one. Three passes had each produced a correct fix that exposed the next state of the same file; the fourth naming the class itself is the signal the policy describes, and the fix it asks for — one owning cross-process mutation primitive every writer routes through — is machinery outside this change · #840 (re-raise on any disagreement between `container_configs` and a group's per-group container config file, an exclusion reappearing after a config update or enabler run, or any new writer of that per-group config file)
- 2026-09-15 · #835 · fail-open default · Also the fourth pass, and a claim the fix itself falsified: the doc comments and PR body still said the tolerant reader lets a group with an unreadable config keep booting. It does not, once the file exists — `ensureRuntimeFields` writes identity fields on every spawn and they are always missing from a file the reader had to default, so the new refusal aborts that spawn. The install used to silently repair such a file into a config declaring no exclusions. Wedging one group until a person looks at it is the intended trade, but a fix that changes how a BROKEN install behaves has to restate every claim about the old behaviour, including the ones written in the same commit · the operational consequence is stated at the refusal itself and in the container reader's comment, both of which previously repeated the old claim (`src/container-config.ts`, `container/agent-runner/src/excluded-plugins.ts`)
- 2026-09-16 · #835 · enforcement scope · Fifth substitute pass, and a real bypass the four before it missed: the skill walker applies its exclusion per LAYOUT RULE, and it had the check on only two of the eight. Rules 4-6 walk root-level sub-plugin directories by fixed name — a repo's `plugin/`, `<repo>-cursor-integration/`, `<repo>-claude-plugin/` — and they run before the manifest-gated rules that carried the check, so with first-match-wins an excluded sub-plugin in one of those shapes was already recorded by the time anything asked. The entry was honoured by Claude's walker and Codex's and silently not by this one, which is a provider-parity break as well as a scope leak, and the mirror carries those skills to OpenCode regardless of the host-side XDG filter. A control enforced at each site of a walk is a control the next site added forgets; the walk's rules all end at one recording function, and that is where the question belongs · the predicate is asked once in `recordCandidate`, about `pluginName` joined to the candidate's path under the plugin dir, so every layout rule present or future is covered and the two per-rule checks are gone (both twin copies, `src/plugin-skill-discovery.ts` and the container's), with the three fixed-name layouts pinned in `container/agent-runner/src/plugin-exclusion-walkers.test.ts`, mutation-checked — removing the hoisted check fails that test and the two pre-existing sub-plugin ones
- 2026-09-16 · #835 · mutation coverage · Caught in my own work while checking the fix above, and the reason the fix's host-side test says something different now: the first version of that test claimed to pin the hoisted check in the HOST copy of the walker, and passed with the check deleted. Nothing on the host passes `excludePlugins` into discovery any more — an earlier round replaced the only such caller with one that asks the predicate about each discovered skill's own path — so the host copy's exclusion parameter has no live caller and no host test can exercise it through a public entry point. The test was not wrong about its assertion, only about its reason, which is the failure mode that survives review: it reads as coverage for a guard it cannot fail on · the test now names the mechanism it actually pins (ancestor coverage over the drop set's own relative path) and is mutation-checked against that mechanism, and the host copy's option carries a comment saying it has no host caller, why it stays as the container twin's parallel, and that the twin's tests are what pin it (`src/plugin-skill-discovery.ts`, `src/providers/opencode-skill-exclusion.test.ts`)
- 2026-09-16 · #835 · pattern audit · Seventh substitute pass, a delta check after merging main, and the one thing the merge broke that no test could see: this PR was written against a revision of #826 where an `excludePlugins` entry matching nothing was an inert no-op logged at startup. #826's FINAL state, which is what landed, refuses the spawn for such an entry — it walks each entry segment by segment against the real tree before the mounts are built. Three of this PR's surfaces and its body still described the behaviour of the revision it was branched from, and the code around them merged cleanly because none of it contradicts the new rule; only the prose did. A base that changes its mind about a behaviour leaves no conflict marker on the documents that describe it, so a merge has a re-reading obligation the resolver's conflict list does not express: every claim the branch makes about the base has to be re-derived against the base as it actually merged · the four surfaces now state the spawn refusal (`src/container-config.ts`, `.claude/skills/enable-agent-plugins/SKILL.md`, `docs/skills-model.md`, and the PR body), the container loader's comment says why it logs the declared list rather than re-deciding what the host already judged (`container/agent-runner/src/excluded-plugins.ts`), and the walker test that pinned the old behaviour is retitled to pin what it actually covers — the walkers' own output for an entry the host would have refused
