- 2026-09-17 · #896 · check-then-act · Found by the substitute review, and the third finding on the same seam (baseline-only pin, then whole-result pin, then pin storage): the "pinned per campaign" freeze range lived in ONE mutable slot of the PR state, guarded by a same-head lookup. A slow poll still holding an OLD head's evaluation passed that lookup (the slot held a different head), overwrote the slot after a newer head had pinned unknown/`full` and opened its campaign, and the next poll of the newer head recomputed — the scope shrink the pin exists to prevent. The PR lock serialized the writes and preserved nothing. A value that is "pinned" but stored where another key's writer can replace it is not pinned; the two earlier fixes patched what was stored, not where · pins are immutable files keyed by head SHA, created with `ln` (fails on EEXIST) inside `range_pin_promote`, read only through `range_pin_lookup` (`container/skills/smoke-test/scripts/smoke-pr-gate.sh`); tests 5j-iv (the reviewer's interleaving, via a hold seam) and 5j-v (second promotion for one head is a same-inode no-op) in `smoke-pr-gate.test.sh`
- 2026-09-17 · #896 · pattern audit · A range-sized JSON value was passed to `jq` as `--argjson`. Linux caps one argument at 128 KiB, the live range was already ~41 KB, and past the cap `jq` fails to exec — the pin candidate vanished and a settled freeze was skipped forever with nothing logged. The first site named was one of six carrying the same value · every range-sized value reaches `jq` through `--slurpfile` from a builtin `printf`, or stdin (pin candidate, facts emit, `check` output, settle-candidate line, settled wake); test 5j-iii drives a 5,200-path range end to end and fails against the prior commit
