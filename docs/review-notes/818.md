- 2026-09-15 · #818 · unobservable failure · #812's Codex rate-limit READ was fail-open telemetry whose only error report was a container log line that dies with the container, and its sibling PUSH path kept writing `rate_limit_samples` rows, so two hours of a half-dead feature read as health fleet-wide (408 push rows, zero pull rows) and a human reading a live container's logs was the only thing that found it; a fail-open path needs a detection surface that does not depend on its own error reporting surviving, and for a two-writer table that surface is the asymmetry between the writers · `scripts/rate-limit-telemetry-health.ts` reports an (agent group, credential_set) pair pushing inside the window with no `usage_pull` row in it, and counts DB-open/query failures and unparsable timestamps separately from zero rows so a swallowed read never reads as "no rows"; 30 cases in `scripts/rate-limit-telemetry-health.test.ts`, 12 mutations all killed
