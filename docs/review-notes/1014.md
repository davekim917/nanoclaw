- 2026-09-23 · #1014 · over-broad guard · Holding a mid-turn follow-up's claim until it is consumed handed the existing claim-stuck rule a set of claims it had never seen. The rule forgives an aged claim only when the heartbeat moved after it, and the heartbeat moves only on provider events. The claude provider runs without partial messages, so a check-in that arrived during a long think, with no tool in flight, would be kill-claimed after 60 s: a healthy turn killed mid-work. Test (b) pinned that kill as "unchanged". The rule was unchanged, but what it was judging was not. When a change widens what a guard sees, re-derive the guard's verdict for each new input, not just its code · `decideStuckAction` forgives a claim while the provider is executing a query that has produced an event and a heartbeat exists (`src/modules/sweep-container-health/index.ts:570`, applied at `src/modules/sweep-container-health/index.ts:673`). A query with no event since it started (hung at the gate) is still killed, and the ceiling still reaps a silent one; pinned by `src/modules/sweep-container-health/health.test.ts:2088`, with the old-runner and new-runner DB shapes read through the real host reader in `src/modules/mailbox/mailbox.test.ts:1064`. Removing the forgiveness fails 2 of those cases, and forgiving on `provider_executing` alone fails 4
- 2026-09-23 · #1014 · untested production path · "Acked only once consumed" was not what the code did: every pending follow-up was completed on any `result`. The claude CLI can answer a turn while a pushed prompt is still queued behind it, so that result acked a push the model had not read, and a container dying before the queued turn lost it: the original bug in a narrower window. The runner tests drove a fake query whose `push()` returned no prompt id, which is the any-result fallback path. Production's claude provider returns ids and can queue, so the suite never exercised the shipped behaviour · follow-ups are completed only when their pushed prompt id is in the result's `answeredPrompts` or a later `settled` (`container/agent-runner/src/poll-loop.ts:2105`, applied at `container/agent-runner/src/poll-loop.ts:2804` and `container/agent-runner/src/poll-loop.ts:3021`). The fake query now returns ids the way the claude provider does, in `container/agent-runner/src/poll-loop.test.ts:6030`, `:6043` and `:6054`; restoring any-result completion fails 2, and dropping the `settled` branch fails 1
- 2026-09-23 · #1014 · uncited behavioural claim · The review's P1 fix said to key the forgiveness on `provider_last_event_at`, "already in ContainerState". The column is in the schema, but only the Codex provider writes it (`setProviderHealthState` from `providers/codex.ts`), and container startup NULLs it for every provider. On the claude path it is always NULL, so a fix built on it would never have forgiven anything there. The claim cited where the column is declared, not where it is written. Checking the writer before building caught it · the signal actually used, `provider_query_event_at`, is created by the container's schema backfill (`container/agent-runner/src/modules/mailbox/schema.ts:207`) and written by the runner (`container/agent-runner/src/modules/mailbox/container-state.ts:192`, `container/agent-runner/src/modules/mailbox/container-state.ts:198`). Its writes are pinned where they happen (`container/agent-runner/src/poll-loop.test.ts:6080`) and its read where the host does it (`src/modules/mailbox/mailbox.test.ts:1064`)
