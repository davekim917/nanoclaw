- 2026-09-20 · #961 · uncited behavioural claim · A comment asserted an ordering the boot does not have ("runs AFTER the step above, which is the only thing that legitimately adds a name to the shared tree during this boot"). `reconcileWorkgroupFsState` runs at `src/main.ts:683`; `runBootMountQuiescence` at `:693` then calls `reconcileWorkgroupSharedDirs` and the memory gate, both of which add shared names after the prune has taken its listing. The safety was real but rested on two different facts — reserved names, and `migrateWorkgroup` writing a shared entry before its compat link — neither of which was the one claimed. Read the caller before asserting what runs when · the comment now cites the two facts the safety actually rests on (`src/modules/workgroup/fs-reconcile.ts`)
- 2026-09-20 · #961 · mutation coverage · "survives a member folder that does not exist yet" passed with its guard removed: the populated member is scanned first, so the assertion's outcome is already true before the missing folder throws, and the outer per-workgroup catch swallows the throw so `not.toThrow()` held too. A case whose subject never decides the outcome is not covering it · the case asserts the workgroup was not skipped wholesale, which only the guard can satisfy (`src/modules/workgroup/fs-reconcile.test.ts`)
- 2026-09-20 · #961 · mutation coverage · "prunes NOTHING when the shared tree cannot be listed" passed with the bail removed, because the mock called `fs.readdirSync` — by then itself — and the member scan died of recursion into its own try/catch, so the doctored call was never actually made. Capture the real implementation before `vi.spyOn`, and assert the doctored call happened · the spy closes over the captured original and the case asserts the call was made (`src/modules/workgroup/fs-reconcile.test.ts`)
- 2026-09-20 · #961 · mutation coverage · `expect(spy).toHaveBeenCalledWith(...)` after `spy.mockRestore()` always sees zero calls, so the assertion passed against any implementation. Assert before restoring · the assertions moved ahead of the restore (`src/modules/workgroup/fs-reconcile.test.ts`)
- 2026-09-20 · #961 · mutation coverage · A re-confirm added at review was untestable by the obvious test: the guard sits after `sharedNames.has(name)`, so any fixture with the name in the listing never reaches it. Killing it needed the name absent from the listing but real on disk — the actual race — with a dangling symlink as the payload, the only shape where `lstat` (name taken) and `existsSync` (does not resolve) disagree. When a mutation survives, check whether an earlier clause short-circuits the one under test before concluding the test is adequate · the case builds the dangling-symlink shape (`src/modules/workgroup/fs-reconcile.test.ts`)
- 2026-09-20 · #961 · mutation coverage · The source called `sharedNames.has(name)` the guard the whole design rests on, and it survived both removal and replacement by `existsSync`: the `lstat` re-confirm added one round earlier answers "present" in every fixture that has the name on disk, masking the clause beneath it. Killing it needed the shape neither guard can fake — the shared tree vanishing after its listing succeeded. Adding a defensive layer can silently retire the coverage of the layer beneath it; re-run the older mutations after adding one · a case removes the tree between listing and re-confirm (`src/modules/workgroup/fs-reconcile.test.ts`)
- 2026-09-20 · #961 · fail-open default · A listing failure must not read as "nothing is there". The shared tree is read once per workgroup and a failure returns; a per-link `existsSync` would answer "gone" for every name the instant that tree were unreadable, deleting the workgroup's entire compat layer. The listing counts a name, so a resolving check disagrees with it for an entry that exists but dangles · the read bails on failure and the re-confirm uses `lstat`, not `existsSync` (`src/modules/workgroup/fs-reconcile.ts`)
- 2026-09-20 · #961 · fail-open default · The prune's gate was copied from the other steps (`WORKGROUP_SHARED_FS || .migrated`). Under the flag this install sets, that is fail-OPEN in the one failure mode worth defending against: if the shared tree is lost the marker inside it goes too, and step 2's recursive `mkdirSync` recreates the directory before step 3 looks, so the listing SUCCEEDS with `['artifacts']` and every real name reads as gone — on a six-member workgroup, every compat link deleted in one boot, with nothing thrown for the readdir bail to catch. Copying a sibling's predicate is not choosing one: ask what each disjunct admits in the state you are defending against · the gate requires the marker, which only `migrateWorkgroup` writes (`src/modules/workgroup/fs-reconcile.ts`)
- 2026-09-20 · #961 · destroy before replace · The prune deletes the very links `reconcileWorkgroupSharedDirs` uses to re-derive the established shared set, and that pass runs LATER in the same boot. Without a guard, an agent who removed the shared entry and re-created the seed's real dir would have the name silently fall out of the shared set into `candidates`, private to the seed, with no warning. Before deleting state, ask which later pass reads it · the prune leaves names the later union re-derives (`src/modules/workgroup/fs-reconcile.ts`)
- 2026-09-20 · #961 · stale state · Three successive versions of the step-3 comment gave three different reasons for the same line of code, the first two wrong; requiring the marker then made the ordering not load-bearing at all, since the marker lives inside the shared tree so nothing step 2 creates can change step 3's answer. When a fix lands, re-read the comments that justified the code it replaced — they do not fail loudly when they go stale · the comment states the marker invariant rather than an ordering (`src/modules/workgroup/fs-reconcile.ts`)
