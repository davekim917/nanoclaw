- 2026-09-17 · #902 · inherited claim · `hasChatOutboundAfter` decided "did the person get a reply" by modelling where an outbound row lands, and was wrong three times in three review rounds: `kind = 'chat'` missed the `chat-sdk` rows `send_card` writes (`container/agent-runner/src/mcp-tools/interactive.ts:156`); an unconditional `threadKey` exclusion ignored that delivery re-parents a keyed row only when it has no thread_id (`src/delivery.ts:1575`), a guard I had read an hour earlier; and a thread-exact match assumed replies carry the person's thread, when final-text blocks are stamped from the channel's newest inbound row or the batch anchor (`sendToDestination` in `container/agent-runner/src/poll-loop.ts`). Each wrong "no" is a false nudge and a duplicate answer — worse than main, which never nudged. An enumeration of row shapes is a claim about every writer, and there is no single authority for a reply's thread · `container/agent-runner/src/modules/mailbox/reads.ts` now matches kind and channel+platform only, fails open, and its comment states the asymmetry: a wrong "yes" equals main, a wrong "no" does not
- 2026-09-17 · #902 · mutation coverage · Two tests restated the code's belief instead of sitting where it could be wrong: the first keyed-thread case wrote its row with no thread_id against a null-thread debt (the one configuration where the wrong clause and the right one agree), and the row-derived conversation had no case at all because the test helper writes no routing columns, so reverting that change kept the suite green. Both were found by a reviewer, not by me · `container/agent-runner/src/poll-loop.test.ts` has a case whose person-row names a different channel from the batch routing; it and the card case were each run against the reverted code and fail there
- 2026-09-17 · #902 · pattern audit · Round 1 built the conversation-scoped reply check for the `send_message` path while the text branch of the same `result` handler kept settling the debt on any delivered block, wherever it went — a second site of one invariant that did not call the primitive · `container/agent-runner/src/poll-loop.ts` settles the text branch through the same `hasChatOutboundAfter` call as the empty branch
