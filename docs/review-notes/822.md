- 2026-09-15 · #822 · enforcement scope · A deploy's "everything now runs the new code" guarantee bound only to the one unit `scripts/deploy.sh` named, so the fork's second long-running service kept serving whatever it booted with: nanoclaw-codex-sync.service runs `pnpm exec tsx src/codex-sync-watcher.ts`, tsx loads the source once at process start, and a two-day-old watcher re-mirrored the pre-#813 `CODEX_WORKER_MODELS` map over `~/.codex/agents` and all 18 `groups/*/.codex/agents` on every watched change, reverting a hand-fixed value three times. A second hardcoded name would have bound just as narrowly at the third unit, and this host already carries a third Type=simple nanoclaw unit file; the restart set has to be DISCOVERED from the property that actually defines the bug (an active non-`oneshot` service whose `WorkingDirectory` is this checkout) and the discovery has to carry its own control, because a broken enumeration answers "no siblings" in exactly the words a sibling-free host uses. Ordering is part of the scope too: `systemctl restart nanoclaw-v2` SIGTERMs the cgroup the detached deploy script lives in, so a restart appended after it is on a line that never runs and a test asserting the unit name appears in the script would pass over it · `long_running_repo_units` in `scripts/deploy.sh`, with nanoclaw-v2.service as the enumeration's own control (a discovery that loses it refuses the deploy) and a failing restart writing `status: failed` before the handoff; exercised — not grepped — in `src/deploy-sibling-restart.test.ts`, which runs the real tail of the script against a fake `systemctl` that SIGKILLs its caller on the `nanoclaw-v2` restart so anything past that line is unreachable in the harness too (10/10 mutations killed, including the relocated-block and swallowed-failure ones)
- 2026-09-15 · #822 · pattern audit · Recurrence of the same defect inside the fix for it, found by review r1: the pre-handoff shell trap was taught to put a restarted sibling back onto the restored checkout, and the OTHER rollback path that shares the shape — the post-handoff crash guard, which resets the checkout three boots later after `systemctl restart nanoclaw-v2` has already killed the deploy shell — was not audited and kept resetting the commit while leaving the watcher resident on the rejected code, under a status line announcing a clean automatic rollback. A rollback is a pattern with more than one site, and a fix written for the site the defect was found at has to be carried to every site that undoes the same thing. The interesting constraint is that the second site cannot re-derive the set: the shell's in-memory list dies with the shell, and re-deriving at rollback time would both act on the host three boots later rather than on what this deploy did (the argument `imageBase` already makes) and put a second copy of the discovery predicate in another language with nothing to catch the drift — so the deploy records it in the manifest that already carries every other deploy-time fact, written after the restarts so reaching it proves they came back · `restartedUnits` in the data/deploy-rollback.json manifest, emitted after the restart loop in `scripts/deploy.sh` and read fail-closed by `readRestartedUnits` in `src/deploy-crash-guard.ts`, where absent, empty and malformed are three distinct reported answers rather than one silence and a failed restart lands in the operator-facing status string; the producing half is pinned in `src/deploy-sibling-restart.test.ts` and the consuming half drives `performRollback` against a recording double in `src/deploy-crash-guard.test.ts` (19/19 mutations killed across both)
- 2026-09-15 · #822 · unescaped interpolation · Found by review r2: the crash-guard rollback manifest was built with a `printf '{"commit":"%s",…,"restartedUnits":[%s]}'` template, which is only correct while no value carries a JSON metacharacter — and the very field this PR added can. systemd escapes any byte a unit id may not hold literally as a backslash-x pair, so a legitimately-named unit puts a BACKSLASH in the list and the template emits something that is not valid JSON. Nothing fails at the write: the deploy reports ok, the restart hands off, and on the next boot `readJson` catches the parse error and answers null, `evaluateBoot` reads null as `no-op`, and the crashing deployment silently loses automatic rollback for the host AND every sibling — strictly worse than the defect the field was added to fix. The lesson is not "escape the backslash": a structured artifact gets written by an encoder, every field of it, because "these particular producers cannot emit a metacharacter today" is a property of today's producers that the next field added will not inherit · `scripts/write-deploy-rollback-manifest.mjs` takes its values from the ENVIRONMENT (never argv) and emits the whole object through a JSON encoder, writing to a temp file that is renamed, with `scripts/deploy.sh` refusing the handoff if it cannot be produced; round-trip cases for a unit id carrying a backslash, a quote and a control character live in `scripts/write-deploy-rollback-manifest.test.ts`, and the end-to-end path from discovery through the restart loop to a parsable manifest in `src/deploy-sibling-restart.test.ts` (7 mutations killed, including restoring the template)
- 2026-09-15 · #822 · fail-open default · Also r2, in the three-way reader written for the line above: `restartedUnits` set to an explicit null took the `undefined` branch, so a manifest that is valid JSON but says something wrong was read as a manifest from before the field existed — the checkout reset, nothing restarted, and the rollback reported complete, which is exactly the collapse of present-but-malformed into "nothing to do" that having three answers instead of two exists to prevent. JSON cannot express `undefined`, so a parsed manifest yields it exactly when the writer omitted the key: a MISSING KEY is the legacy signal and nothing else is. The test shipped asserting the wrong branch, so it was pinning the defect rather than catching it — an oracle written from the same belief as the code never disagrees with it · `readRestartedUnits` in `src/deploy-crash-guard.ts` answers absent only for `undefined`, with null and every other non-list reported as malformed and named in the operator-facing rollback status; the null case is in both the reader's own table and the `performRollback` malformed loop in `src/deploy-crash-guard.test.ts`, mutation-checked against restoring the null-as-absent branch
