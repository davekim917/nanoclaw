- 2026-09-22 · #1010 · hook tests · A shared Bash-rewrite hook is registered by two chains that each had their own test file, so validating a change by running the editing chain's file left the other chain's file red for three review rounds; enumerating the covering files was already tried and recorded as a non-mechanism, so the prefix contract for BOTH chains now lives in one file — a partial run is structurally impossible for this hook, though nothing yet forces that file to run per-PR (only nightly ci-full) · `container/agent-runner/src/providers/claude.stdinEmit.test.ts`
- 2026-09-22 · #1010 · deferred hardening · Stdin closure is enforced on the Claude provider only, so a Codex or OpenCode group running a command that reads stdin is still exposed to the same 30-minute hang; recorded rather than fixed because whether codex applies a hook's rewritten updatedInput, and under which key, is unproven, and emitting it on every Bash call would be unverified surface on every Codex container · #1038 (re-raise on the first hang of this shape on a non-Claude group, or once fd 0 for a non-Claude tool process is shown to be an unwritten socket)
