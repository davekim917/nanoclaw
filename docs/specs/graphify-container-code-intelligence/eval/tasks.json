{
  "version": 1,
  "tasks": [
    {
      "id": "container-resource-resolution",
      "prompt": "Investigate how NanoClaw resolves container memory request, hard limit, swap limit, CPU, and PID defaults, including validations for inconsistent memory settings. Inspect the frozen repository under repo/ and read the authoritative source before returning the structured claims.",
      "expectedFiles": ["src/config.ts", "src/container-resources.ts"],
      "claims": [
        {
          "id": "install_default_memory_limit",
          "question": "What exact built-in string supplies the install memory limit when no environment override exists?",
          "type": "string",
          "expected": "3g"
        },
        {
          "id": "request_default_source",
          "question": "When no group memory request is declared, which install default supplies requestMb?",
          "type": "enum",
          "options": ["memoryReservation", "memoryLimit", "fixed5120"],
          "expected": "memoryReservation"
        },
        {
          "id": "omitted_request_with_explicit_limit",
          "question": "When limitMb is explicitly declared and requestMb is omitted, what value supplies requestMb?",
          "type": "enum",
          "options": ["limitMb", "memoryReservation", "unset"],
          "expected": "limitMb"
        },
        {
          "id": "request_may_exceed_limit",
          "question": "May a valid requestMb be greater than limitMb?",
          "type": "boolean",
          "expected": false
        },
        {
          "id": "swap_may_be_below_limit",
          "question": "May a valid memorySwapLimitMb be less than limitMb?",
          "type": "boolean",
          "expected": false
        },
        {
          "id": "omitted_swap_without_explicit_limit",
          "question": "When neither a group limit nor group swap limit is declared, which install default supplies memorySwapLimitMb?",
          "type": "enum",
          "options": ["memorySwapLimit", "memoryLimit", "unset"],
          "expected": "memorySwapLimit"
        },
        {
          "id": "omitted_swap_with_explicit_limit",
          "question": "When limitMb is explicitly declared and memorySwapLimitMb is omitted, what value supplies memorySwapLimitMb?",
          "type": "enum",
          "options": ["limitMb", "memorySwapLimit", "unset"],
          "expected": "limitMb"
        },
        {
          "id": "install_default_cpu_limit",
          "question": "What CPU limit is applied by the built-in install default when no environment or group override exists?",
          "type": "enum",
          "options": ["unset", "one-cpu", "two-cpus"],
          "expected": "unset"
        },
        {
          "id": "install_default_pids_limit",
          "question": "What numeric PID limit is supplied by the built-in install default?",
          "type": "number",
          "expected": 512
        }
      ]
    },
    {
      "id": "graphify-container-contract",
      "prompt": "Investigate the universal Docker arguments NanoClaw adds for Graphify and how they coexist with ordinary container memory flags. Inspect the frozen repository under repo/ and read the authoritative source before returning the structured claims.",
      "expectedFiles": ["src/container-runner.ts", "src/container-resources.ts"],
      "claims": [
        {
          "id": "container_marker_value",
          "question": "What numeric value is assigned to NANOCLAW_CONTAINER?",
          "type": "number",
          "expected": 1
        },
        {
          "id": "stage_mount_path",
          "question": "What exact container path receives the Graphify tmpfs stage mount?",
          "type": "string",
          "expected": "/workspace/.graphify-stage"
        },
        {
          "id": "stage_tmpfs_bytes",
          "question": "What exact byte size is configured for the Graphify tmpfs?",
          "type": "number",
          "expected": 201326592
        },
        {
          "id": "stage_tmpfs_uid",
          "question": "What uid owns the Graphify tmpfs mount?",
          "type": "number",
          "expected": 1001
        },
        {
          "id": "stage_tmpfs_gid",
          "question": "What gid owns the Graphify tmpfs mount?",
          "type": "number",
          "expected": 1001
        },
        {
          "id": "memory_reservation_distinct_from_limit",
          "question": "Are --memory-reservation and --memory distinct resolved Docker settings?",
          "type": "boolean",
          "expected": true
        },
        {
          "id": "docker_memory_source",
          "question": "Which resolved memory field maps to Docker --memory?",
          "type": "enum",
          "options": ["limitMb", "requestMb", "memorySwapLimitMb"],
          "expected": "limitMb"
        },
        {
          "id": "docker_memory_reservation_source",
          "question": "Which resolved memory field maps to Docker --memory-reservation?",
          "type": "enum",
          "options": ["requestMb", "limitMb", "memorySwapLimitMb"],
          "expected": "requestMb"
        },
        {
          "id": "docker_memory_swap_source",
          "question": "Which resolved memory field maps to Docker --memory-swap?",
          "type": "enum",
          "options": ["memorySwapLimitMb", "limitMb", "requestMb"],
          "expected": "memorySwapLimitMb"
        },
        {
          "id": "docker_memory_swap_semantics",
          "question": "What does Docker --memory-swap represent in this contract?",
          "type": "enum",
          "options": ["total-ram-plus-swap", "swap-only", "ram-only"],
          "expected": "total-ram-plus-swap"
        }
      ]
    },
    {
      "id": "graphify-cache-identity",
      "prompt": "Investigate how NanoClaw chooses host Graphify cache and worker-lock directories for ordinary sessions versus thread-worktree siblings, including the container mount points. Inspect the frozen repository under repo/ and read the authoritative source before returning the structured claims.",
      "expectedFiles": ["src/session-manager.ts", "src/container-runner.ts"],
      "claims": [
        {
          "id": "ordinary_cache_helper",
          "question": "Which helper selects the host Graphify cache for an ordinary session?",
          "type": "enum",
          "options": ["sessionGraphifyCacheDir", "threadGraphifyCacheDir", "gitDir"],
          "expected": "sessionGraphifyCacheDir"
        },
        {
          "id": "thread_sibling_cache_helper",
          "question": "Which helper selects the shared host Graphify cache for thread-worktree siblings?",
          "type": "enum",
          "options": ["threadGraphifyCacheDir", "sessionGraphifyCacheDir", "gitDir"],
          "expected": "threadGraphifyCacheDir"
        },
        {
          "id": "thread_worktrees_marker_value",
          "question": "What numeric value must NANOCLAW_THREAD_WORKTREES equal for thread-worktree behavior?",
          "type": "number",
          "expected": 1
        },
        {
          "id": "cache_container_mount",
          "question": "What exact container path receives the Graphify cache mount?",
          "type": "string",
          "expected": "/workspace/.cache/graphify"
        },
        {
          "id": "worker_lock_container_mount",
          "question": "What exact container path receives the Graphify worker-lock runtime mount?",
          "type": "string",
          "expected": "/run/nanoclaw-graphify"
        }
      ]
    },
    {
      "id": "memory-admission-priority",
      "prompt": "Investigate MemoryAdmissionController behavior for interactive versus scheduled work, duplicate requests, over-budget requests, release, and FIFO ordering. Inspect the frozen repository under repo/ and read the authoritative source before returning the structured claims.",
      "expectedFiles": ["src/memory-admission.ts"],
      "claims": [
        {
          "id": "interactive_before_scheduled",
          "question": "Is interactive work ordered before scheduled work?",
          "type": "boolean",
          "expected": true
        },
        {
          "id": "fifo_within_priority",
          "question": "Is FIFO preserved within each priority class?",
          "type": "boolean",
          "expected": true
        },
        {
          "id": "oversize_request_result",
          "question": "Which result identifies a request that exceeds the total memory budget?",
          "type": "enum",
          "options": ["request_exceeds_budget", "queued", "admitted"],
          "expected": "request_exceeds_budget"
        },
        {
          "id": "release_drains_newly_admissible",
          "question": "Does release drain queued work that becomes newly admissible?",
          "type": "boolean",
          "expected": true
        },
        {
          "id": "already_reserved_duplicate_status",
          "question": "What status is returned when request is called for an ID that already has a reservation?",
          "type": "enum",
          "options": ["admitted", "queued", "rejected"],
          "expected": "admitted"
        },
        {
          "id": "queued_scheduled_retry_promotes_interactive",
          "question": "Does retrying an already queued scheduled ID with interactive priority promote that queued entry to interactive?",
          "type": "boolean",
          "expected": true
        }
      ]
    },
    {
      "id": "command-gate-routing",
      "prompt": "Investigate the host command gate ordering for threaded context, leading mentions, filtered commands, dashboard-token interception, admin commands, and unknown slash commands. Inspect the frozen repository under repo/ and read the authoritative source before returning the structured claims.",
      "expectedFiles": ["src/command-gate.ts"],
      "claims": [
        {
          "id": "final_latest_message_selects_text",
          "question": "Does the final [Latest message] marker select the user text?",
          "type": "boolean",
          "expected": true
        },
        {
          "id": "leading_mentions_stripped",
          "question": "Are leading formal and bare mentions stripped before command matching?",
          "type": "boolean",
          "expected": true
        },
        {
          "id": "dashboard_before_fanout",
          "question": "Is /dashboard-token intercepted before fan-out?",
          "type": "boolean",
          "expected": true
        },
        {
          "id": "unknown_slash_disposition",
          "question": "How are unknown slash commands handled?",
          "type": "enum",
          "options": ["pass-through", "filtered", "rejected"],
          "expected": "pass-through"
        },
        {
          "id": "admin_authorization_location",
          "question": "Where is admin authorization checked?",
          "type": "enum",
          "options": ["host", "container"],
          "expected": "host"
        },
        {
          "id": "filtered_command_action",
          "question": "What gate action is returned for a command in FILTERED_COMMANDS?",
          "type": "enum",
          "options": ["filter", "pass", "deny"],
          "expected": "filter"
        },
        {
          "id": "authorized_admin_command_action",
          "question": "What gate action is returned for an authorized command in ADMIN_COMMANDS?",
          "type": "enum",
          "options": ["pass", "filter", "deny"],
          "expected": "pass"
        },
        {
          "id": "unauthorized_admin_command_action",
          "question": "What gate action is returned for an unauthorized command in ADMIN_COMMANDS?",
          "type": "enum",
          "options": ["deny", "pass", "filter"],
          "expected": "deny"
        }
      ]
    },
    {
      "id": "timezone-conversion-contract",
      "prompt": "Investigate NanoClaw timezone validation, display formatting, and conversion of naive versus offset-bearing timestamps to UTC, including the invalid-timezone fallback. Inspect the frozen repository under repo/ and read the authoritative source before returning the structured claims.",
      "expectedFiles": ["src/timezone.ts"],
      "claims": [
        {
          "id": "invalid_timezone_result",
          "question": "What does resolveTimezone return for an invalid IANA timezone?",
          "type": "enum",
          "options": ["UTC", "throw", "host-timezone"],
          "expected": "UTC"
        },
        {
          "id": "offset_timestamp_path",
          "question": "How are offset-bearing timestamps parsed?",
          "type": "enum",
          "options": ["Date-constructor-direct", "resolved-zone-wall-clock", "host-local"],
          "expected": "Date-constructor-direct"
        },
        {
          "id": "naive_timestamp_path",
          "question": "How are naive timestamps interpreted?",
          "type": "enum",
          "options": ["resolved-zone-wall-clock", "Date-constructor-direct", "host-local"],
          "expected": "resolved-zone-wall-clock"
        },
        {
          "id": "local_stamp_locale",
          "question": "Which exact locale does formatLocalStamp use?",
          "type": "string",
          "expected": "sv-SE"
        },
        {
          "id": "local_time_locale",
          "question": "Which exact locale does formatLocalTime use?",
          "type": "string",
          "expected": "en-US"
        },
        {
          "id": "local_time_invalid_zone_behavior",
          "question": "Which timezone does formatLocalTime use when given an invalid timezone?",
          "type": "enum",
          "options": ["UTC", "host-timezone", "throw"],
          "expected": "UTC"
        }
      ]
    }
  ]
}
