- 2026-09-17 · #906 · pattern audit · To avoid re-sending a `<message>` block already delivered mid-turn, the result handler removed the block from the final text before calling `dispatchResultText`. That function decides three things from `sent`: whether the leftover prose goes to the person through the unwrapped-output fallback, whether the wrapping nudge fires, and whether an errored turn re-posts the text. Editing its input changed all three — a verbatim repeat plus one sentence of narration posted the narration to the channel, which main never did. A value that a function uses as a decision input cannot be adjusted from outside without auditing every branch that reads it · `container/agent-runner/src/poll-loop.ts` passes the delivered spans into `dispatchResultText` as `alreadyDelivered`, which skips the send and still counts it
- 2026-09-17 · #906 · pattern audit · The interim path pre-checked "is this destination addressable now" with its own name lookup (trimmed, `here` and peers assumed resolvable) and then called a dispatcher whose lookup is untrimmed and whose `here`/peer recovery needs a resolvable origin. Where the two disagreed the block fell into the dispatcher's unknown-destination branch and its `[dropped: …]` note reached the channel through the fallback. Two implementations of one question, written an hour apart · `container/agent-runner/src/poll-loop.ts` has no pre-check: mid-turn text goes through `dispatchResultText` with `blocksOnly`, which never falls back, and only a block it actually routed is recorded
- 2026-09-17 · #906 · mutation coverage · To keep a `<message>` block quoted in code from being sent, the interim path deleted code spans from the text and then matched blocks in what was left — so the block it delivered was the post-deletion string. A real update lost every backticked sha and path and all fenced output, and because the mutilated span was what got recorded, the final text's intact repeat no longer matched and was sent again. Every test body was plain prose, so nothing exercised a filter whose whole subject is code. A filter that decides where things are must not be the thing that produces what is sent · `container/agent-runner/src/poll-loop.ts` masks code spans to equal-length spaces to locate blocks and cuts each block from the original text; a test delivers a block carrying inline and fenced code and repeats it verbatim
